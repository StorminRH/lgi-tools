---
description: pstack per-role model choices (overrides skill defaults)
alwaysApply: true
---
# pstack model configuration. One line per role. Delete a line to fall back to the skill default.
# `inherit-parent` or `auto` as a value: the role runs on the parent chat model (omit Task `model`). Alias entries in a panel list still count toward its fan-out.
# Cloud VMs copy this file from .cursor/vm-home onto ~/.cursor. Keep it out of .cursor/rules so Cursor does not load it as a project rule.
# budget: unlimited (max)
feature, refactoring: cursor-grok-4.6-xhigh
bug-fix: muse-spark-1.3-max
perf-issue: muse-spark-1.3-max
hillclimb: muse-spark-1.3-max
judgment and prose: muse-spark-1.3-max
hardest tasks: muse-spark-1.3-max
how explorer: cursor-grok-4.6-xhigh
how explainer: muse-spark-1.3-max
why investigators: cursor-grok-4.6-xhigh
why synthesizer: muse-spark-1.3-max
reflect tooling: muse-spark-1.3-max
reflect judgment, divergent, synthesizer: muse-spark-1.3-max
arena runners: muse-spark-1.3-max, cursor-grok-4.6-xhigh, glm-5.2-high
arena cross-judge pool: muse-spark-1.3-max, cursor-grok-4.6-xhigh, glm-5.2-high
swarm workers: cursor-grok-4.6-xhigh
architect runners: muse-spark-1.3-max, cursor-grok-4.6-xhigh, glm-5.2-high
interrogate reviewers: muse-spark-1.3-max, cursor-grok-4.6-xhigh, glm-5.2-high
