import { AtlasLandingFallback } from './AtlasLandingFallback';

/** Instant site-shaped shell for /atlas while the administrator gate resolves. */
export default function AtlasLoading() {
  return <AtlasLandingFallback />;
}
