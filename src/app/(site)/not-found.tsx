import { NotFoundContent } from '@/components/composition/NotFoundContent';

/**
 * Renders a segment-level 404 inside the surrounding site layout.
 */
export default function SiteNotFound() {
  return <NotFoundContent />;
}
