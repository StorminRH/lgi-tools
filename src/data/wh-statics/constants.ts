/** Canonical community feed containing per-system wormhole static assignments. */
export const WH_STATICS_FEED_URL = 'https://anoik.is/static/static.json';

/** Fail-fast timeout for the small anoik.is JSON response. */
export const WH_STATICS_FETCH_TIMEOUT_MS = 10_000;

/** Retention window for reviewed or superseded statics snapshots. */
export const WH_STATICS_SNAPSHOT_RETENTION_DAYS = 400;
