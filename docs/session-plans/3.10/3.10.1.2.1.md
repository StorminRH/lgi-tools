# Session 3.10.1.2.1 Implementation Plan — Day-one responsibility-layer restructure with zero-exception boundaries

**Plan status:** Approved
**Approved:** 2026-07-23
**Contract:** `docs/session-contracts/3.10/3.10.1.2.1.md`
**Contract digest:** `sha256:92da24984161141357dc199fe57180abfc2af83e80320c01d86dbbb815812fe7`
**Planning standard:** `docs/workflows/schema/session-plan.md`
**Execution status:** Pending
**Baseline effect:** Improves

## Bottom line (READ FIRST)

- **GOAL:** The source tree has the shape it would have had with boundaries
  from day one — cross-slice composition in a composition band above the
  slices (`src/composition` for server orchestration,
  `src/components/composition` for app-shell UI including auth's account
  components), capabilities below them
  (`src/platform/{auth,esi,owner-sync,search,purge,page-settings}`), request
  plumbing in `src/transport`, the whole online-status slice in `src/data`,
  the identity schema in the database band, the role vocabulary in the
  configuration band, and the tsx entries in `src/scripts` — enforced by
  one-way Fallow rules with zero cross-layer exceptions, blocking cycle
  detection, and a lint-enforced server-only rail.
- **DONE =** SC-1 through SC-15 below, plus this observable result: the
  relocated tree passes `pnpm verify` with every seeded boundary violation
  failing and every legitimate edge green, the final boundary configuration
  contains no upward allow, no slice-peer allow outside the declared band
  orders, and no exception entry, and the local Convex push plus the
  complete tsx CLI matrix run clean on the moved graph.
- **OUT OF SCOPE:**
  - Any runtime behavior change, server/client conversion, or component
    redesign — every relocation is a file move or a named export-seam split
    plus import re-pointing, with the single sanctioned owner-reconcile
    inversion seam as the contract's HC-4 states.
  - The Phase 2 transport spine (typed failure set, problem mapper, mutation
    pipeline); this session only creates the `src/transport` home they later
    land in.

<hard_constraints>

- **Contract HC-1:** The primary in-repo server-only gate is the ESLint
  restricted-import rule running inside `pnpm verify`/lint; the `server-only`
  package guard fires only at the Vercel production build and is a backstop,
  never the enforcing gate. Consequence: the lint rail and enumeration test
  must be red-proven before the markers count as evidence, and the
  enumeration walk (also inside verify) closes the `'use client'` files
  under `src/app` that path globs cannot express.
- **Contract HC-2:** The cycle rules flip to `error` only after a
  confirmed-clean dependency-graph run on the relocated map; no cycle may be
  suppressed, ignored, or waived to achieve the flip.
- **Contract HC-3:** Zero cross-layer exceptions in the final map: no allow
  edge points from a lower layer to a higher one, no peer allow exists
  between feature slices or between data slices outside the declared
  eve-data reference-core order, platform-internal edges follow the declared
  acyclic capability order (auth and owner-sync above esi and the socket
  contracts) with no two-way pair, and no former "recorded pattern" survives
  as an allow entry; anything that appears to need one is a misplacement to
  fix by moving or splitting, or a split-trigger stop for an operator
  ruling.
- **Contract HC-4:** No application behavior changes and the typed-env rail
  is untouched; every moved or split module keeps its exact exports and
  logic, with the single sanctioned exception of the named owner-reconcile
  inversion seam, whose behavior preservation is proven by the wiring
  import in the auth route's module graph and the existing auth flow tests.
- **Contract HC-5:** Convex isolate modules and the Convex-bundled and
  CLI-shared server graphs are exempt from `server-only` markers only
  because the marker throws in non-Next runtimes; each runtime exemption is
  recorded with its reason and is not a dependency-direction exception.
- **Contract HC-6:** No Fallow waiver, baseline entry, threshold relaxation,
  or undeclared boundary exception is added to reach a green run.
- **Contract HC-7:** Each relocation family lands as its own commit with
  `pnpm verify` green before the next family begins — twelve commits, one
  per Ordered work step, each carrying its own zone-map increment (including
  the Fallow `entry` array when entries move) and any ESLint glob updates
  its files invalidate. Interim per-step maps may carry the temporary
  documented allows enumerated under Edge and failure behavior; step 9
  deletes every one of them, and HC-3 binds the final map.
- **Contract HC-8:** No production-mode build runs locally; deployment-shaped
  proof comes from the local Convex push, the tsx CLI matrix, the Neon
  branch drill, and optional manual preview deployments removed after use —
  the shared-state drills run only after their named operator authorization.
- **Plan:** The Convex-bundled chain (`platform/esi/**`,
  `lib/fetch-with-timeout`, `lib/sync-engine`,
  `data/online-status/esi-projection` and `sync-eligibility`,
  `platform/auth/api-contract`) must remain free of `next/*`, React, and
  `server-only` imports; the local Convex push gates that invariant.
- **Plan:** Import re-pointing covers `@/` specifiers, relative imports
  inside moved families, test-double string literals (`vi.mock`,
  `vi.doMock`, `vi.importActual`), and stale old-path mentions in code
  comments, so the cumulative grep can demand zero hits. The structural
  changes are the six named export-seam splits (eve-sso constants; the
  `startCharacterLink` action; the account-delete purge flow out of
  `account-purge`; the role vocabulary out of the identity schema; the
  `SdePipelineSummary` type; the search contract's structural types) plus
  the single owner-reconcile inversion seam — nothing else edits logic.
- **Plan:** `package.json` script entries and the Fallow `entry` array move
  with the scripts family in the same commit, and `drizzle.config.ts`
  re-points with the aggregator's move, so no deploy-critical entry ever
  dangles and no relocated entry script reads as unused.

</hard_constraints>

**Branch:** `lifecycle/3.10.1.2` · **ends in PR:** yes · **gate:** per-step commits with green `pnpm verify` on the preserved lifecycle source branch; the operator-approved review-cap amendment delivers that source history through three sequential PRs, each held until Greptile and CodeRabbit both settle before any batched response or push

**Contract UX gate:** `No` · **required pause:** no browser-design pause; execution stops for explicit operator authorization before the Neon branch drill and before any optional preview deployment (shared-state actions)

## Read first

- `AGENTS.md`
- `docs/session-contracts/3.10/3.10.1.2.1.md`
- `.fallowrc.json`
- `docs/architecture-boundaries.md`
- `eslint.config.mjs`

## Current state and prerequisites

| Contract input | Live verdict | Evidence | Execution consequence |
| --- | --- | --- | --- |
| `DEP-1` | `Verified` | 3.10.1.1 shipped the 44-zone deny-by-default map; `fallow list --boundaries` reports zero unclassified production files; `.fallowrc.json` sets `coverage.requireAllFiles: true` and lists the tsx entries in `entry` | Every move commit must extend the zone map — and the `entry` array when entries move — in the same commit, or that step's verify run fails |
| `DEP-2` | `Verified` | Census 2026-07-23: registry aggregators are client boot modules side-effect-imported by `AppHeaderShell.tsx:28` and `PageMenuProvider.tsx:38`; `absorbLinkedCharacterOnProof` is purge-free but `reconcileCharacterOwner` transitively calls `runPurge` through `purgeTransferredCharacter` (`owner-transfer.ts:131,159`) and is invoked from `auth.ts:182-190`; every auth UI component is imported only by `src/app/**` and shell composition; `table-growth-registry` has zero production importers; sync workers are imported only by API routes and the cron dispatcher; `src/db` orchestration (`sde-pipeline`, `esi-snapshot-retention`, `cron-gate`, and `schema.ts` — the drizzle aggregator whose four runtime consumers each import their own slice's tables through it) composes higher slices; `data/commands` composes auth endpoints, transport, and the search contract; `data/telemetry`/`preferences` FK-import only auth's `schema.ts` tables; `api-contract.ts` runtime-imports `CHARACTER_ROLES` from the schema into the client graph; `lib/owner-sync` type-imports the ESI gate; transport's own imports are `@/lib/service-auth` and `zod` only | Every former "pattern" resolves by placement: aggregators and orchestration lift into the composition band, the identity schema drops into the database band with the role vocabulary dropping to configuration, the purge-transitive reconcile pair hoists behind the owner-reconcile inversion seam, `data/commands` becomes the composition app-command source, owner-sync becomes a platform capability above transport-free `esi`, and no allow entry survives |
| `DEP-3` | `Verified` | `src/features/online-status` imports only `@/lib/env`, `@/lib/fetch-with-timeout`, `@/lib/sync-engine`, and `@/purge/types` — nothing from `@/features`, `@/db`, or `@/lib/esi` | The whole slice relocates to `src/data/online-status`; Convex's two helper imports become ordinary downward edges and the 3.10.1.1 exception is deleted |
| `DEP-4` | `Verified` | `npx fallow dead-code` reports "No issues found" with zero cycles (2026-07-23) | The flip to `error` happens in step 10, after the step-9 tightened map re-verifies clean, not before |
| `DEP-5` | `Verified` | `server-only` absent from package.json; no client file currently reaches a server root; `convex/` imports `@/lib/esi` and `@/lib/esi/authed-read`; `build:vercel` runs `migrate`, `backfill-users-if-empty`, `ingest-sde-if-empty` under `tsx`, whose graphs reach `src/db/index.ts` and `@/lib/env`; `RevokeRedirectLightbox.tsx` imports only `EVE_AUTHORIZED_APPS_URL` from `eve-sso.ts` | Markers go only on Next-only roots; Convex- and CLI-shared graphs get recorded runtime exemptions; vitest gets a `server-only` alias stub; eve-sso splits constants from server functions so the marker lands on the server half |
| `DEP-6` | `Verified` | Vendor import census 2026-07-23: `google-auth-library` → `data/gsc/source.ts` only; `yauzl` → `data/eve-data/source.ts` only; `jose` → `eve-sso.ts` only; `better-auth` server entries → auth config, owner-transfer, one auth route, one app util; `postgres` value imports → `src/db` client, scripts, and `cron-gate.ts` (the `data/gsc/ingest.ts` and `data/market-prices/cache.ts` imports are type-only) | The enumeration test asserts these exact owner sets on the relocated paths; the single-owner rule replaces every former vendor pass |
| `DEP-7` | `Verified` | `readEnv`/`requireEnv` rail present in `src/lib/env.ts`; AGENTS.md requires asking before shared-state or deployment actions and removing previews after use | The typed-env rail is untouched; both drills carry named operator-authorization pauses |

## Why now

Sub-version 3.10.1.1 made the boundary map complete and mechanical, which
exposed that the map was drawn around a grown shape: auth filed as a peer
feature, cross-slice orchestration and the drizzle aggregator buried in
`src/db`, registry aggregators fused with the contracts features implement,
app-shell UI mixed with reusable components, and app-command composition
filed as a data slice. The operator directed (2026-07-23) that the tree take
its day-one-correct shape with zero carried-forward exceptions before the
fences become blocking. Relocating first and hardening second delivers
roadmap §3.10.1.2 and §3.10.1.3 on the corrected shape in one pass, and
every later Phase 2–3 slice (transport spine, vendor registry, generated
architecture map) then lands on truthful layer names.

## Scope (the destination)

The finished tree, top band to bottom: `src/app` (routes and pages), the
`api` handler zone (`src/app/api/**`), `src/scripts` (the six tsx entries
plus the script-only helpers `script-runtime.ts`, `migrate-url.ts`,
`sde-bootstrap.ts`, `refresh-prices-args.ts`, with `package.json` and the
Fallow `entry` array re-pointed), and the `runtime` zone (`proxy.ts`,
instrumentation). `src/composition` owns server-side cross-slice
composition: `search/` (`register-all.ts` and the app-command source
relocated from `data/commands`), `purge/` (`register-all.ts`,
`orchestrator.ts`, and the whole-schema `registry.test.ts` governance
suite), `page-settings/register-all.ts`, `sync/` (the esi-refresh
dispatcher and its owner-sync adapter, the six dataset sync workers,
`live-dataset-view.ts`, and the shared owner-sync port), `account-lifecycle/`
(the account-delete purge flow split out of `account-purge.ts`, the hoisted
`owner-transfer.ts` purge-transitive pair, and the wiring module that
registers the owner-reconcile hook), `pipelines/` (`sde-pipeline.ts`,
`esi-snapshot-retention.ts`, `cron-gate.ts`), `drizzle-schema.ts` (the
whole-schema aggregator from `src/db/schema.ts`, with `drizzle.config.ts`
re-pointed and the four runtime barrel consumers re-pointed to their own
slices' schema modules), `table-growth-registry.ts`, and the cross-slice
governance suites (`dataset-declarations.test.ts`,
`table-retention.db.test.ts`). `src/components/composition` owns app-shell
UI (24 shell/dashboard modules including the PageMenu chain and the
telemetry client) with auth's 13 presentation modules under `account/`; its
client boot modules keep their side-effect aggregator imports (an in-band
downward edge). `src/features` keeps its surviving domain slices.
`src/platform` owns capabilities in the declared order: `auth` (server
modules minus the identity schema and role vocabulary, the client session
surface, contracts, `eve-sso-constants.ts`, `link-character.ts`, the
purge-free reconciliation, and the owner-reconcile hook contract),
`owner-sync` (from `src/lib/owner-sync`), `esi` (from `src/lib/esi`), and
the socket contracts `search` (with structural types replacing its two
upward type-imports), `purge`, `page-settings`; `platform/auth` sits above
`transport` (its contracts and `ConvexClientProvider` import the typed
client). `src/transport` owns `api-client.ts`, `route-body.ts`, `cron.ts`,
`route-id.ts`, importing only foundations. `src/data` gains
`online-status` (whole slice) and loses `commands`; `data/eve-data` is the
declared reference core below the other data slices and gains the
`SdePipelineSummary` type. `src/db` keeps the client (`index.ts`), the
relocated identity schema (`auth-schema.ts`, importing the role vocabulary
from `src/config/character-roles.ts`), `advisory-lock.ts`, migrations, and
test support; `esi-refresh-jobs.db.test.ts` co-locates into its owning
data slice. Zone rules enforce the declared one-way order with zero
exception entries; both cycle rules become `error`; the server-only rail
(lint + four markers + enumeration test with a type-aware,
dynamic-import-aware client-reach walk and vendor owner-set assertions)
guards the server roots; the `auth-surface` baseline row, AF-008 trigger,
and their hard-coded checker (`check_baseline_claims.py` plus fixtures)
are remapped; `docs/architecture-boundaries.md` is rewritten as the layer
map and stale path references in living docs are fixed, with
`content/devlog/` and `content/changelog/` documented as historical
records excluded from the sweep. Contract §§3–6 are satisfied without
widening: the only structural changes are the six named export-seam splits
and the single sanctioned inversion seam, and no new behavior exists.

### Scope coverage

| Contract boundary | Implementation mapping or protection |
| --- | --- |
| `IS-1` | Ordered work steps 1–8: auth (with schema/role drops and the lifecycle hoist), the esi/owner-sync capability pair, transport, registry and command splits, db orchestration lift, online-status dissolution, scripts home, and the component split — one per-family commit each |
| `IS-2` | The six named export-seam splits and the owner-reconcile inversion seam ride their owning family commits (steps 1, 4, and 5) with behavior preserved |
| `IS-3` | Ordered work steps 1–8 (per-move zone increments) and step 9 (the zero-exception tightening; auth-surface zone and the Convex online-status exception deleted) |
| `IS-4` | Ordered work step 10: both cycle rules flipped to `error` after the step-9 green run |
| `IS-5` | Ordered work step 11: `server-only` dependency, markers, vitest stub, lint rail, enumeration test with vendor owner-set scans |
| `IS-6` | Ordered work step 12: map rewrite, guide/doc reconciliation, baseline row, AF-008, and `check_baseline_claims.py` remap |
| `IS-7` | Steps 2, 11, and 12 proofs: Convex push, tsx CLI matrix, authorized Neon drill, optional authorized preview drill |
| `OOS-1` | Every step is `git mv`, a named export-seam split, or the sanctioned inversion seam plus specifier re-pointing; SC-15's verify run plus SC-12's route smoke prove unchanged behavior |
| `OOS-2` | `src/features` and its surviving slices keep their names; no step touches them except the auth and online-status removals |
| `OOS-3` | `src/transport` receives only the four existing modules; no new transport code is written |
| `OOS-4` | No presentation, styling, or Phase 2+ content appears in any step |
| `OOS-5` | Delivery stops at per-step commits; PR, merge, and production actions stay with operator-invoked close-out |

## Resolved implementation decisions

- **Contract PD-1 — composition-band and leaf membership: role-based
  physical split, verified against live importers.**
  `src/components/composition/` (24 shell modules + tests): AppHeader,
  AppHeaderShell, NavMenu, NavTools, GlobalSearch, global-search-view,
  Footer, HeroBanner, JsonLd, HomeDashboard, HomeFeatureCards, HomeHero,
  HomeLeftColumn, HomeLiveStats, HomeLoginCta, HomeNewsCard,
  HomeRosterPanel, FeedbackButton, ServerStatus,
  server-status-presentation, TelemetryReporter, telemetry/client.ts (as
  `composition/telemetry/client.ts`), PageMenuProvider, PageMenuSection.
  `src/components/composition/account/` (13 modules): AccountDangerZone,
  AccountMenu, AdminForceLogoutForm, AdminReassignCharacterForm,
  AdminUnlinkCharacterForm, GrantedScopesList, LinkCharacterButton (its
  `startCharacterLink` action splits into `platform/auth/link-character.ts`
  because leaf `character-strip.tsx` calls it), LoginButton,
  RevokeRedirectLightbox, RoleToggleForm, SwitchCharacterForm,
  UnlinkCharacterForm, role-toggle-view.ts. Leaf (19 modules remaining in
  `src/components/`): PreferencesProvider, use-account-characters,
  RunAsFrame, character-portrait, OnlineStatusProvider, character-strip,
  run-as-state, type-icon, eve-image, live-character-card,
  live-character-sync, character-strip-model, character-strip-section,
  character-strip-view, RigSupply, use-system-search, use-live-dataset,
  telemetry/telemetry-payload.ts, telemetry/page-view-metadata.ts.
  `AuthProvider.tsx` and `ConvexClientProvider.tsx` are capability surface
  and stay in `platform/auth`. The shell's client boot modules keep their
  side-effect `register-all` imports — a sanctioned downward edge from the
  UI composition zone into the server composition zone within the same
  band. **Rejected:** moving the register-alls into the UI zone or
  server-side initialization — the first misfiles server aggregation in a
  UI home, the second changes client-boot behavior.
- **Contract PD-2 — platform child sets, capability order, splits, and the
  inversion seam.** `platform/auth` = `src/features/auth` minus the 13
  presentation modules, minus `schema.ts` (→ `src/db/auth-schema.ts`,
  dissolving the `data/telemetry`, `data/preferences`, and
  db-test-harness FK edges), minus `CHARACTER_ROLES` (→
  `src/config/character-roles.ts`, keeping the client-reachable
  `api-contract.ts` free of any `src/db` import), and minus the
  purge-transitive pair `reconcileCharacterOwner`/`purgeTransferredCharacter`
  (→ `composition/account-lifecycle/`), plus `eve-sso-constants.ts`
  (re-exported by `eve-sso.ts` so no server caller changes),
  `link-character.ts`, the purge-free reconciliation
  (`absorbLinkedCharacterOnProof`, `reconcileAfterCharacterRemoval`), and
  the owner-reconcile hook contract (`registerCharacterOwnerReconciler`/
  `getCharacterOwnerReconciler`): `auth.ts:182-190` invokes the registered
  reconciler exactly where it invoked `reconcileCharacterOwner`, and the
  composition wiring module registers the hoisted implementation inside
  `src/app/api/auth/[...all]/route.ts`'s module graph — every better-auth
  callback flows through that route, so registration always precedes
  invocation and behavior is preserved (proven by the existing auth flow
  tests). `platform/owner-sync` = `src/lib/owner-sync`. `platform/esi` =
  `src/lib/esi`. Socket contracts: `platform/search` = `index.ts`,
  `match.ts`, `rank.ts`; `platform/purge` = `types.ts`, `coverage.ts`;
  `platform/page-settings` = the six contract/utility files. Declared
  capability order (acyclic, no two-way pair): `auth` and `owner-sync`
  above `esi` and the socket contracts, and `auth` above `transport` (its
  contracts and `ConvexClientProvider` import the typed client; transport
  imports only foundations). `sync-engine`, `rate-limit`, and
  `fetch-with-timeout` stay in `src/lib`. **Rejected:** flat peer
  isolation, and keeping any purge-transitive function in platform — live
  call graphs (`owner-transfer.ts:131→159`) make both false.
- **Contract PD-3 — entry-script home and the drizzle aggregator.**
  `src/scripts/` receives the six tsx entries (`migrate.ts`,
  `ingest-sde.ts`, `ingest-sde-if-empty.ts`, `refresh-prices.ts`,
  `refresh-sde.ts`, `backfill-users-if-empty.ts`) and their script-only
  helpers (`script-runtime.ts`, `migrate-url.ts`, `sde-bootstrap.ts`,
  `refresh-prices-args.ts`); the same commit updates every `package.json`
  `db:*` path, `build:vercel`, and the Fallow `entry` array. The
  whole-schema aggregator `src/db/schema.ts` moves to
  `src/composition/drizzle-schema.ts` (whole-schema reflection is
  cross-slice governance; `drizzle.config.ts` and `migrate.ts` re-point —
  `scripts → composition` is a sanctioned downward edge), and its four
  runtime barrel consumers re-point to their own slices' schema modules
  (identical symbols, e.g. `market-prices/queries.ts` imports
  `./schema`). `advisory-lock.ts` stays in `src/db`. **Rejected:** the
  aggregator in `src/scripts` — the whole-schema governance tests
  (composition and esi-datasets zones) would then import upward into the
  entry band; and leaving it in `src/db` — its ~19 slice re-exports are
  exactly the upward edges the final `db` rule forbids.
- **Contract PD-4 — server roots and markers on relocated paths.**
  Import-blocked roots: `src/db/**`, `src/scripts/**`, `src/lib/env.ts`,
  `src/platform/auth/auth.ts`, `src/platform/auth/eve-sso.ts`,
  `src/platform/esi/**`, `src/lib/rate-limit.ts`, `src/data/gsc/source.ts`,
  `src/data/eve-data/source.ts`,
  `src/data/esi-refresh-jobs/pending-signal.ts`. Markers
  (`import 'server-only'` first line): `platform/auth/auth.ts`,
  `platform/auth/eve-sso.ts`, `lib/rate-limit.ts`, `data/gsc/source.ts`.
  Recorded runtime exemptions: `db/**`, `scripts/**`, and `lib/env.ts`
  (tsx CLI graphs, including the three Vercel pre-build entries),
  `platform/esi/**` (Convex bundle), `data/eve-data/source.ts` (SDE CLI
  graph), `pending-signal.ts` (CLI-shared worker graph). The role
  vocabulary in `src/config` keeps every client graph free of `src/db`.
  **Rejected:** marking everything under `--conditions=react-server`
  CLIs — changes module resolution for the deploy-gating scripts to
  duplicate coverage lint already provides.
- **Contract PD-5 — enumeration mechanics: three pure scans, endpoint-gate
  style, runtime-aware.** A new `src/lib/server-only-boundary.test.ts`
  (filesystem reads only) asserts: (a) the marker set equals the four-file
  list; (b) every file with a non-type import of a vendor SDK lies inside
  that SDK's declared owner set — `postgres`/`@neondatabase/serverless` →
  `src/db/**`, `src/scripts/**`, `src/composition/pipelines/cron-gate.ts`;
  `@upstash/redis`/`@upstash/ratelimit` → `lib/rate-limit.ts`,
  `platform/esi/**`, `data/esi-refresh-jobs/pending-signal.ts`;
  `google-auth-library` → `data/gsc/**`; `yauzl` →
  `data/eve-data/source.ts`; `jose` → `platform/auth/**`; `better-auth`
  server entries → `platform/auth/**`,
  `composition/account-lifecycle/**`,
  `src/app/api/auth/[...all]/route.ts`,
  `src/app/industry/active-job-character-ids.ts`; (c) a breadth-first walk
  over static `import`/`export from` and dynamic `import('…')` string
  literals — skipping `import type`/`export type` and inline `type`
  specifiers, resolving `@/` aliases — from every `'use client'` file
  reaches no root file; (d) each root carries a marker or a reasoned
  exemption entry; and (e) `eslint.config.mjs` text contains every root
  pattern so the two lists cannot drift. Pure helpers are unit-tested with
  seeded red fixtures in the same file. **Rejected:** deriving roots from
  the Fallow config — it names zones, not the per-file marker/exemption
  split.
- **Contract PD-6 — transport members.** `api-client.ts`, `route-body.ts`,
  `cron.ts`, `route-id.ts` (+ tests) move to `src/transport/`;
  `src/proxy.ts` re-points its `@/lib/route-id` import and the `runtime`
  zone allow gains `transport`. Transport's live imports are
  `@/lib/service-auth` and `zod` only, so its final allow is `lib,
  config` — it sits below `platform/auth`, whose contracts import it.
  `fetch-with-timeout.ts` stays in `src/lib`. Zone design: a distinct
  `api` zone (`src/app/api/**`, listed before `app` for first-match) and
  the `transport` zone; `api` may import `transport`, `composition`,
  `features`, `platform`, `data`, `db`, `lib`, `config`. **Rejected:** one
  merged zone — Fallow cannot enforce direction inside a single zone; and
  the earlier `transport → platform, data, db` allows — all dead edges.
- **Contract PD-7 — lint authoring under flat-config replacement.** A
  `serverRootImportPatterns` array names the root specifiers (`@/db`,
  `@/db/*`, `@/scripts/*`, `@/lib/env`, `@/platform/esi`,
  `@/platform/esi/*`, `@/platform/auth/auth`, `@/platform/auth/eve-sso`,
  `@/lib/rate-limit`, `@/data/gsc/source`, `@/data/eve-data/source`,
  `@/data/esi-refresh-jobs/pending-signal`). It spreads into the
  `patterns` arrays of every `no-restricted-imports` block whose files are
  client globs: a new block for `src/components/**` (both composition and
  leaf), `src/features/**/components/**`, `src/features/**/use-*.{ts,tsx}`,
  `src/data/**/use-*.{ts,tsx}`, and
  `src/platform/auth/{auth-client.ts,components/**}` (re-listing the
  next/image, staleness, Base UI, and sonner bans those files inherit, per
  the config's replacement-semantics note), and the existing wrapper,
  toast, and esi-datasets blocks; `eve-image.tsx` keeps its own re-listing
  block. `'use client'` files under `src/app` (where server files
  legitimately import roots, e.g. `app/page.tsx` reads `@/lib/env`) are
  closed by the enumeration walk running inside verify. Per-move ESLint
  updates ride their move commits: the ESI host-literal exemption glob in
  step 2 and the cron-shell bypass `paths` entries in step 3.
  **Rejected:** one repo-wide block — server files legitimately import the
  roots.
- **Contract PD-8 — commit sequencing and drill authorization.** Twelve
  commits, one per Ordered work step, each ending `pnpm verify` green with
  its zone/lint/entry increments included (HC-7), with interim allows
  enumerated under Edge and failure behavior and deleted in step 9. The
  Neon branch drill runs only after an explicit operator go-ahead at step
  12, with the concrete mechanics in SC-15; the optional both-database
  preview deployment triggers only if the Convex push warns about the
  moved chain, the Neon drill shows migration ambiguity, or a route-smoke
  regression traces to module resolution — and also requires explicit
  operator go-ahead, with removal shown after inspection. **Rejected:**
  unconditional or unprompted drills — shared-state actions require the
  operator per repository policy.
- **Contract PD-9 — search-contract type placement.**
  `platform/search/index.ts` replaces
  `import type { Session } from '@/features/auth/types'` and
  `import type { EveImageDescriptor } from '@/data/eve-data/type-images'`
  with structural type declarations owned by the contract itself,
  compile-time-only edits with zero runtime change; implementers keep
  satisfying them structurally. The socket contracts then import no
  platform sibling and no higher layer, type-only or otherwise.
  **Rejected:** an `allowTypeOnly` peer edge — the exact kind of recorded
  exception this session exists to delete.

### Audit-remediation mapping

Not applicable — this is not an audit-remediation contract.

## Design pressure and baseline effect

### Hotspot proximity

- **Touched measured surfaces:** the `auth-surface files` baseline row and
  the AF-008 watch trigger (`files(zone:auth-surface) >= 4`) — both keyed
  to a zone this session retires, and both hard-coded in
  `check_baseline_claims.py` (`zone_file_count(..., "auth-surface")`),
  which errors on an unknown zone. No measured function, coverage, or
  export surface changes.
- **Live proximity evidence:** `check_agent_drift.py` runs
  `check_baseline_claims.py`, so the row, the trigger, the checker's
  hard-coded lookup, and its fixtures must all be remapped in-session
  (step 12), not noted. All other rows are path-stable or count-stable
  under the moves. Verdict: adjacent to exactly this one measurement pair;
  outside every hotspot.

### Preparatory refactor

None — the session is itself pure relocation plus the named splits and the
single inversion seam; live structure already exposes clean seams (each
moved family has a single directory boundary and alias-based importers;
the split functions have no intertwined state; the reconciler call site is
a single line pair in `auth.ts`), proven by the per-step green verify
runs.

### Baseline effect and update

- **Effect:** `Improves` — removes the sanctioned shared↔features loophole,
  every misfiled ownership seam, and every recorded cross-layer exception,
  and adds the browser/server rail, with no LOC growth beyond the split
  modules' headers and the hook contract, and no measured-surface change.
- **Required update:** the `auth-surface files` row is remapped to count
  the three relocated contract files at their `platform/auth` paths (same
  values, version-start frozen at 3), AF-008's trip form is remapped to
  the equivalent supported measurement over those paths, and
  `check_baseline_claims.py` plus its fixtures are updated to the new
  paths — extending `repo_measures.py` with a path-form counter plus
  fixture only if the existing grammar lacks one; no other row changes and
  no metric re-measurement.

## Implementation blueprint

### Owned surfaces

- `src/platform/auth/` — the identity capability: server config, SSO
  (split), token crypto, queries, purge contributor, contracts, session
  client surface, `link-character.ts`, the purge-free reconciliation, and
  the owner-reconcile hook contract.
- `src/platform/owner-sync/` — the per-owner ESI sync engine (pure).
- `src/platform/esi/` — the ESI gate, unchanged contents; still the single
  ESI door.
- `src/platform/search/`, `src/platform/purge/`,
  `src/platform/page-settings/` — the socket contracts and pure utilities
  features implement, with no upward or sibling import.
- `src/composition/` — server-side cross-slice composition: registry
  aggregators, the app-command source, purge orchestrator and its
  whole-schema governance test, sync workers and dispatcher,
  account-lifecycle purge flows with the hook wiring, data pipelines and
  cron gate, the drizzle schema aggregator, table-growth registry,
  governance suites.
- `src/components/composition/` — app-shell UI importable only by `app`,
  with auth presentation under `account/`; its client boot modules import
  the aggregators downward.
- `src/components/` (leaf) — feature-importable presentation; never
  imports features or composition.
- `src/transport/` — typed client, body parsing, cron shell, route-id
  parsing; imports foundations only; the future Phase 2 spine home. The
  `api` zone is the same layer's handler side.
- `src/scripts/` — the tsx entries and script-only helpers;
  `package.json` and the Fallow `entry` array point here.
- `src/config/character-roles.ts` — the client-safe authoritative role
  vocabulary.
- `src/data/online-status/` — the whole relocated slice.
- `src/db/` — client, identity schema (`auth-schema.ts`), advisory locks,
  migrations, test support; nothing that imports a higher band.
- `.fallowrc.json` — the layer zone map, evolved per move commit and
  tightened to zero exceptions in step 9; both cycle rules at `error`
  after step 10.
- `eslint.config.mjs` — `serverRootImportPatterns`, its client-glob
  blocks, and the relocated esi-host and cron-shell rails.
- `src/lib/server-only-boundary.test.ts` — marker, vendor owner-set, and
  client-reach enforcement.
- `docs/architecture-boundaries.md` — the single prose owner of the layer
  map, the band orders, and the marker runtime exemptions.
- `docs/CODE_HEALTH_BASELINE.md`, `.agent-local/check_baseline_claims.py`
  (+ fixtures), and `.agent-local/repo_measures.py` (only if the path-form
  counter is needed).

### Interfaces and contracts

- The owner-reconcile hook contract is the session's one new production
  surface: `registerCharacterOwnerReconciler(fn)` and
  `getCharacterOwnerReconciler()` in `platform/auth`, each with a `/** */`
  contract comment; the registered signature is exactly
  `reconcileCharacterOwner`'s existing signature, and `auth.ts` invokes
  the registered reconciler at its current call sites. Every other export
  relocates verbatim (with `eve-sso.ts` re-exporting its constants so no
  server caller changes), so no other interface comments are required.
- `serverRootImportPatterns` (eslint.config.mjs) — module-private config
  constant; not a production export.
- `vitest.config.ts` alias — `server-only` resolves to a local empty stub
  module for all suites.
- Zone rules (`.fallowrc.json`), final state (allows are downward-only;
  band-internal orders declared): `app` → composition,
  components-composition, components, ui, features, platform, data,
  transport, db, lib, config; `api` → transport, composition, features,
  platform, data, db, lib, config; `scripts` → composition, platform,
  data, db, lib, config; `runtime` → transport, features, data, lib,
  config; `composition` → features, platform, data, db, transport, lib,
  config; `components-composition` → composition, components, ui,
  features, platform, data, transport, lib, config; `features` →
  platform, data, transport, db, lib, config, ui, components;
  `components` (leaf) → ui, lib, config, data, transport,
  `platform/auth`, `platform/search`, `platform/page-settings`; `data`
  (slices) → `data/eve-data` (the declared reference core),
  `platform/esi`, `platform/purge`, `platform/search`,
  `platform/owner-sync`, transport, db, lib, config; `platform/auth` →
  `platform/esi`, `platform/purge`, data, transport, db, lib, config;
  `platform/owner-sync` → `platform/esi`, lib; socket contracts → lib
  only; `platform/esi` → lib, config; `transport` → lib, config; `db` →
  lib, config; `lib` → config; `ui` → lib, config; `convex` →
  `platform/esi`, `platform/auth`, data, lib; `esi-datasets` (test-only
  zone) → composition, db, data, features, platform, lib. No exception
  entries anywhere; final allows are trimmed to live edges at step 9 (a
  listed allow with no live edge is dropped, never added speculatively).

### Control and data flow

The only runtime-flow delta is the owner-reconcile seam: better-auth
callbacks (which all enter through `src/app/api/auth/[...all]/route.ts`)
now reach `reconcileCharacterOwner` through the hook registered by the
composition wiring module in that route's module graph, instead of a
direct import — same function, same call sites, same ordering. Everything
else is unchanged: requests enter through the same route handlers,
validate through the same contracts, and reach the same modules; only
module specifiers differ. The Convex isolate bundles the same chain from
its new `platform/esi` and `data/online-status` locations; the tsx CLI
entries run the same graphs from `src/scripts`; drizzle-kit reads the same
aggregator from `src/composition/drizzle-schema.ts`. The lint rail and
enumeration test observe the graph without altering it.

### Edge and failure behavior

- Convex bundle regression (a moved module drags in `next/*`/React/marker)
  → the step-2 and step-6 local Convex pushes fail; fix before the commit
  lands.
- CLI graph regression (a moved specifier breaks `tsx` resolution) → the
  step-11 CLI matrix fails; the three Vercel pre-build entries are the
  hard gate, run against `src/scripts` paths.
- A relocated entry script reads as unused (`unused-files: error`) → the
  Fallow `entry` array update rides the step-7 commit; its omission fails
  that step's verify run.
- An unregistered owner-reconcile hook (a code path reaching the reconciler
  before the wiring import) → the hook accessor throws with a named error;
  the existing auth flow tests plus the wiring import in the auth route's
  module graph prove registration always precedes invocation.
- A hidden upward edge surfaces during the step-9 tightening → it is a
  real architecture finding: correct the module's layer assignment within
  the approved map, or stop on the contract's split trigger if it needs an
  operator ruling; never add an undeclared allow.
- A stale `vi.mock`/`vi.importActual` literal after a move → the mock
  stops applying silently; each family's zero-stale-literal grep is the
  guard, and SC-13's cumulative pattern covers every moved family
  including the db orchestration and shell paths.
- Stale build caches after moves → remove `.next/dev` before the route
  smoke so renders are trusted.
- Seeded-violation files are temporary, never committed: each is created,
  shown failing, and deleted within its proof step.
- Interim allows, enumerated (each listed in its step, all deleted in
  step 9, whose diff shows zero surviving interim entries):
  data→`platform/auth` (the commands edge, steps 1–4);
  components→`platform/auth` (steps 1–8); `platform/auth`→ui and
  →components (its riding UI modules, steps 1–8);
  `platform/auth`→transport interim via the current `@/lib/api-client`
  path (steps 1–3, then the final allow);
  components→composition (the register-all boot edges, steps 4–8);
  db→composition (the entry scripts' pipeline imports, steps 5–7);
  esi-datasets→composition (added at step 5, kept in the final map).

### Ordered work

1. **Hoist the auth capability.** `git mv src/features/auth
   src/platform/auth`; move `schema.ts` → `src/db/auth-schema.ts` and
   split `CHARACTER_ROLES` → `src/config/character-roles.ts` (imported by
   both the schema and `api-contract.ts`); split `eve-sso-constants.ts`
   (re-exported by `eve-sso.ts`) and `link-character.ts`; create
   `src/composition/account-lifecycle/` with the account-delete purge
   flow split out of `account-purge.ts`, the hoisted
   `reconcileCharacterOwner`/`purgeTransferredCharacter` pair, and the
   hook wiring module; add the owner-reconcile hook contract to
   `platform/auth` and re-point `auth.ts:182-190` and the three lifecycle
   API routes (which import the wiring); re-point the 87 production
   importer files (122 with tests), the `db` relative `synthetic-email`
   import, every `@/features/auth/schema` FK import to
   `@/db/auth-schema`, and every mock literal; add the interim zone
   increments listed under Edge behavior. Prove with the
   zero-stale-literal grep, `rg "runPurge|purgeTransferredCharacter"
   src/platform/auth` → hook-contract mentions only, the auth flow tests
   green, and `pnpm verify` green; commit.
2. **Move the ESI capability pair.** `git mv src/lib/esi
   src/platform/esi` and `git mv src/lib/owner-sync
   src/platform/owner-sync`; re-point the 33 `@/lib/esi*` importers
   (including `convex/`) and the `@/lib/owner-sync` importers plus mock
   literals; add both platform zones; update the ESLint esi-host exemption
   glob in the same commit. Prove with the grep, `pnpm verify` green, and
   `pnpm exec convex dev --once` succeeding on the moved chain; commit.
3. **Carve transport.** Move `api-client.ts`, `route-body.ts`, `cron.ts`,
   `route-id.ts` (+ tests) to `src/transport/`; re-point their importers
   including `src/proxy.ts` and mock literals; add the `transport` (allow:
   lib, config) and `api` zones and the `runtime` allow update; update the
   cron-shell bypass `paths` entries in the same commit. Prove with the
   grep and `pnpm verify` green; commit.
4. **Split the registries and the app-command source.** Move the socket
   contracts → `platform/{search,purge,page-settings}` and the
   aggregators → `src/composition/<name>/`, with `purge/registry.test.ts`
   riding to `composition/purge/`; move `data/commands/search.ts` (+
   test) → `src/composition/search/commands-source.ts`, deleting the
   `data/commands` slice; apply the PD-9 structural-type edit; re-point
   the 45 production registry importers and mock literals; delete the
   three former top-level homes. Prove with the grep and `pnpm verify`
   green; commit.
5. **Lift the db orchestration.** Move the ten sync modules to
   `src/composition/sync/`; `sde-pipeline.ts`, `esi-snapshot-retention.ts`,
   `cron-gate.ts` to `src/composition/pipelines/`; `src/db/schema.ts` to
   `src/composition/drizzle-schema.ts` (re-pointing `drizzle.config.ts`,
   `migrate.ts`, and the whole-schema governance consumers, and
   re-pointing the four runtime barrel consumers to their own slices'
   schema modules); `table-growth-registry.ts`,
   `dataset-declarations.test.ts`, and `table-retention.db.test.ts` to
   `src/composition/`; `esi-refresh-jobs.db.test.ts` into
   `src/data/esi-refresh-jobs/`; move the `SdePipelineSummary` type into
   `src/data/eve-data/api-contract.ts` with the pipeline importing it
   downward; add the esi-datasets→composition allow; re-point API route,
   cron declaration, script, and test importers plus mock literals. Prove
   with the grep and `pnpm verify` green; commit.
6. **Dissolve online-status into data.** `git mv
   src/features/online-status src/data/online-status`; re-point
   `convex/onlineStatusSync.ts`, the purge aggregator, and
   `character-portrait.tsx`; delete the Convex exception entry. Prove with
   the grep, `pnpm exec convex dev --once` green, and `pnpm verify` green;
   commit.
7. **Home the entry scripts.** Move the six tsx entries and the four
   script-only helpers to `src/scripts/`; update every `package.json`
   `db:*` path, `build:vercel`, and the Fallow `entry` array; add the
   `scripts` zone. Prove with the grep, `pnpm db:migrate` exiting 0 from
   the new path, and `pnpm verify` green; commit.
8. **Split the components.** Create `src/components/composition/` (24
   shell modules, telemetry client under `composition/telemetry/`) and
   `src/components/composition/account/` (the 13 auth presentation
   modules); re-point `app`, shell, and auth-internal importers; leaf
   remainder stays. Prove with the grep and `pnpm verify` green; commit.
9. **Tighten the map to zero exceptions.** Rewrite `.fallowrc.json` rules
   to the final one-way layer model listed under Interfaces and contracts:
   delete the auth-surface zone, every interim allow, and every exception
   entry; trim listed allows to live edges. Prove with `pnpm verify`
   green, `fallow list --boundaries` full coverage, a config inspection
   showing zero upward or exception entries, and the seeded red set
   (platform capability-order violation `esi`→`auth`,
   feature→composition, upward `lib`→`platform/auth`, data-slice peer
   outside the reference core, `transport`→`api`) each failing then
   removed; commit.
10. **Flip the cycle rules.** Set `circular-dependencies` and
    `re-export-cycle` to `error`. Prove with `pnpm verify` green; commit.
11. **Install the server-only rail.** Add the `server-only` dependency,
    the four markers, the vitest stub, `serverRootImportPatterns` with its
    client-glob blocks, and `src/lib/server-only-boundary.test.ts` with
    seeded red fixtures. Prove with the seeded client-import lint failure,
    the test's red fixtures and green real-tree pass, `pnpm verify` green,
    and the six-entry CLI matrix (`pnpm db:migrate`, `pnpm exec tsx
    src/scripts/ingest-sde-if-empty.ts`, `pnpm exec tsx
    src/scripts/backfill-users-if-empty.ts`, `pnpm db:ingest:sde`,
    `pnpm db:refresh-prices`, `pnpm db:refresh-sde --force` — the forced
    final run rebuilds the resolved blueprint tables so the local dataset
    ends healthy); commit.
12. **Rewrite the map and reconcile every reference.** Rewrite
    `docs/architecture-boundaries.md` as the layer map (bands, zones,
    band-internal orders, one-way rules, the inversion seam, marker
    runtime exemptions, zero exception entries); update AGENTS.md and
    `src/AGENTS.md` boundary sentences including the composition-exemplar
    paths; sweep and fix stale path references in `docs/CONVEX.md`,
    `CONTRIBUTING.md`, `README.md`, and `.env.example` via the explicit
    SC-14 grep, recording that `content/devlog/` and `content/changelog/`
    are historical records excluded from the sweep; remap the
    `auth-surface files` baseline row, AF-008 trip form,
    `check_baseline_claims.py` and its fixtures (extending
    `repo_measures.py` only if needed); run
    `python3 .agent-local/reconcile_skill_ledger.py` and
    `python3 .agent-local/check_agent_drift.py`; run the route smoke; run
    the operator-authorized Neon branch drill (and the preview drill only
    on its PD-8 triggers, also operator-authorized). Prove with the drift
    and ledger output green and commit.

## Success criteria (agent-runnable — show the output)

- **SC-1 — Contract DC-1 / AC-1.** `npx fallow list --boundaries` → the
  six `platform/*` children (auth, owner-sync, esi, search, purge,
  page-settings) each listed, `src/features/auth`, `src/lib/esi`,
  `src/lib/owner-sync`, and the three former registry homes absent,
  `src/db/auth-schema.ts` in the db zone and
  `src/config/character-roles.ts` in the config zone, zero unclassified
  production files; a seeded `platform/esi` import of `platform/auth`
  (against the declared capability order) → `pnpm fallow` fails naming the
  edge.
- **SC-2 — Contract DC-2 / AC-2.** `npx fallow list --boundaries` → both
  composition homes listed with their member sets including
  `account-lifecycle`, `pipelines`, `drizzle-schema.ts`, the app-command
  source, and the governance suites; a seeded `@/composition/sync` import
  in a feature file and a seeded `@/components/composition/AppHeader`
  import in `platform/auth` → both fail `pnpm fallow`; the live
  `AppHeaderShell` → `composition/search/register-all` boot edge passes
  green.
- **SC-3 — Contract DC-3 / AC-3.** `ls src/transport/` → the four moved
  modules; a seeded import of an `api` route file from `src/transport` →
  `pnpm fallow` fails; `ls src/features/*/api-contract.ts` → contract
  files unmoved.
- **SC-4 — Contract DC-4 / AC-4.** `ls src/features/online-status` → no
  such directory; `ls src/data/online-status/` → all four modules;
  `rg "features/online-status" convex src .fallowrc.json` → zero hits, the
  former Convex exception entry gone.
- **SC-5 — Contract DC-5 / AC-5.** `npx fallow list --boundaries` → the
  leaf components zone covers `.ts` and `.tsx` members and the `scripts`
  zone owns the six entries plus four helpers;
  `rg '"db:|build:vercel' package.json` → every script path points at
  `src/scripts/`; `rg 'schema' drizzle.config.ts` →
  `src/composition/drizzle-schema.ts`; the Fallow `entry` array lists the
  new paths.
- **SC-6 — Contract DC-6 / AC-6.** A config inspection over
  `.fallowrc.json` (`python3` or `jq`) → no allow edge from a lower band
  to a higher one, no slice-peer allow outside the declared eve-data
  reference-core order, no exception entry; the seeded violation set
  (upward, slice peer, feature→composition) each fail `pnpm fallow`; the
  vendor owner-set assertions pass in the enumeration test run.
- **SC-7 — Contract DC-7 / AC-7.** `grep -n '"circular-dependencies"\|"re-export-cycle"'
  .fallowrc.json` → both `"error"`, and `pnpm fallow` → green full run
  with no suppression added.
- **SC-8 — Contract DC-8 / AC-8.** Seeded `import { db } from '@/db'` in a
  leaf component → `pnpm lint` fails citing the restricted-import rule;
  removing the seed → `pnpm lint` green with zero warnings.
- **SC-9 — Contract DC-8 / AC-8.** `pnpm test src/lib/server-only-boundary`
  → green on the real tree (type-only and vendor-owner cases included);
  its seeded fixtures (unmarked new root, client reach via static and
  dynamic import) → shown failing in the fixture assertions; the marker
  set equals the four-file list, every exempt root shows its recorded
  reason, and the lint-pattern alignment assertion passes.
- **SC-10 — Contract DC-9 / AC-9.** `pnpm exec convex dev --once` → bundle
  and push succeed against the moved `platform/esi` and
  `data/online-status` chain with no unresolved-import or marker error.
- **SC-11 — Contract DC-9 / AC-9 / V-4.** The six-entry CLI matrix
  (`pnpm db:migrate`, `pnpm exec tsx src/scripts/ingest-sde-if-empty.ts`,
  `pnpm exec tsx src/scripts/backfill-users-if-empty.ts`,
  `pnpm db:ingest:sde`, `pnpm db:refresh-prices`,
  `pnpm db:refresh-sde --force`) against local Postgres → every entry
  exits 0 on the moved graph, with the forced final run leaving the
  resolved blueprint tables rebuilt and the local dataset healthy.
- **SC-12 — Contract DC-9 / V-5 / G-1.** `rm -rf .next/dev`, start
  `pnpm dev` in the background, then per route run
  `curl -s -o /tmp/body -w '%{http_code}' http://localhost:3000<route>`
  → the public routes (`/`, `/sites`, `/industry`, `/devlog`,
  `/changelog`) return `200` with a page-identifying string shown from
  the saved body; the signed-out gated routes (`/jobs`, `/skills`,
  `/structures`, `/characters`) return their current redirect status and
  `location` unchanged; no route returns a 5xx; G-1 requires no
  browser-design pause and none is taken.
- **SC-13 — Contract DC-9.** The cumulative stale-literal grep
  (`rg "@/features/auth|@/features/online-status|@/lib/esi[/']|@/lib/owner-sync|@/lib/api-client|@/lib/route-body|@/lib/cron|@/lib/route-id|@/search|@/purge|@/page-settings|@/data/commands|@/db/schema|@/db/sde-pipeline|@/db/cron-gate|@/db/esi-snapshot-retention|@/db/esi-refresh|@/db/owner-sync-port|@/db/live-dataset-view|@/db/table-growth-registry|@/db/[a-z-]*-sync|@/components/(AppHeader|NavMenu|NavTools|GlobalSearch|Footer|HeroBanner|JsonLd|Home[A-Za-z]*|FeedbackButton|ServerStatus|server-status|TelemetryReporter|PageMenu|telemetry/client)" src convex --glob '!*.md'`)
  → zero hits at step 12, comments included (the `[/']` anchor keeps the
  untouched `@/lib/esi-datasets` home out of scope, and `@/search`/
  `@/purge`/`@/page-settings` do not match their relocated
  `@/platform/*` forms).
- **SC-14 — Contract DC-10 / AC-10 / V-6.** `python3
  .agent-local/check_agent_drift.py` and `python3
  .agent-local/reconcile_skill_ledger.py` → both green after the map,
  guide, baseline row, AF-008, and `check_baseline_claims.py` remap
  updates; plus the explicit root-file sweep
  (`rg "src/(features/auth|features/online-status|lib/esi[^-]|lib/owner-sync|search/|purge/|page-settings/|db/schema|db/sde-pipeline)" README.md CONTRIBUTING.md .env.example docs/CONVEX.md`)
  → zero hits, with the historical-record exclusion for `content/`
  stated in the rewritten map.
- **SC-15 — Contract V-1 / V-2 / V-3 / V-4 / V-7.** `pnpm verify` →
  typecheck, zero-warning lint, full coverage suite, and coverage-backed
  Fallow all green on the final tree; the operator-authorized Neon branch
  drill: `neonctl branches create --name drill-3-10-1-2 --output json` →
  capture the branch's connection string, export it via the environment
  variable `resolveMigrationUrl()` reads (`src/scripts/migrate-url.ts`),
  `pnpm db:migrate` → applied/no-op output, then
  `neonctl branches delete drill-3-10-1-2` → teardown proof; and
  `python3 .agent-local/check_release_consistency.py --check` → zero
  errors and warnings; the remaining V-7 release gates (pre-PR design
  review, pinned checkpoint, CI, Greptile, PR-state and production
  evidence) execute inside operator-invoked close-out per the Delivery
  item below.

## End of session

- Confirm every `DONE =` item is evidenced, every `hard_constraints`
  boundary held, the `Improves` baseline verdict recorded, and the
  contract UX marker (`No`) honored — the only pauses taken are the named
  drill authorizations.
- **Delivery:** preserve the twelve verify-green Ordered work commits on
  `lifecycle/3.10.1.2`. The original 522-file PR exceeded Greptile's
  500-file and CodeRabbit's 300-file hard caps, so the operator approved
  three sequential delivery PRs at existing commit boundaries: part 1
  through identity, part 2 through the ESI/transport/composition sockets,
  and part 3 through the final tree and release records. Each part runs
  its exact-diff pre-PR review and pinned checkpoint, waits for both bots
  to finish before any batched response or push, then completes clean
  merge and exact production proof before the next branch is prepared.
- **Lifecycle artifacts:** this plan's `Execution status` flips to
  `Complete` only in part 3; parts 1 and 2 deliberately retain the pending
  release identity, while part 3 publishes the roadmap row, `APP_VERSION`,
  and changelog entry per the version plan. The baseline row, AF-008, and
  checker remap land in step 12; SCRATCHPAD records the layer map as
  current state.
- **Handoff:** rerun `python3 .agent-local/resolve_development_state.py
  --pretty` after close-out and report the fresh directive; the next
  lifecycle action resolves from the updated roadmap state through a
  fresh `start-session`.
