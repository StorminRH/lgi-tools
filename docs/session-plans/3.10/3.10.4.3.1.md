# Session 3.10.4.3.1 Implementation Plan — Operator punch list, adoption re-audit, and version closure

**Plan status:** Approved
**Approved:** 2026-07-26
**Contract:** `docs/session-contracts/3.10/3.10.4.3.1.md`
**Contract digest:** `sha256:dc3d77720716018cd6ed51cca2e98caf4a378af621d78abc1d3c0185134a0aeb`
**Planning standard:** `docs/workflows/schema/session-plan.md`
**Execution status:** Pending
**Baseline effect:** Neutral

## Bottom line (READ FIRST)

- **GOAL:** Version 3.10 closes: every operator punch item is resolved inside
  the fixed fix vocabulary, the recorded adoption re-audit runs clean, and the
  version-final release records ship in one PR stopped at the operator-reserved
  merge gate.
- **DONE =** SC-1 through SC-11 below, plus: the sub-version PR is open,
  unmerged, green, and settled, carrying terminal Phase 4 roadmap rows,
  `APP_VERSION = '3.10.4.3'`, the published changelog entry, and the Complete
  execution marker, after the operator has declared the punch lists resolved.
- **OUT OF SCOPE:**
  - The code-health version-close audit (Contract OOS-4) — the resolver selects
    it after this PR merges; nothing from it is pre-absorbed here.
  - New primitives, token families, or one-off styles (Contract OOS-3) — a
    punch item that needs one pauses for the operator's amend-or-backlog ruling
    instead of being absorbed.

<hard_constraints>

- **Contract HC-1:** The fix vocabulary is absolute — every fix is expressed
  through existing primitives, feature tone maps, or registered tokens. A fix
  that cannot be pauses for amendment or routes to `docs/backlog.md` with its
  survey citation. Consequence: every punch-fix diff is inspectable against
  this boundary (SC-2).
- **Contract HC-2:** Multiple sittings resume this same session and branch; a
  sitting is not a new contract and produces no new delivery boundary. All
  sittings commit to `lifecycle/3.10.4.3` and feed one PR.
- **Contract HC-3:** The re-audit uses the survey's recorded methods or their
  superseding rails (the PD-3 mapping) so its result diffs against the original
  record.
- **Contract HC-4:** Every rail and census gate from 3.10.4.1/3.10.4.2 stays
  green throughout; no exemption is added silently — any exemption change lands
  in `src/composition/ui-adoption-registry.ts` plus its exact flat-config block
  with the census updated in the same commit.
- **Contract HC-5:** The version-final PR contains the complete truthful
  post-merge state: terminal roadmap rows, matching `APP_VERSION`, the
  published changelog entry with absorbed pending fragments, and the Complete
  execution marker exist on the branch before the PR opens (DC-5's set); the
  as-built record joins the PR during close-out once the PR number exists, per
  `docs/workflows/close-out.md`. No post-merge reconciliation, no follow-up
  lifecycle-only PR.
- **Contract HC-6:** Motion, keyboard, focus, and reduced-motion behavior are
  preserved by every fix — punch fixes are presentation-class changes; any fix
  touching an interactive primitive re-runs that primitive's open-state or
  keyboard probe.
- **Plan:** `src/features/industry-planner/industry-styles.ts` becomes the only
  owner of ME/TE ownership-state color meaning; no component keeps a local
  state→tone map after step 1.
- **Plan:** Flat-config exemption blocks replace rule options rather than
  merging them — any edited block re-lists every neighboring rail (3.10.4.1
  successor note; the 3.10.2.4.1 config-diff technique proves it when a block
  is edited).
- **Plan:** Never run a production build locally; UI verification is dev-server
  ux-check plus probes; render-mode reasoning uses the installed Next.js guides
  and honest rows in `scripts/route-classification.json`.
- **Plan (operator-directed vocabulary extension, 2026-07-26):** Loading
  presentation is in-vocabulary for this session: Suspense boundary placement
  across every route, fallback selection from the loading primitives,
  sanctioned `'use cache'`/`cacheLife`/`cacheTag` annotations under the
  existing `src/AGENTS.md` most-static-honest-mode rules, the house-style
  strengthening of that guide section itself (explicitly not a lint rule, by
  operator ruling), and the EVE type-image intent resolvers in
  `src/data/eve-data/type-images.ts` (the operator directed the no-observed-
  monogram rule and the hero rendition experiment; rendition choices stay in
  that one intent-resolver home). A route whose shell fix needs
  genuine feature re-plumbing (data-model or per-component price-flow redesign)
  still pauses at G-3. The operator folded this in at planning rather than
  routing it to an amendment; the plan records that divergence from a
  styling-only reading of HC-1.

</hard_constraints>

**Branch:** `lifecycle/3.10.4.3` · **ends in PR:** yes · **gate:** the PR opens only after the final sitting's operator approval and the recorded clean re-audit, then stops open and unmerged with green current-head CI and a settled review gate at the operator-reserved merge gate.

**Contract UX gate:** `Yes` · **required pause:** every sitting ends with the
operator's local browser review of that sitting's fixes (G-1); the operator's
punch-lists-resolved declaration is the completion authority for DC-1 (G-2);
any out-of-vocabulary item pauses for the operator's amend-or-backlog ruling
(G-3); the final sitting's approval with ux-check evidence precedes PR
creation.

## Read first

- `CLAUDE.md` → `AGENTS.md`, and `src/AGENTS.md` for all UI work
- `docs/session-contracts/3.10/3.10.4.3.1.md` (the contract)
- `docs/version-audits/3.10/PHASE_4_ADOPTION_SURVEY.md` — rows, dispositions,
  delivery ledger, and the appendix re-audit methods
- `src/composition/ui-adoption-registry.ts` and `eslint.config.mjs` — the live
  rails the re-audit diffs against
- `src/features/industry-planner/components/kpi-tile.tsx`, `MeAdjuster.tsx`,
  `NodeCard.tsx` — the seed surfaces
- `src/app/sites/page.tsx` and `scripts/route-classification.json` — the
  loading/streaming exemplar and the render-mode record the audit diffs against
- The installed Next.js Cache Components / PPR guides under
  `node_modules/next/dist/docs/` before any boundary or cache-annotation change
- `docs/workflows/close-out.md` — the delivery pipeline this session finishes
  through

## Current state and prerequisites

| Contract input | Live verdict | Evidence | Execution consequence |
| --- | --- | --- | --- |
| `DEP-1` | `Verified` | PR #309 and PR #310 are squash-merged to `main` (PR #310 merged 2026-07-26T19:43Z); `src/config/app-version.ts` reads `'3.10.4.2'`; SCRATCHPAD records 3.10.4.2 complete and shipped | Punch reports come from operator live use of current production; sittings begin immediately |
| `DEP-2` | `Verified` | The survey's appendix records per-family detection commands; the 3.10.4.1 delivery ledger marks every A1 row Delivered; rails live in `eslint.config.mjs`, `src/esi-datasets/ui-adoption.test.ts`, `scripts/ui-adoption-rail.test.mjs` | The re-audit re-runs the appendix per family with the PD-3 supersession map; results diff against the recorded survey |
| `DEP-3` | `Verified` | Contract HC-1; the primitive/tone-map/token vocabulary from 3.10.4.1/4.2 is live in `src/components/ui/` and `*-styles.ts` files | Triage of every punch item happens against this vocabulary before any fix |
| `DEP-4` | `Verified` | Live code shows the seed gaps are real: `KpiHead` is an unconstrained flex row whose `right` slot carries controls (SegmentedControl), so control-bearing tiles inflate their label row; ME/TE state tones live locally in `MeAdjuster.tsx` `iconTone` and `NodeCard.tsx` `FRAME_TONE`, not in `industry-styles.ts` | The three seed checks are pre-registered punch items PL-001…PL-003 and are fixed, not merely verified |
| `DEP-5` | `Verified` | Roadmap row 3.10.4.3 is `PLANNED`; `content/changelog/pending/` holds zero fragments (README only); the v3.10 changelog master heading and summary already exist | This PR publishes the terminal row, `APP_VERSION`, and the `### v3.10.4.3` entry; the version-close audit stays out of scope |

Operator-added planning input (2026-07-26, this session): the loading and
streaming presentation itself is a reported punch area — `/sites` renders a
bare "Loading sites…" label and then drops the whole page in because its
entire content sits in one Suspense hole over a background-only shell
(`src/app/sites/page.tsx` documents that shell as deliberate); the Skeleton
shimmer primitive has only three production consumers (RunAsFrame's build
character portrait, LoginButton, admin); and the survey's fallback census
(32/32 conformant: 19 LoadingLabel, 6 Skeleton, 6 null, 1 stand-in) proves
conformance, not quality. Captured as PL-005/PL-006 below.

## Why now

3.10.4.1 and 3.10.4.2 shipped the adopted, refined, responsive presentation
system to production, and the contract gates punch-list discovery on a real
soak of that shipped state. This is the last Phase 4 slice: resolving the
operator's live-use findings inside the fixed vocabulary and re-running the
survey clean is what turns Phase 4's universal claim from intention into
recorded evidence, and its PR closes version 3.10's delivery so the resolver
can move to the version-close audit.

## Scope (the destination)

At session end, every punch item the operator reported from live use has a
terminal disposition in a recorded ledger: fixed inside the vocabulary,
backlogged with citation by operator ruling, paused into an amendment, or
withdrawn. The three planner seed items are fixed: cockpit KPI figures share
one baseline whether or not a tile's label row carries controls; ME/TE
ownership-state color meaning has exactly one owner in the industry tone map;
and owned-blueprint highlighting reads identically across NodeCard, HeroCard,
CockpitBuildPlan, and MeAdjuster. The one defect routed from 3.10.4.2's
responsive matrix (development-only instrumentation noise in later capture
runs, handed over in PR #310's notes) has a terminal disposition. Loading is
an intentional, consistent presentation mode: a recorded policy says which
loading primitive a Suspense fallback uses and when; every route has been
audited so its static shell carries all honestly static content — typically
the large majority of the page, prerendered from the Neon-cached data — with
small Suspense holes remaining only for genuinely per-user or per-request
data, and live figures continuing to update in view — `/sites` is the exemplar, where the
catalogue renders immediately and only price-dependent figures update after
view — content-shaped holes (planner landing templates and research searches,
portraits, character readouts) wear the Skeleton primitive instead of bare
labels or nothing, and the strengthened house style in `src/AGENTS.md` keeps
future agents from re-introducing the background-only shell idiom. Price
updates share one affordance: the widened `LivePrice` primitive shows a
clearly perceptible in-progress and just-updated animation (operator-chosen)
on the sites and planner price surfaces alike, and concurrent toasts stack
readably instead of overlapping. The adoption
re-audit has re-run every recorded survey method (or its superseding rail),
its results are recorded in the survey document, every survey row is terminal,
and residue is dispositioned with an empty accepted-residual set expected.
The branch carries the complete version-final release records, and the PR
stops unmerged at the merge gate. Contract §§3–6 are satisfied without
widening: no feature behavior changes, no new primitives or token families,
no AF-005 crossing, and the version-close audit remains untouched.

### Scope coverage

| Contract boundary | Implementation mapping or protection |
| --- | --- |
| `IS-1` | Punch fixes across industry-planner routes and components first (steps 1–2), the loading/streaming pass across all routes (step 3), the price-affordance and toast fixes (step 4), then every remaining route including admin during sittings (steps 5–6) |
| `IS-2` | Fixes land in `src/components/ui/` primitives, `*-styles.ts` tone maps, registered tokens, or (per the operator-directed extension) Suspense boundary and fallback placement, with any required rail/registry/census update in the same commit |
| `IS-3` | Step 7 runs the re-audit, records results and residue dispositions in the survey document |
| `IS-4` | Step 8 writes roadmap, `APP_VERSION`, changelog with absorbed fragments, and lifecycle state on the branch |
| `IS-5` | ux-check runs on punch-affected routes each sitting; operator review pauses close every sitting (G-1) |
| `OOS-1` | Triage classifies any behavior-changing request as out of scope; only visual corrections of approved punch items are implemented |
| `OOS-2` | No PricingProvider concern redesign; the single PL-007-authorized `refreshing` field pass-through into `MarketDataValue` is the only sanctioned AF-005-adjacent edit, and the diff is inspected against the AF-005 path set for any other crossing before each sitting review |
| `OOS-3` | Any item needing a new primitive/token/one-off pauses at G-3; nothing is created without an approved amendment |
| `OOS-4` | No baseline refresh or audit artifact is produced; the version-close audit stays with the resolver after merge |
| `OOS-5` | Backlogged Phase 4 decisions (scroll-aware section links, AD-039 CSS relocation) stay in `docs/backlog.md` unless the operator re-dispositions them in a sitting, recorded in the ledger |

## Resolved implementation decisions

- **Contract PD-1 — KPI baseline mechanism: fixed label-row height.** Give
  `KpiHead` (in `kpi-tile.tsx`) a fixed height sized to the tallest real
  label-row occupancy, with `items-center`; `KPI_FIG`'s uniform `mt-2.5` then
  starts every figure at the same offset, so figures share one baseline
  whether or not the tile carries controls. The operator's screenshot shows
  the true tallest state: the Input-Cost and Net-Margin labels wrap to two
  lines beside their toggles, so the fixed height is sized to that occupancy
  (or the labels are kept to one line and the height to the control height —
  whichever the live measurements support; figures on one plane is the
  requirement, the exact height is implementation evidence). One-file change;
  all six tiles inherit it because every tile composes `KpiHead`.
  **Rejected:** shared grid track via CSS subgrid — it requires every tile to
  become a `row-span-2` subgrid participant, flattening `KpiTile`'s
  Card-with-children contract across all six tiles and reasoning about
  doubled implicit rows at three breakpoint column counts (2/3/6), for no
  stronger guarantee than the fixed row gives.
- **Contract PD-2 — sitting workflow mechanics: a recorded punch ledger at
  `docs/version-audits/3.10/PHASE_4_PUNCH_LEDGER.md`.** Each operator report
  becomes one `PL-NNN` row: sitting date, route/surface, the report in the
  operator's terms, triage verdict against the fix vocabulary, disposition
  (`Fixed` / `Backlogged` with citation / `Amendment-paused` / `Withdrawn` /
  `No-change` with reason), and fix evidence (files or commit). Seed items are
  pre-registered before the first sitting: PL-001 KPI baseline, PL-002 ME/TE
  tone ownership, PL-003 owned-highlight consistency, PL-004 the routed matrix
  defect, PL-005 the Suspense-boundary audit with `/sites` as exemplar,
  PL-006 the loading-fallback quality pass, PL-007 the unified price-update
  affordance, PL-008 the template-toast/sync-toast overlap bug, PL-009 the
  owned-asset outline retone (blue → green), PL-010 the research-job monogram
  bug and no-observed-monogram sweep, PL-011 the hero-card blueprint-image
  experiment, PL-012 the multibuy CopyButton adoption, PL-013 the
  multibuy/raw-ledger chip-or-pill trigger experiment, PL-014 the
  templates-trigger clickability experiment, PL-015 the ME/TE glyph optical
  alignment, PL-016 the build-character time-bonus icon-trigger experiment,
  and PL-017 the related-inputs green-border/connector-line experiment
  (sequenced last by operator direction) (all operator-reported during
  planning, 2026-07-26; a toast
  visual restyle was explicitly deferred as an afterthought and is recorded
  in the ledger as operator-deferred, not scoped). A sitting ends with ux-check on that sitting's affected routes and
  the operator's review (G-1); the operator's DC-1 resolution declaration is
  recorded in the ledger header (G-2). **Rejected:** tracking punch items only
  in chat or in the session plan — HC-2's multi-sitting resumption needs a
  durable in-repo record, and V-1 requires a citable ledger.
- **Contract PD-3 — re-audit execution form: appendix greps with a recorded
  supersession map, results in the survey document.** Families whose rails
  landed in 3.10.4.1/4.2 use the gate as the method, recorded as such: the
  ESLint element/semantic/recipe/token bans plus
  `src/esi-datasets/ui-adoption.test.ts` and `scripts/ui-adoption-rail.test.mjs`
  supersede the button/input/table/`title=`/token-scope/CSS-family greps; the
  judgment-grade families keep their recorded greps verbatim (loading literals,
  headings, `grid-cols-[`, `role=` sweeps, Suspense census). The run lands as a
  new `## 3.10.4.3 adoption re-audit — <date>` section in
  `PHASE_4_ADOPTION_SURVEY.md`: per-family method used (grep or superseding
  gate), result, diff against the recorded survey, plus a terminal mark for the
  rows without one (AD-034/AD-035 delivered by 3.10.4.2's header/content-rail
  work; AD-039 backlogged with citation; the AD-018 EX subset re-confirmed),
  and a residue table where any finding receives the operator's explicit
  disposition — expected empty. Deliberate 3.10.4.2 restyling of keep-as-is
  surfaces is recorded as approved change, not drift (DEP-2). **Rejected:**
  re-running only the greps — it would double-report everything a rail now
  owns and diverge from HC-3's "rails supersede" provision; and recording
  results outside the survey doc — the survey is the re-audit contract and the
  diff must live beside the original record.
- **Contract PD-4 — changelog composition: one `### v3.10.4.3 — <date>` entry
  in `content/changelog/v3.10.md`, absorbing the pending fragments present at
  cutoff.** `content/changelog/pending/` currently holds zero fragments; the
  cutoff check re-lists the directory at release-record time and absorbs
  whatever exists then (deleting absorbed fragments). The entry summarizes in
  user terms: planner KPI/efficiency-color polish, the operator punch-list
  corrections actually delivered, and the completed presentation-system
  verification; internal work (ledger, re-audit) is included per the
  changelog-documents-everything rule. The v3.10 master heading and its
  plain-text summary already exist and are not re-written. `APP_VERSION`
  becomes `'3.10.4.3'`; roadmap row 3.10.4.3 turns `SHIPPED` (terminal) in the
  same commit set. **Rejected:** a separate master-heading rewrite or a new
  pending fragment for this session — planned lifecycle work publishes a
  version entry directly, never a fragment.

- **Plan — loading presentation policy (operator-directed): a recorded
  fallback vocabulary owned by the loading primitives.** A Suspense fallback
  is: the `Skeleton` primitive (shimmer, `role="status"`) when the hole has a
  recognizable content shape the user waits on — portraits, character
  readouts, cards, tiles, dashboards, search/template panels; `LoadingLabel`
  only for small textual/data readouts where a shape would be noise; `null`
  only for sub-perceptual or purely decorative holes. The policy is recorded
  in the two primitives' contract comments (their existing `/** */` blocks
  name when each applies), and the re-audit's fallback census judges against
  it. Existing conformant-but-bare fallbacks are upgraded where the policy
  says shape: the planner landing templates/research holes, portrait and
  character-info holes, and route-level holes that survive the boundary audit.
  **Rejected:** a new policy document — the primitives' own contracts are the
  single-owner home; and per-route bespoke skeleton components — composition
  of the one `Skeleton` primitive stays the vocabulary.
- **Plan — Suspense boundary destination (operator-directed, clarified
  2026-07-26): every route keeps its honest render mode, but its static shell
  carries all honestly static content.** The operator's stated architecture
  is the destination: most pages stay *partial*, with "partial" meaning a
  shell that prerenders the large majority of the page (typically ~95%) at
  build or from `'use cache'`/`cacheLife` caches out of the Neon-cached data,
  carrying the most recent built values; live figures update in view through
  the existing client affordances (LivePrice flash is the exemplar); and
  small request-time Suspense holes remain only for genuinely per-user or
  per-request data (session, `searchParams`, `cookies()`, linked-character
  reads), wearing policy-conformant fallbacks. This is not a conversion to
  fully static routes — the operator explicitly ruled that out as not
  honestly possible.
  The audit walks **all** routes in `scripts/route-classification.json` — not
  just known offenders — and records one ledger row per route under PL-005:
  current shell-versus-hole reality, the richest honest shell destination, the
  fix applied or the recorded justification for staying as-is. `/sites` is
  fixed first: its background-only shell idiom (documented as deliberate in
  the file) is re-judged as drift from the recorded static-first intent (the
  CSP was relaxed specifically to unblock static rendering); the catalogue
  chrome and non-price site content render without waiting on the priced
  overlay (the hourly-cached price-tagged site detail noted by the og-image
  route is the diagnosis starting point), and price figures update in view. A
  route whose split needs real feature re-plumbing pauses at G-3 with the
  diagnosis recorded. Render-mode changes update the route's classification
  row honestly in the same commit; no local production build is ever run —
  expected modes are reasoned and dev-verified, and the post-merge Vercel
  build proves classification. The operator explicitly kept this deeper audit
  in-session rather than routing it to an amendment (2026-07-26).
  **Rejected:** treating the recorded "partial" classification as proof of a
  good shell — `/sites` is classified partial and still drops the whole page
  in; the audit judges rendered reality, not the label. Also rejected:
  request-time streaming as the default for live figures — prerendered values
  plus in-view refresh is the recorded house architecture.
- **Plan — unified price-update affordance (operator-directed): evolve the
  existing `LivePrice` primitive into the single price-update affordance;
  design a clearly perceptible animation.** Today the affordance is split and
  weak: on sites, the in-progress state is a co-opted `PriceConfidence`
  loading circle popped in beside the figure
  (`ResourceRow.tsx: {pending && <PriceConfidence level="unknown" loading />}`,
  the `price-confidence--loading` CSS state) and the just-updated state is
  `live-price.tsx`'s subtle brightness pulse, which the operator judges
  half-broken ("you can't really tell it's happening"); in the planner the
  KPI figures already wrap `LivePrice` (the same weak pulse), the raw ledger
  and build-plan prices render `formatIsk` bare with no affordance at all,
  and the only in-progress signal is the keyed sync toast. The fix widens the existing primitive — the 3.10.4.1 Dot
  precedent, not a new primitive family: `LivePrice` gains two visible
  states (update-in-progress and just-updated) with a redesigned animation
  built as stylesheet keyframes/motion tokens (CSSOM pattern, no inline
  style), reduced-motion honored (HC-6), and no `aria-live` (the existing
  no-announce contract stands). Animation candidates are exercised on
  `/preview/primitives` and the operator picks at a sitting review — the
  animation is taste, so the operator's choice is the spec. Both price
  surfaces adopt it at enumerated leaf render sites: on sites,
  `SiteResourcesLive`/`ResourceRow` (already consuming); in the planner,
  `CockpitKpis.tsx` (already wrapping `LivePrice`, gains the in-progress
  state), `CockpitRawLedger.tsx`, and `CockpitBuildPlan.tsx` price subtotals
  (today bare `formatIsk`), with any additional surface recorded as a
  sitting-level operator ruling. The in-progress state needs one
  concern-conforming wiring addition, resolved here because the seam does
  not exist today: `LivePrice` gains an optional `pending?: boolean` prop,
  and `MarketDataValue` (in `planner-contexts.tsx`) gains
  `refreshing: boolean`, threaded from `PricingProvider`'s existing
  `useRefreshOnView` flag. The context's own contract comment already
  declares it owns "refresh state," so this completes the declared
  market-data concern — **it is not a PricingProvider concern redesign, and
  PricingProvider is otherwise untouched (AF-005)**; if adoption were to
  require more than this single field pass-through, the item pauses at G-3.
  Future price-bearing features inherit the affordance by
  consuming the primitive, and the animation it defines is the **one
  universal updating-value animation**: any live numeric readout site-wide
  signals its updates through this primitive and its motion tokens, never a
  per-feature variant (the primitive may be renamed to say "value" rather
  than "price" as an implementation choice; one owner either way).
  `PriceConfidence`'s `loading` state is retired outright — its sole
  production consumer is the deleted sites co-option, and the badge itself
  verifiably never has an update window to animate: every real render
  computes its level synchronously client-side from pricing state already in
  hand (`cockpit-kpis-view.ts` derives the thin-anchor badge from the
  `pricing` prop in the same render that receives new prices;
  `build-pricing.ts` computes aggregate confidence inside the pricing
  derivation). `PriceConfidence` becomes a static data-quality badge with no
  animation of its own; the `price-confidence--loading` CSS is deleted.
  **Rejected:** a brand-new primitive beside `live-price` (two owners for one
  concern); keeping per-feature animations (the exact divergence the operator
  reported); and a separate PriceConfidence animation (nothing ever waits
  there — animating it would fabricate a state that cannot occur).
- **Plan — toast overlap fix (operator-reported bug): simultaneous toasts
  must stack, not overlap.** The template-loaded success toast
  (`TemplateLoader.tsx`) and the keyed `> syncing…` toast (`loading-toast.tsx`,
  fired while the loaded template's prices refresh) render over each other.
  Diagnosis starts at the `unstyled: true` Toaster configuration in
  `src/components/ui/toast.tsx` — sonner's stack offsets depend on measured
  toast heights and its own stylesheet, which `unstyled` partially drops —
  and the fix lands in `toast.tsx` (the sole sonner owner) so stacking works
  for every pair of concurrent toasts, proven with an open-state probe
  showing both toasts visible and offset. sonner stays the library; no
  second toast system. **Rejected:** suppressing one of the toasts —
  both messages are wanted; the presentation is the defect.
- **Plan — remaining seed-fix mechanics (operator-directed, 2026-07-26).**
  *PL-009 owned-asset outline retone:* the ownership outline on
  blueprint/buildable indicators (today `FRAME_TONE.owned =
  'border-evb-bright'` in `NodeCard`) moves to the ISK green matching the
  owned item counts — the blue outline blends into the blueprint art. The
  retone lands as the tone-map move's `owned` mapping (one line in the new
  `industry-styles.ts` export), so all consumers shift together; whether the
  ME/TE gem/value blue also retones is operator taste, decided at the sitting
  review with the default being outline-only. *PL-010 research-job monogram
  (diagnose first):* the naive hypothesis is FALSE — `esi-projection.ts`
  documents `product_type_id` as absent on copying/research jobs, so
  `jobImage(undefined, blueprintTypeId)` already serves the `bp` rendition on
  that path; the observed monogram's cause is elsewhere (candidate causes to
  test against the live failing corp job: an invention/copy job whose
  present `product_type_id` is a blueprint/BPC type served as `/icon`; a
  formula or blueprint type whose `/bp` rendition itself 404s; a corp-versus-
  personal projection difference). Step 1 reproduces the failing job first —
  recording activity id, `product_type_id`, requested rendition URL, and HTTP
  status — then fixes the confirmed cause inside the `type-images.ts` intent
  resolvers, proven in the co-located view tests. The wider rule the
  monogram stays as a last-resort guard, but an *observed* monogram on real
  data is a rendition-choice defect — sittings sweep the image-bearing
  surfaces and any sighting becomes a ledger item. *PL-011 hero blueprint
  experiment:* the planner hero swaps its 3D `render` for the blueprint `bp`
  rendition (`heroImage` intent) as a sitting experiment; the operator judges
  keep-or-revert on the live route. *PL-012 multibuy CopyButton adoption:*
  `MultibuyPanel` renders a plain `Button` labeled "Copy" with a hand-rolled
  clipboard call and toast feedback — an adoption bypass beside the existing
  `CopyButton` primitive, which already owns the idle→copied→idle feedback
  cycle and the clipboard-unavailable state. The panel migrates to
  `CopyButton` (keeping its entry-count message semantics where the
  primitive's slots allow), and the hand-rolled clipboard path is deleted.
  *PL-013/PL-014 clickability experiments:* the multibuy and raw-ledger
  disclosure triggers and the planner templates trigger read as plain text
  today; sittings experiment with the pressable Chip/Pill vocabulary (or
  another existing affordance) so users can tell they are interactive, with
  the operator judging each on the live route. *PL-015 ME/TE glyph
  alignment:* the gem reads optically right of the hourglass in the planner's
  paired fields; the fix aligns the two SVG glyphs' optical boxes in
  `MeAdjuster.tsx` (viewBox/path centering), proven visually at the sitting.
  *PL-016 time-bonus icon trigger:* the build-character TE-bonus display is
  redesigned to a small icon trigger whose hover/focus opens the existing
  Popover breakdown (`BuildSkillsIndicator` already composes Popover and the
  per-skill rows — the change is the trigger presentation); the icon choice
  is experimental, operator-judged at the sitting, standard palette.
  *PL-017 related-inputs callout (highly experimental, sequenced last):* the
  build plan already computes a per-row `related` flag driving the dim
  cascade (`CockpitBuildPlan`); the experiment adds a thin ISK-green border
  on related input rows via that flag, and optionally connector lines from
  the selected buildable to each input. The border half fits the vocabulary
  (a tone-map class on an existing state). The connector-line half needs new
  measured-overlay machinery no primitive owns — if the operator likes the
  direction after seeing the border, the line half routes through G-3
  (amend-or-backlog) rather than being absorbed; explicit keep-or-revert
  recorded either way. **Rejected:** hardcoding tones
  or renditions at call sites — both stay in their intent/tone-map owners —
  and keeping a second hand-rolled clipboard implementation beside the
  primitive.
- **Plan — house-style re-establishment (operator-directed): strengthen
  `src/AGENTS.md` "Routes and render modes", not a lint rule.** The existing
  most-static-honest-mode ladder survives but gains the composition rules the
  drift proves it needs: the static shell must carry the page's meaningful
  chrome and all content not genuinely request-scoped (a background-only
  shell is named as the anti-pattern); Suspense holes are as small as honesty
  allows; every fallback follows the recorded loading-primitive policy;
  prerendered-then-in-view-refresh is preferred over request-time streaming
  for live figures. The same edit records the operator's frame-alignment
  principle for the styling section's vocabulary: content aligned with its
  frame aligns *within* the frame — a row of cards shares one internal
  plane for its text (an invisible grid, in the operator's words), and
  mixed-height headers or controls must not push siblings' content off that
  plane; the KPI fix (PD-1) is the named exemplar, and sittings apply the
  principle wherever the up-and-down printing appears. The operator ruled a
  lint rule inappropriate — boundary placement is judgment-grade — so the
  guide (which both agent trees import) plus the route-classification
  justifications are the enforcement surface.
  After the guide edit, the agent-policy maintenance steps run:
  `python3 .agent-local/reconcile_skill_ledger.py` and
  `python3 .agent-local/check_agent_drift.py` green before close-out.
  **Rejected:** a lint/census rail (operator ruling; a mechanical rule cannot
  judge which content is honestly request-scoped) and a new standalone design
  document (single-owner policy lives in the guides).

### Audit-remediation mapping

Not applicable — this is not an audit-remediation contract.

## Design pressure and baseline effect

### Hotspot proximity

- **Touched measured surfaces:** One — the baseline row
  `Planner concern-context fields | 4 / 10 / 18 / 6 / 13`
  (`docs/CODE_HEALTH_BASELINE.md`): PL-007's authorized `refreshing` addition
  makes `MarketDataValue` five fields.
- **Live proximity evidence:** All other punch fixes land in presentation
  files (`src/components/ui/`, `*-styles.ts`, planner components) that are
  not baseline rows; the AF-005 boundary paths beyond the authorized
  pass-through and the AF-006/AF-007 telemetry watch surfaces are not in any
  planned diff. Verdict: adjacent on the one named row, outside elsewhere.

### Preparatory refactor

None — the seams already exist: `kpi-tile.tsx` owns the shared tile chrome,
`industry-styles.ts` is the established domain→tone owner, and the
registry/census pair owns exemptions. The seed fixes are moves into existing
owners, not restructures.

### Baseline effect and update

- **Effect:** Neutral — fixes re-express existing looks through existing
  owners and suppressions do not grow. The one measured-surface change is the
  operator-authorized `refreshing` field completing `MarketDataValue`'s
  declared refresh-state concern: a census-value update, not decay pressure,
  recorded here as an explicit operator-directed exception.
- **Required update:** Refresh the
  `Planner concern-context fields` row to `5 / 10 / 18 / 6 / 13` in the same
  change that adds the field; the version-close audit after merge owns the
  next full baseline refresh.

## Implementation blueprint

### Owned surfaces

- `src/features/industry-planner/industry-styles.ts` — gains sole ownership of
  ME/TE ownership-state color meaning (state → border/fill/glow/text classes).
- `src/features/industry-planner/components/kpi-tile.tsx` — `KpiHead` owns the
  fixed label-row height; all six KPI tiles inherit the shared figure baseline.
- `src/features/industry-planner/components/{MeAdjuster,NodeCard,HeroCard,CockpitBuildPlan}.tsx`
  — consume the tone-map export; no local state→tone map survives.
- `src/components/ui/skeleton.tsx` and `src/components/ui/loading-label.tsx` —
  their contract comments own the recorded loading-fallback policy.
- `src/components/ui/live-price.tsx` (+ its keyframes/motion tokens in
  `globals.css`, + a new co-located `live-price.test.ts`) — owns the
  two-state price-update affordance for every price-bearing surface, present
  and future; the test file covers the in-progress state, the just-updated
  replay, first-mount no-flash, and the reduced-motion variant.
- `src/features/industry-planner/components/planner-contexts.tsx` —
  `MarketDataValue` gains `refreshing: boolean` (completing its declared
  refresh-state concern); `PricingProvider` passes its existing flag through.
- `src/components/ui/toast.tsx` — owns correct stacking for concurrent
  toasts; remains the sole sonner seam.
- `src/data/eve-data/type-images.ts` — its intent resolvers own every
  rendition choice; `jobImage` gains activity awareness and `heroImage`
  carries the operator's hero-experiment outcome.
- `src/AGENTS.md` ("Routes and render modes") — owns the re-established
  shell-composition house style; both agent trees inherit it, with the
  skill-ledger and drift checks green after the edit.
- Route page files under `src/app/` whose boundary the audit moves (starting
  `src/app/sites/page.tsx`), plus `scripts/route-classification.json` — own
  the shell-versus-hole split and its honest render-mode record.
- `docs/version-audits/3.10/PHASE_4_PUNCH_LEDGER.md` — new; owns the punch
  record and dispositions, including the per-route boundary-audit results
  under PL-005.
- `docs/version-audits/3.10/PHASE_4_ADOPTION_SURVEY.md` — gains the re-audit
  section and terminal marks; rows and appendix are otherwise immutable.
- `src/composition/ui-adoption-registry.ts` + `eslint.config.mjs` +
  `src/esi-datasets/ui-adoption.test.ts` — updated only if an approved punch
  fix changes an exemption; always in one commit with the census.
- `docs/VERSION_3_10_PLAN.md` (status row), `src/config/app-version.ts`,
  `content/changelog/v3.10.md`, `content/changelog/pending/`,
  `docs/session-plans/3.10/3.10.4.3.1.md` (execution marker, at close-out),
  `docs/session-as-built/3.10/3.10.4.3.1.md` — the release records.

### Interfaces and contracts

- `industry-styles.ts` new export — a pure state→classes mapping for the
  owned/manual/unowned (+ bonus/reaction glyph) states, shaped for its four
  consumers (border class for NodeCard's frame, fill+glow for the gem and
  hourglass, text class for adjuster values and HeroCard labels). Exact
  signature settles at implementation against the four call shapes; it carries
  a `/** */` contract comment naming itself the only owner of this meaning.
- `KpiHead` — signature unchanged (`{ label, right? }`); its rendered row
  gains a fixed height; callers change nothing.
- `PHASE_4_PUNCH_LEDGER.md` — columns as resolved in PD-2; prose document, no
  parser.
- `LivePrice` — gains optional `pending?: boolean` (in-progress state; absent
  = current value-change-only behavior), preserving the existing
  `{ value, className }` contract and the no-`aria-live` rule.
- `MarketDataValue` (planner-contexts.tsx) — gains `refreshing: boolean`,
  supplied by `PricingProvider` from its existing `useRefreshOnView` result;
  consumers treat it as read-only display state.
- `jobImage` (type-images.ts) — its shape settles from PL-010's confirmed
  live diagnosis (the research path already serves `bp`; the defect is in an
  adjacent rendition choice); callers are the job-view models only, and
  rendition logic stays in the intent resolvers.
- No other production route, API, schema, or data interface changes.

### Control and data flow

Style punch fixes are render-layer class and markup-composition changes with
no runtime data flow change. Two sanctioned exceptions exist. First, the
loading/streaming pass (step 3) moves Suspense boundaries and may annotate
global slow-changing reads with `'use cache'`/`cacheLife`/`cacheTag`,
changing *when* existing reads run (build/cache time versus request time) but
never what is read, written, or validated. Second, the PL-007 wiring (step 4)
threads the already-computed `refreshing` boolean from `PricingProvider`
through `MarketDataValue` to leaf `pending` props — read-only display state,
no new fetch or mutation; request-time reads (`searchParams`, `cookies()`,
session, live prices) remain inside holes, and route handlers, mutations, and
data schemas are untouched. Any punch item requiring an actual data or
behavior change pauses at G-3.

### Edge and failure behavior

- Punch item outside the fix vocabulary → G-3 pause; operator rules amendment
  (split trigger recorded, session returns through the lifecycle) or backlog
  (item lands in `docs/backlog.md` with survey/ledger citation); ledger records
  the ruling.
- Re-audit finds residue → each finding gets a ledger-style row in the survey's
  residue table and an explicit operator disposition; an undispositioned
  finding blocks DC-3.
- PL-004 (dev instrumentation noise) diagnosis shows a development-only Next
  artifact with clean durable probes → the honest terminal disposition may be
  `No-change` with the diagnosis recorded, or a bounded ux-check tooling
  accommodation; a Next-version or framework fix is out of scope.
- Operator unavailable mid-sitting → work stops at the sitting boundary;
  resumption re-enters through `start-session` on this branch (HC-2).
- A punch fix would edit an AF-005-bounded file beyond the PL-007-authorized
  `refreshing` pass-through → stop, report, route to amendment; never
  absorbed.
- A boundary-audit route whose shell split needs feature re-plumbing (data
  reshaping, per-component price-flow redesign) → G-3 pause with the recorded
  diagnosis; the route keeps its current boundary until the operator rules.
- A boundary move would change a route's honest render mode → the
  classification row and its one-line justification change in the same
  commit; a move that cannot stay honest is not made.
- Planner adoption of the price affordance would require more than the
  PL-007-authorized single `refreshing` pass-through (AF-005) → G-3 pause
  with the diagnosis; leaf-site consumption plus that one field is the only
  sanctioned form.

### Ordered work

1. **Seed planner fixes (PL-001…PL-003, PL-009, PL-010, PL-012, PL-015).**
   Move ME/TE state color meaning into `industry-styles.ts` and repoint
   `MeAdjuster` (`iconTone`, field value classes), `NodeCard` (`FRAME_TONE`),
   `HeroCard` (label tones), and `CockpitBuildPlan` at the single export,
   setting the owned-outline mapping to the ISK green (PL-009); fix
   `KpiHead`'s label-row height in `kpi-tile.tsx`; reproduce the failing
   corp-job monogram against live data (activity id, `product_type_id`,
   requested rendition URL, HTTP status) and fix the confirmed cause in the
   `type-images.ts` intent resolvers (PL-010 — the research path already
   serves `bp`, so the naive activity fix is known insufficient); migrate the
   multibuy panel's hand-rolled
   copy path to the `CopyButton` primitive (PL-012); align the gem and
   hourglass glyphs' optical boxes (PL-015). Prove with the planner
   component, job-view, and copy-button tests, a keyboard probe on the
   adjuster popover (HC-6), and visual verification of the seed checks across
   their named components, including the failing corp job showing its EVE
   image per the confirmed PL-010 diagnosis and the copy control flipping to
   its copied state (AC-2).
2. **Routed matrix defect (PL-004).** Reproduce the dev-only negative
   performance timestamps / aborted dev-request noise from PR #310's notes in a
   capture run, attribute it, and record its terminal disposition in the
   ledger per the edge-behavior rule.
3. **Loading and streaming presentation pass (PL-005, PL-006).** Record the
   fallback policy in the two loading primitives' contract comments and the
   strengthened house style in `src/AGENTS.md` "Routes and render modes"
   (running the skill-ledger and drift checks after the guide edit); walk
   **every** route in `scripts/route-classification.json`, recording one
   PL-005 ledger row per route (current reality, richest honest shell
   destination, fix or justified keep); fix `/sites` first — the named seam:
   `listSiteDetails` is already `'use cache'`/`cacheLife('max')`, so a
   cached priced-catalogue read (mirroring `getPricedSiteDetail`'s hourly
   price-tagged pattern) renders the catalogue and filter chrome in the
   shell, the existing client in-view live-price path
   (`useRefreshOnView`/`SiteLiveProvider`) keeps figures fresh, and the
   `searchParams` sort plus view-cookie reads isolate into small holes —
   then every other route the audit finds short of
   its destination; upgrade content-shaped fallbacks to `Skeleton` per policy
   (planner landing templates/research, portraits, character readouts). Read
   the installed Next Cache Components/PPR guides and confirm current API via
   `find-docs` before the first boundary move. Prove each fixed route with a
   dev-render check that the shell paints without waiting on the holes
   (dev-log split / probe), clean ux-check on affected routes, and honest
   classification rows.
4. **Price-update affordance and toast stacking (PL-007, PL-008).** Widen
   `src/components/ui/live-price.tsx` to the two-state affordance (optional
   `pending?: boolean` + the redesigned update animation, stylesheet
   keyframes + motion tokens, reduced-motion variant) and create its
   co-located `src/components/ui/live-price.test.ts` (in-progress state,
   just-updated replay, first-mount no-flash, reduced-motion); add
   `refreshing: boolean` to `MarketDataValue` in `planner-contexts.tsx`,
   threaded from `PricingProvider`'s existing flag; exercise the candidates
   on `/preview/primitives` and obtain the operator's pick at a sitting
   review; adopt at the enumerated leaf sites (`SiteResourcesLive`/
   `ResourceRow`, `CockpitKpis.tsx`, `CockpitRawLedger.tsx`,
   `CockpitBuildPlan.tsx` subtotals); diagnose and fix the toast stacking
   defect in `src/components/ui/toast.tsx` and create
   `docs/ux-check/probes/toast-stack.mjs` exercising the template-loaded
   toast concurrent with the keyed sync toast. Prove with the new test file
   green, a keyboard/reduced-motion probe, the toast-stack probe showing
   both toasts stacked and readable, and the operator's review of the chosen
   animation on the live routes.
5. **First sitting: planner punch area.** Take the operator's planner reports,
   triage each against the vocabulary, implement approved fixes in their owning
   primitive/tone-map/token, run the PL-011 hero blueprint-image experiment
   for the operator's keep-or-revert call, present the PL-009 outline retone
   (and the gem/value blue question) for taste review, run the PL-013/PL-014
   clickability experiments on the multibuy, raw-ledger, and templates
   triggers, run the PL-016 time-bonus icon-trigger experiment, sweep the
   planner's
   image-bearing surfaces for observed monograms (PL-010 rule), run ux-check
   on affected planner routes, and end with the operator's sitting review
   (G-1). Ledger rows for every item.
6. **Remaining sittings: site-wide punch lists.** Repeat the sitting loop
   across all remaining routes including admin until the operator declares the
   punch lists resolved (G-2, recorded). Exemption-touching fixes update
   registry + flat-config + census in the same commit (HC-4). The PL-017
   related-inputs experiment runs in the final sitting by operator direction:
   border half first, keep-or-revert recorded, and the connector-line half
   only via a G-3 ruling.
7. **Adoption re-audit.** Run the PD-3 method map, record the section, terminal
   marks, and residue dispositions in the survey document; obtain operator
   dispositions for any residue. Prove with the recorded section and green
   rails (DC-3, HC-3).
8. **Version-final release records.** Turn roadmap row 3.10.4.3 `SHIPPED`, set
   `APP_VERSION = '3.10.4.3'`, and publish the `### v3.10.4.3` changelog
   entry absorbing the fragments present at cutoff (the as-built record is
   close-out's, authored once the PR number exists).
   Prove with `python3 .agent-local/check_release_consistency.py --check`
   clean.
9. **Close-out (planned mode).** Invoke `close-out` passing the original
   planned directive: final ux-check evidence, the origin-main-pinned
   verification, pre-PR design review, PR creation, review-gate settlement —
   and stop unmerged at the operator-reserved merge gate (DC-6). The execution
   marker turns Complete only when the truthful final state exists on the
   branch (HC-5).

## Success criteria (agent-runnable — show the output)

- **SC-1 — Contract DC-1 / AC-1 / V-1 / G-2.** `cat docs/version-audits/3.10/PHASE_4_PUNCH_LEDGER.md`
  → every `PL-NNN` row (seeds, sitting items, and the routed matrix defect)
  carries a terminal disposition — including every G-3 amend-or-backlog
  ruling — and the ledger records the operator's resolution declaration.
- **SC-2 — Contract DC-2 / AC-2.** `git diff origin/main --stat` per punch fix
  → fix diffs touch only primitives, tone maps, registered tokens, intent
  resolvers, their consumers, and sanctioned docs; plus the operator-verified
  visual pass of the seed checks (contract seeds plus PL-009's green outline
  and PL-010's confirmed-diagnosis job image) across KpiTiles, MeAdjuster,
  NodeCard, HeroCard, CockpitBuildPlan, and the active-jobs rows.
- **SC-3 — Contract DC-2 (seed mechanics).** `pnpm test` filtered to the
  planner component and `ui-adoption` suites → green, including the tone-map
  move leaving zero local state→tone maps (`grep -rn "FRAME_TONE\|iconTone"
  src/features/industry-planner/components/` returns only tone-map consumers).
- **SC-4 — Contract DC-3 / AC-3 / V-2.** The survey document's new re-audit
  section → per-family method + result + diff recorded, zero unexempted
  variants, zero unrailed primitives, complete exemption list, every `AD-NNN`
  row terminal, residue table empty or fully dispositioned.
- **SC-5 — Contract DC-4 / AC-1.** `grep -n "PL-004" docs/version-audits/3.10/PHASE_4_PUNCH_LEDGER.md`
  → the row shows a terminal disposition with recorded diagnosis for the
  routed responsive-matrix defect.
- **SC-6 — Contract DC-5 / AC-4 / V-4.** `python3
  .agent-local/check_release_consistency.py --check` → `{"errors": [],
  "warnings": []}` with roadmap row `SHIPPED`, `APP_VERSION '3.10.4.3'`, the
  published changelog entry, absorbed (or verified-empty) pending directory,
  and the Complete marker on the branch.
- **SC-7 — Contract V-5 / HC-4.** `FALLOW_AUDIT_BASE=$(git rev-parse
  origin/main) pnpm verify` → exit 0: typecheck, zero-warning lint (all rails
  green), coverage suite, Fallow zero issues.
- **SC-8 — Contract G-1 / V-3 / IS-5.** The `ux-check` sweep
  (`node scripts/ux-capture.mjs` per its skill) on punch-affected routes per
  sitting → capture and probe output clean; the operator's per-sitting
  approvals and the final pre-PR approval recorded.
- **SC-9 — Contract DC-6 / AC-5.** `gh pr view <PR> --json state,statusCheckRollup`
  plus `python3 .agent-local/poll_pr_gate.py <repo> <pr> review` → PR `OPEN`,
  current-head CI green, review gate settled with zero unresolved findings,
  merge not performed.
- **SC-10 — Contract DC-1/DC-2 (PL-005, PL-006) / G-1.** The ledger's PL-005
  audit table checked against `scripts/route-classification.json` → every row
  carries a terminal verdict (fixed or justified keep), plus, for each fixed
  route, the dev-render evidence that the shell paints without waiting on the
  holes (e.g. the `/sites` dev-log split showing content HTML before the
  price overlay resolves); the survey appendix's Suspense census
  (`grep -rn 'Suspense' src --include='*.tsx'`) cross-checked against the
  PL-005 ledger so every hole's fallback — including variable-valued
  `fallback={...}` forms the attribute grep cannot judge — has a recorded
  policy verdict; honest
  classification rows; the `src/AGENTS.md` house-style diff with
  `python3 .agent-local/reconcile_skill_ledger.py` and
  `python3 .agent-local/check_agent_drift.py` green; and the operator's
  review of the loading behavior in a sitting.
- **SC-11 — Contract DC-1/DC-2 (PL-007, PL-008) / G-1 / HC-6.** `pnpm test
  src/components/ui/live-price.test.ts` → green (in-progress, just-updated
  replay, first-mount no-flash, reduced-motion); `/preview/primitives`
  exercising the animation states with the operator's recorded pick; the new
  `docs/ux-check/probes/toast-stack.mjs` showing the template-loaded and
  `> syncing…` toasts simultaneously visible, stacked, and both readable; and
  `grep -rn "price-flash\|price-confidence--loading" src/` showing the legacy
  flash class and the co-opted loading-circle composition retired (or owned
  solely by the primitive), with `PriceConfidence` serving only data-quality
  levels.

## End of session

- Confirm every `DONE =` item is evidenced, every `hard_constraints` boundary
  held, the Neutral baseline verdict stands, and every contract pause (G-1 per
  sitting, G-2 declaration, G-3 rulings, the merge gate) was honored.
- **Delivery:** commit and push on `lifecycle/3.10.4.3`; open the single
  sub-version PR through planned close-out after the final sitting's approval;
  settle the review gate; stop unmerged at the operator-reserved merge gate.
  The operator's later `close-out` invocation on this same branch and PR owns
  the merge and production proof.
- **Lifecycle artifacts:** this plan's `Execution status: Complete` marker (at
  close-out), the as-built record carrying the PR number (authored by
  close-out once the PR exists), roadmap row
  3.10.4.3 `SHIPPED`, `APP_VERSION`, the published changelog entry with
  absorbed fragments, the punch ledger, the survey re-audit section, and the
  SCRATCHPAD update.
- **Handoff:** rerun the resolver after close-out stops; report its directive
  without predicting it (the master plan's expectation after merge is the
  version-close audit, which is not this session's to start).
