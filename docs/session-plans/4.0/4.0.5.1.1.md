# Session 4.0.5.1.1 Implementation Plan — Cycle-1 audit floss

**Plan status:** Approved
**Approved:** 2026-08-15
**Contract:** `docs/session-contracts/4.0/4.0.5.1.1.md`
**Contract digest:** `sha256:391538ca49e063d4d9b7d366f1b3e55db1855a0abf2a1c81e954e5078ceee839`
**Planning standard:** `docs/workflows/schema/session-plan.md`
**Proof standard:** Atomic
**Execution status:** Complete
**Baseline effect:** Neutral

## Bottom line (READ FIRST)

- **GOAL:** Cycle-1 Floss findings AF-010, AF-011, and AF-012 are Delivered, so version-close can restart on current `main` with no open actionable rows.
- **DONE =** SC-1 through SC-7 below, plus: the delivering PR carries the three Delivered markers, the `v4.0.5.1` changelog heading, and `APP_VERSION` `4.0.5.1`.
- **OUT OF SCOPE:**
  - Watch findings AF-006, AF-007, and AF-008, their triggers, or their dispositions (Contract OOS-1).
  - Redesigning `check-doc-refs` to resolve relative paths from the source file (Contract OOS-5).

<hard_constraints>

- **Contract HC-1:** This session maps only AF-010, AF-011, and AF-012 and absorbs no unaudited scope. The diff must not fold in `postProjection`, `purgeMapChain`, Watch rows, baseline Current-cell refresh, AF-009 citation drift, mapper unreachable-Fallow, `SectionHeader` CRAP, or master-plan carry-forwards. SC-7.2 is an exact allowlist of authorized paths.
- **Contract HC-2:** Slice HTTP contracts — path, body, schema, and error class — stay with their composition owners; `postConvexHttpDoor` owns transport only. The thin `postDoor` wrappers remain. Do not inline slice contracts into the shared helper and do not move transport back into the slices.
- **Contract HC-3:** A warning-free document-reference check must come from retargeting the one layout-table cell to `docs/contributing/end-to-end-testing.md`, not from changing the checker's `../` archive-reference rule. `tools/policy/check_doc_refs.py` is read-only in this session.
- **Contract HC-4:** A documented accepted clone is allowed only after a real shared transport owner still cannot satisfy Fallow without changing slice HTTP contracts. Live Fallow reports no `dup:50ee3d46`; do not add a waiver. If that clone reappears, pause — do not invent a replacement.
- **Plan:** This session adds or changes no production export.
- **Plan:** Planned final close-out owns the release triplet. It prepends `### v4.0.5.1 — YYYY-MM-DD` via `docs/workflows/schema/changelog-entry.md`, folds every pending fragment present after syncing `origin/main`, deletes consumed fragments, and sets `APP_VERSION` to exactly `4.0.5.1`. The version's own Fixed bullet is plain language with no bold, inline code, or links: the ux-check workspace guide now names the live end-to-end testing policy. No invented user-facing Added or Changed product copy. Ordered work does not write `content/changelog/v4.0.md` or `src/config/app-version.ts`.
- **Plan:** The already-written remediation persist on this branch (Phase 5 master-plan extension, `docs/session-contracts/4.0/4.0.5.1.1.md`, contract index row, Planned ledger rows with the corrected AF-012 diagnosis) rides in the OW-1 commit. Do not rewrite the contract.
- **Plan:** If `PointerPopover` reappears, delete it in OW-1; do not add a consumer. If retargeting the one cell does not clear the `check-doc-refs` warning, pause; do not change the checker.

</hard_constraints>

**Branch:** `lifecycle/4.0.5.1` · **ends in PR:** yes · **gate:** the sub-version PR passes the standing external review gate and merges via planned `close-out`; no operator UX pause exists before the PR opens.

**Contract UX gate:** `No` · **required pause:** None

## Read first

- `AGENTS.md`
- `docs/session-contracts/4.0/4.0.5.1.1.md`
- `docs/ux-check/README.md` — the one layout cell this session retargets
- `src/components/ui/popover.tsx` — AF-010 proof surface; expected consumed-only exports
- `src/lib/convex-http-door.ts` — AF-011 shared transport owner
- `docs/version-audits/4.0/PLAN.md` — ledger rows this session marks Delivered

## Current state and prerequisites

| Contract input | Live verdict | Evidence | Execution consequence |
| --- | --- | --- | --- |
| `DEP-1` | `Verified` | Cycle-1 ledger in `docs/version-audits/4.0/PLAN.md` lists AF-010, AF-011, and AF-012 as the only Floss rows, each mapped to `4.0.5.1` with Status `Planned` (persist already corrected the AF-012 diagnosis) | OW-2 marks those three rows `Delivered`; do not remap, add, or drop findings |
| `DEP-2` | `Verified` | PR #429 (`160b37ef`) deleted `PointerPopover` and extracted `src/lib/convex-http-door.ts`. Live `src/` has no `PointerPopover` symbol. Both slice `postDoor` wrappers only call `postConvexHttpDoor`. `pnpm exec fallow dupes --fail-on-issues` reports `No code duplication found` and no `dup:50ee3d46` | OW-1 proves those end-states and does not rewrite popover or door code unless proof fails |
| `DEP-3` | `Verified` | `docs/contributing/end-to-end-testing.md` exists. `python3 tools/cli.py policy check-doc-refs --check` warns `docs/ux-check/README.md:19: archive reference does not resolve: ../contributing/end-to-end-testing.md` | OW-1 retargets that cell to `docs/contributing/end-to-end-testing.md` |
| `DEP-4` | `Verified` | Baseline Watch section still carries AF-006/007/008 only; they are not in this bundle | Diff must not edit Watch rows, triggers, or those measured files |

## Why now

Cycle-1 version-close left three Floss rows. Two are already true on live `main` and still sit Planned on the ledger; the third is a live `check-doc-refs` warning on one layout cell. Closing the three rows in one sub-version PR lets the resolver restart the full version-close audit against current `main`. This is the sole session of 4.0.5.1, so planned final close-out also absorbs the live `content/changelog/pending/` inbox after syncing `origin/main`.

## Scope (the destination)

When this session ends, AF-010 remains true (`PointerPopover` absent; `popover.tsx` exports only `PopoverTone`, `Popover`, `PopoverHeading`, and `PopoverRow`), AF-011 remains true (`postConvexHttpDoor` owns transport; slice doors bind path, body, schema, and error class; Fallow reports no `dup:50ee3d46`), and AF-012 is true (the ux-check layout table names `docs/contributing/end-to-end-testing.md` and `check-doc-refs` is warning-free on that line). The ledger diagnosis for AF-012 already names the checker's `../` archive-reference rule. The delivering PR marks all three findings Delivered, publishes `### v4.0.5.1 — YYYY-MM-DD` in `content/changelog/v4.0.md`, and sets `APP_VERSION` to `4.0.5.1`. No runtime jump or elimination path changes. Watch surfaces, `convex/engine.ts`, the mapper host, `postProjection`, `purgeMapChain`, and the document-reference checker stay untouched.

### Scope coverage

| Contract boundary | Implementation mapping or protection |
| --- | --- |
| `IS-1` | OW-1 characterization of `popover.tsx` and the `src/` import graph; code change only if `PointerPopover` reappears (PD-1) |
| `IS-2` | OW-1 characterization of `postConvexHttpDoor` and both slice `postDoor` wrappers plus Fallow dupes; wrappers stay (PD-2, HC-2) |
| `IS-3` | OW-1 retargets `docs/ux-check/README.md` line 19 to `docs/contributing/end-to-end-testing.md`; AF-012 diagnosis already corrected in the persist (PD-3) |
| `IS-4` | OW-2 marks AF-010/011/012 Delivered. Planned final close-out publishes the `v4.0.5.1` changelog entry (including folded pending fragments) and sets `APP_VERSION` to `4.0.5.1` |
| `OOS-1` | SC-7.2 allowlist excludes Watch-trigger and measured Watch files; DEP-4 keeps those rows Watch |
| `OOS-2` | `docs/CODE_HEALTH_BASELINE.md` is not on the SC-7.2 allowlist |
| `OOS-3` | Master-plan AF-009 citations, mapper unreachable-Fallow, and `SectionHeader` are not on the SC-7.2 allowlist; HC-1 |
| `OOS-4` | Master-plan carry-forwards stay in Carry-forwards; this session does not implement them |
| `OOS-5` | `tools/policy/check_doc_refs.py` is not on the SC-7.2 allowlist; HC-3 |

## Resolved implementation decisions

- **Contract PD-1 — AF-010 already true: prove, do not rewrite.** Live `src/` has no `PointerPopover` definition, export, or importer. `popover.tsx` exports only `PopoverTone`, `Popover`, `PopoverHeading`, and `PopoverRow`. PR #429 deleted the unused export. **Rejected:** adding a consumer (Signature Editor already uses `ScannerAnchoredPanel`; inventing a caller would absorb unaudited scope) and leaving a leftover export if proof later finds one (HC-1 / replace-and-delete: delete it in OW-1).
- **Contract PD-2 — AF-011 already true: keep the thin slice wrappers.** `postConvexHttpDoor` owns URL, bearer, POST, status, JSON, and Zod parse. Jump `postDoor` binds `'/jump-evidence' \| '/resolve-jump'`, body, schema, and `JumpConvexUnavailableError`. Elimination `postDoor` binds `'/signature-elimination'`, body, schema, and `EliminationConvexUnavailableError`. Fallow dupes reports no clone group, including no `dup:50ee3d46`. **Rejected:** deleting the wrappers (would move slice HTTP contracts into the shared helper, violating HC-2), folding `postProjection` / `purgeMapChain` into this session (HC-1; different contracts), and writing an accepted-clone waiver (HC-4 is not met).
- **Contract PD-3 — AF-012 retargets the layout cell to the live repo-root path.** Change `../contributing/end-to-end-testing.md` to `docs/contributing/end-to-end-testing.md`. The file exists; the row is a useful pointer; the checker treats `../` as a repo-root archive path, so the repo-root spelling is the honest citation. **Rejected:** dropping the row (silences the checker by deleting a live pointer) and changing the checker's archive-reference rule (HC-3 / OOS-5).

### Audit-remediation mapping

| Finding | Principle-level outcome | Selected plan elements | Proving criteria |
| --- | --- | --- | --- |
| `AF-010` | Unused public surface is gone: `popover.tsx` exports only consumed primitives; `PointerPopover` is absent from the export surface and import graph | PD-1; OW-1 proof (delete only if the symbol returns) | `SC-1` |
| `AF-011` | One shared helper owns Convex HTTP transport; slices own path, body, schema, and error class; Fallow does not report `dup:50ee3d46` | PD-2; HC-2; HC-4; OW-1 proof; wrappers stay | `SC-2` |
| `AF-012` | Layout table names the live repo-root path; `check-doc-refs` is warning-free on that line; ledger diagnosis names the `../` archive-reference rule | PD-3; HC-3; OW-1 retarget; persist already corrected the diagnosis; OW-2 marks Delivered | `SC-3`, `SC-4`, `SC-5` |

## Design pressure and baseline effect

### Hotspot proximity

- **Touched measured surfaces:** None. Watch AF-006/007/008 files (`src/data/telemetry/queries.ts`, `src/data/esi-refresh-jobs/queries.ts`, auth contract paths) are not edited. `convex/engine.ts` and `src/mapper/index.ts` are not on the door path.
- **Live proximity evidence:** The only production source edit is `src/config/app-version.ts` (version string). The AF-012 edit is `docs/ux-check/README.md`. Door and popover files are proof-only. Verdict: outside every Watch hotspot and outside the mapper host / `convex/engine.ts` surfaces.

### Preparatory refactor

None — `postConvexHttpDoor` is already the transport seam, `popover.tsx` already has the consumed-only export surface, and the layout table already has the one cell to retarget. No restructuring is required before OW-1.

### Baseline effect and update

- **Effect:** `Neutral` — no Watch-listed surface grows, no Fallow threshold override is added, and `convex/engine.ts` / the mapper host are untouched. Fallow clone/unused-export signals are proof, not a reason to change thresholds.
- **Required update:** None. Contract OOS-2 forbids baseline Current-cell refresh and Code-ref advance.

## Implementation blueprint

### Owned surfaces

- `docs/ux-check/README.md` — layout-table cell cites `docs/contributing/end-to-end-testing.md` (OW-1).
- `docs/version-audits/4.0/PLAN.md` — AF-010, AF-011, and AF-012 Status `Delivered`; AF-012 diagnosis already names the archive-reference rule (OW-2 status; diagnosis rides from persist).
- Close-out owned (not Ordered work): `content/changelog/v4.0.md` (`### v4.0.5.1 — YYYY-MM-DD` plus folded pending fragments), `src/config/app-version.ts` (`4.0.5.1`), `docs/session-as-built/4.0/4.0.5.1.1.md`, consumed `content/changelog/pending/*.md` deletions, and the roadmap row → COMPLETE.
- Already on the branch (OW-1 commit, do not rewrite the contract): `docs/VERSION_4_0_PLAN.md` Phase 5 / `4.0.5.1` row, `docs/session-contracts/4.0/4.0.5.1.1.md`, `docs/session-contracts/4.0/INDEX.md`, and this plan at `docs/session-plans/4.0/4.0.5.1.1.md` after planning persist.

### Interfaces and contracts

- This session adds or changes no production export. `postConvexHttpDoor`, jump `postDoor`, elimination `postDoor`, and the four popover exports keep their current signatures.
- Layout-table cell (docs-owned) — path string `docs/contributing/end-to-end-testing.md` in the existing row whose What-column is the tiny Playwright smoke-suite policy.
- Ledger rows AF-010 / AF-011 / AF-012 — `Remediation` stays `4.0.5.1`; `Status` becomes `Delivered`.
- `APP_VERSION` — string literal `'4.0.5.1'`.
- Changelog heading — `### v4.0.5.1 — YYYY-MM-DD` (close-out ship date; the only legal form) and the pinned plain-language Fixed bullet under Plan constraints, plus whatever `#### Added` / `#### Changed` / `#### Fixed` / `#### Removed` groups the pending-fragment fold adds.

### Control and data flow

No runtime data flow changes. Jump and elimination still go slice `postDoor` → `postConvexHttpDoor` → Convex HTTP. The only observable change is `check-doc-refs` going silent on `docs/ux-check/README.md`.

### Edge and failure behavior

- `PointerPopover` reappears in `src/` → delete the export in OW-1; do not add a consumer.
- Fallow reports `dup:50ee3d46` again → pause and talk with the operator; do not add an accepted-clone waiver and do not fold `postProjection` / `purgeMapChain` in.
- Retargeting the one cell does not clear the warning → pause; do not change `check-doc-refs`.
- `postProjection` or `purgeMapChain` still own their own POST+auth+JSON → leave them; they are outside AF-011 (HC-1).
- Shared-door or popover proof fails for any other reason → finish the named end-state in OW-1; do not widen into Watch or engine/mapper surfaces.

### Ordered work

1. **Prove AF-010 and AF-011; retarget AF-012.** On `src/components/ui/popover.tsx`, `src/lib/convex-http-door.ts`, `src/composition/jump-resolver/convex-door.ts`, `src/composition/signature-elimination/convex-door.ts`, and `docs/ux-check/README.md`, keep the first four proof-only unless a named end-state is false, and change the layout cell from `../contributing/end-to-end-testing.md` to `docs/contributing/end-to-end-testing.md`. Include the already-written remediation persist in this commit. Prove with SC-1, SC-2, and SC-3.
2. **Mark the findings Delivered.** On `docs/version-audits/4.0/PLAN.md`, set AF-010, AF-011, and AF-012 to `Delivered`. Do not write the changelog or `APP_VERSION` — planned final close-out owns those. Prove with SC-4, SC-5, and SC-7.

## Success criteria (agent-runnable — show the output)

- **SC-1 — Contract DC-1 / AC-1 / V-1.** `PointerPopover` is absent and the popover primitive exports only consumed symbols.

  | Proof | Evidence action | Required observable |
  | --- | --- | --- |
  | `SC-1.1` | `rg -n "PointerPopover" src` | No matches |
  | `SC-1.2` | `rg -n "^export " src/components/ui/popover.tsx` | Exactly four hits: `PopoverTone`, `Popover`, `PopoverHeading`, `PopoverRow`; no `PointerPopover` |
  | `SC-1.3` | `rg -n "from '@/components/ui/popover'" src` | `Popover`, `PopoverHeading`, and `PopoverRow` each appear in at least one import. `PopoverTone` need not be imported by name — it is the public `tone` prop type on `Popover`, not a leftover unused export |

- **SC-2 — Contract DC-2 / AC-2 / V-2 / HC-2 / HC-4.** Shared door owns transport; slices do not; Fallow reports no `dup:50ee3d46`.

  | Proof | Evidence action | Required observable |
  | --- | --- | --- |
  | `SC-2.1` | `rg -n -e postConvexHttpDoor -e fetchWithTimeout -e safeParse -e CONVEX_SERVICE_SECRET src/composition/jump-resolver/convex-door.ts src/composition/signature-elimination/convex-door.ts` | Each file references `postConvexHttpDoor` and does not reference `fetchWithTimeout`, `safeParse`, or `CONVEX_SERVICE_SECRET` |
  | `SC-2.2` | `pnpm exec fallow dupes --fail-on-issues` | Output contains `No code duplication found` and does not contain `50ee3d46` |
  | `SC-2.3` | `pnpm test src/lib/convex-http-door.test.ts` | Suite green; cases cover bearer JSON POST plus closed failure for unset config, transport error, non-OK status, invalid JSON, and schema failure |

- **SC-3 — Contract DC-3 / AC-3 / V-3 / HC-3.** The layout cell uses the live repo-root path and the checker is warning-free on that line.

  | Proof | Evidence action | Required observable |
  | --- | --- | --- |
  | `SC-3.1` | `rg -n "end-to-end-testing" docs/ux-check/README.md` | The matching line contains `docs/contributing/end-to-end-testing.md` and does not contain `../contributing` |
  | `SC-3.2` | `python3 tools/cli.py policy check-doc-refs --check` | JSON `warnings` does not include `docs/ux-check/README.md` |

- **SC-4 — Contract DC-4 / AC-4 / V-3.** The AF-012 ledger diagnosis names archive-reference semantics.

  | Proof | Evidence action | Required observable |
  | --- | --- | --- |
  | `SC-4.1` | `rg -n "AF-012" docs/version-audits/4.0/PLAN.md` | The findings-table row names the checker's `../` archive-reference rule (or equivalent archive-reference wording) and does not claim the target file was deleted |

- **SC-5 — Contract DC-5 / AC-5.** The delivering PR marks the three findings Delivered.

  | Proof | Evidence action | Required observable |
  | --- | --- | --- |
  | `SC-5.1` | `rg -n -e AF-010 -e AF-011 -e AF-012 docs/version-audits/4.0/PLAN.md` | Each of the three findings-table rows has Status `Delivered` and Remediation `4.0.5.1` |

- **SC-6 — Contract IS-4 (close-out).** The sub-version changelog and application version record `4.0.5.1`.

  | Proof | Evidence action | Required observable |
  | --- | --- | --- |
  | `SC-6.1` | `rg -n "^### v4.0.5.1 — " content/changelog/v4.0.md` | Exactly one dated heading |
  | `SC-6.2` | Inspect the complete bounded `### v4.0.5.1 —` section through the next `### ` heading or end of file | The section includes `#### Fixed` and a plain-language bullet that the ux-check workspace guide now names the live end-to-end testing policy, with no bold, inline code, or links in that bullet. Folded pending groups may appear in the same section |
  | `SC-6.3` | `rg -n "APP_VERSION" src/config/app-version.ts` | `export const APP_VERSION = '4.0.5.1'` |

- **SC-7 — Contract V-4 / HC-1.** Standing definition of done is green and the diff stays inside the mapped findings.

  | Proof | Evidence action | Required observable |
  | --- | --- | --- |
  | `SC-7.1` | `FALLOW_AUDIT_BASE=$(git rev-parse origin/main) pnpm verify` | Green; no failing suite |
  | `SC-7.2` | `git diff --name-only origin/main` | After OW-2 and before close-out delivery records, the name list is exactly these six paths: `docs/VERSION_4_0_PLAN.md`, `docs/session-contracts/4.0/4.0.5.1.1.md`, `docs/session-contracts/4.0/INDEX.md`, `docs/session-plans/4.0/4.0.5.1.1.md`, `docs/ux-check/README.md`, `docs/version-audits/4.0/PLAN.md`. Close-out later adds the changelog, `APP_VERSION`, as-built, pending-fragment deletions, and roadmap status |

## End of session

- Confirm every `DONE =` item is evidenced and every `hard_constraints` boundary held.
- **Delivery:** push the branch after OW commits and open the one sub-version PR to `main` via planned final close-out (this is the sole session of 4.0.5.1). Close-out syncs `origin/main`, folds pending changelog fragments, prepends `### v4.0.5.1 — YYYY-MM-DD`, bumps `APP_VERSION` to `4.0.5.1`, writes the as-built, marks the roadmap row COMPLETE, and merges with production proof.
- **Lifecycle artifacts:** plan `Execution status` → Complete (close-out); roadmap row 4.0.5.1 → COMPLETE (close-out); changelog entry + version bump (close-out); session as-built at `docs/session-as-built/4.0/4.0.5.1.1.md` (close-out); ledger AF-010/011/012 Delivered (OW-2).
- **Handoff:** after merge, rerun the resolver — expected directive is a full version-close audit against current `main` (`plan-version-audit` or `version-audit` as the resolver names).
