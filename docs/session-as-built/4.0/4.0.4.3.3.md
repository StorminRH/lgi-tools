# Session 4.0.4.3.3 As-Built — Signature viewer, site-card hosting, and observation proof

**Record status:** Final
**Recorded:** 2026-08-11
**Contract:** `docs/session-contracts/4.0/4.0.4.3.3.md`
**Contract digest:** `sha256:9561d46d919cf4015ef375971bd1a103391ed461fcdc5a7686744084c104ae9b`
**Plan:** `docs/session-plans/4.0/4.0.4.3.3.md`
**Plan digest:** `sha256:fa091bef6cb6ca80e91d4ab5a47dc2430d02f6a16ed70de1cf4d22963d8175b0`
**Branch:** `lifecycle/4.0.4.3`
**PR:** `#406`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Named combat and harvestable scanner rows open the fully expanded sites card in the shared scanner-anchored panel (bracket, leader, Escape/outside dismiss, read-only for viewers); `/sites/[id]` uses the same standalone card in a `max-w-[32rem]` column; harvestable rows flash live Est. ISK from an hourly priced scanner catalogue while combat stays on static blue-loot; the four 4.0.4.3.2 corrections land; and the observation collection is proven end-to-end across confirmed, human, assumed, and jump-verified tiers with the privacy boundary intact. Ranking graduation and provenance chrome were deliberately not built.

## Divergences from plan

- **Plan statement:** Contract HC-2 — "Inferred values never enter the observation stream."
  **Built instead:** Deduced fills continue to log at the `assumed` tier (D-B / migration `0054`); no code change this session beyond recording the reconciliation.
  **Why:** Operator ruling D-B (2026-08-10, 4.0.4.3.2) superseded HC-2 before this session planned.
  **Authority:** Operator: ruling D-B (2026-08-10); plan hard-constraint reconciliation.
- **Plan statement:** Contract IS-1 / PD-1–PD-3 — merge-profile ranking graduation and operator-facing provenance presentation.
  **Built instead:** Pick list remains bootstrap statics-first (`wormholeTypeSearch` untouched); provenance tiers stay database-only with no user-facing chrome.
  **Why:** Zero observation volume yet; chrome would clutter without informing ranking.
  **Authority:** Operator: PD-2/PD-3 direction during planning (2026-08-11).
- **Plan statement:** Contract OOS-2 — no changes to parser, lifecycle, solver, or collapse pathway.
  **Built instead:** Sole exception: `linkKnowledgePatch` carries human `lifeStage`/`lifeStageObservedAt` onto the resolved row when the target lacks its own (timestamped Unset wins both directions).
  **Why:** Operator-directed 4.0.4.3.2 review correction required for honest stub→resolved linking.
  **Authority:** Operator: directed corrections list in the approved plan.
- **Plan statement:** Stale human-retype race against elimination — settleIdentity last-write-wins.
  **Built instead:** Recorded as a known limitation; corpus heals on the next elimination pass.
  **Why:** All input paths converge on the same identity; a landed-first write is acceptable.
  **Authority:** Operator: accepted known limitation during planning.
- **Plan statement:** Owned surfaces for OW-2/OW-3 — standalone card and scanner panel hosting without naming scanner live Est. ISK or a priced catalogue rework.
  **Built instead:** G-1 added scanner live Est. ISK (`ScannerLivePrices`, harvestable flash), `getScannerSiteIndex` (hourly priced) isolated from lightweight `getSiteSearchIndex`, and `SiteCatalogueProvider` as the reactive atlas seed; site viewer measure `md:w-[22rem]` / detail `max-w-[32rem]`.
  **Why:** Operator G-1 review required live Est. ISK alignment with the card and retuned measures.
  **Authority:** Operator: G-1 disposition (2026-08-11).

## Final surfaces

- `src/mapper/signatures/ScannerAnchoredPanel.tsx` + `ActiveScannerPanel.tsx` / `ActiveSiteViewer.tsx` — shared panel chrome; connection-or-site `panelTarget`.
- `src/mapper/signatures/scanner-row-open.ts` — catalogue-matched site affordance; injectable `resolveSiteId`.
- `src/features/wormhole-sites/site-catalogue.tsx` — reactive `SiteCatalogueProvider` / `useSiteCatalogue` for first-paint atlas lookups.
- `src/features/wormhole-sites/queries.ts` — `getSiteSearchIndex` (deploy-static `max`) vs `getScannerSiteIndex` (hourly priced + live recipes).
- `src/features/wormhole-sites/components/SiteCard.tsx` — `presentation: 'catalogue' | 'standalone'`.
- `src/features/wormhole-sites/components/ScannerLivePrices.tsx` — harvestable live Est. ISK; combat static.
- `src/app/(map)/layout.tsx` — seeds `SiteCatalogueProvider` with `getScannerSiteIndex`.
- `convex/mapScan.ts` — `linkKnowledgePatch` lifeStage carry.
- `docs/ux-check/probes/atlas-signature-viewer.mjs` + `sites-standalone-detail.mjs` — G-1 probes.

## Discovered work

None.

## Successor notes

- Do not re-couple AppHeader/sitemap to `getScannerSiteIndex`; keep the lightweight/max path for those callers.
- Mapper imports scanner live prices and `useSiteCatalogue` only through `@/features/wormhole-sites/widget`.
- Interactive scanner rows must keep the sr-only action prefix — never an overriding `aria-label`.
- Ranking that later consumes `wh_observations` must include `assumed` rows and must not resurrect the retired edge card.
- Stale human-retype vs elimination remains an accepted heal-on-next-pass limitation.

## Verification summary

- **SC-1:** `Passed` — `wormhole-type-search` 7/7 green; `git diff origin/main -- wormhole-type-search.ts use-wormhole-editor-data.ts` empty (SC-1.1–1.2).
- **SC-2:** `Passed` — `convex/mapScan` protected-outcome cases green; `rg typeProvenance|destinationProvenance --glob '*.tsx' src/mapper` no matches (SC-2.1–2.2).
- **SC-3:** `Passed` — jump-resolver asserts `confirmed` and `human`; signature-elimination asserts `assumed`; mapJump stores `destinationProvenance: 'jump-verified'`; wh-observations correction-in-place green (SC-3.1–3.5).
- **SC-4:** `Passed` — wh-observations K162/hour-coarse guards green; schema remains five privacy-safe columns (SC-4.1–4.2).
- **SC-5:** `Passed` — signatures + wormhole-sites suites green (affordance, standalone card, drift test); UX probes assert open/bracket/dismiss (SC-5.1–5.3).
- **SC-6:** `Passed` — mapScan lifeStage carry; MapWindow narrow-stack max-height cap; lifecycle probe re-stamps before identical re-paste; automatic-jump probe asserts `[data-chain-node-static-stub]` readouts (SC-6.1–6.4).
- **SC-7:** `Passed` — ux-check report.json `/atlas` + `/sites/49` desktop/mobile clean; SCRATCHPAD G-1 disposition (2026-08-11); `FALLOW_AUDIT_BASE=$(git rev-parse origin/main) pnpm verify` exit 0 at `6298ada9` (562 files / 5129 tests; Fallow dead code 0 / complexity 0) (SC-7.1–7.3).
- **Adversarial review:** Subject: Diff mode, base `a1f2dfda`, head `2a7ce842` then correction patch through `6298ada9`; Roles: holistic, ownership, interface (two rounds); Runtime identity: requested=inherit, observed=Not observable / GPT-5.6 Sol / Fable 5 (round 1 holistic); Verdict: PASS; Disposition: accepted 2 MAJOR (reactive SiteCatalogueProvider; getSiteSearchIndex/getScannerSiteIndex split) + 3 MINOR (sr-only row action prefix; SCRATCHPAD notes; SiteSearchEntry comment) fixed on-branch; rejected none.
