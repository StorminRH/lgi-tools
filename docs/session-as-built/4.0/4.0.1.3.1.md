# Session 4.0.1.3.1 As-Built — Client universe assets, wormhole codex and the first widget export

**Record status:** Final
**Recorded:** 2026-07-28
**Contract:** `docs/session-contracts/4.0/4.0.1.3.1.md`
**Contract digest:** `sha256:b5084721d21aab1632bb9e69bef7e4d9ada7b9999f9290bbbc0cf218905726d6`
**Plan:** `docs/session-plans/4.0/4.0.1.3.1.md`
**Plan digest:** `sha256:21054c52cfd8440f17feab462f2b1e21bd6f2d99de1588c52c2874bc584cbdd1`
**Branch:** `lifecycle/4.0.1.3`
**PR:** `#318`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Browser sessions can now load versioned system, stargate, and wormhole reference
data through immutable routes, and features have their first reusable,
container-filling wormhole-site widget under a recorded export convention.

## Divergences from plan

- Plan hard constraint K162-only dogma absence: generation also excludes the
  exact SDE placeholders `QA Wormhole A` (32894) and `QA Wormhole B` (32895),
  while still failing on every other unexpected group-988 name or dogma gap,
  because the live SDE proved those two non-production rows exist. Authority:
  operator direction.
- The published v4.0 heading changed from `The Living Map` to
  `Atlas of Worlds` as a session rider. The frozen master-plan title remains
  unchanged. Authority: operator direction.

## Final surfaces

- `src/data/eve-data/universe-assets.ts` — derives and caches the versioned
  system directory, reciprocal adjacency graph, and wormhole codex.
- `src/data/eve-data/universe-assets-client.ts` — exposes retrying,
  browser-session-memoized lookup loaders for all three assets.
- `src/data/eve-data/api-contract.ts` — owns the universe-asset wire schemas,
  endpoints, and shared cache policy.
- `src/app/api/universe/assets/` — serves the no-store manifest and immutable
  version-addressed assets.
- `src/features/wormhole-sites/widget.tsx` — exports the D15 site-card widget
  and its container-filling props contract.
- `docs/architecture-boundaries.md` — records `widget.tsx` as the feature
  embeddable public-surface convention.

## Discovered work

None.

## Successor notes

- The two named QA rows are exact exceptions, not a general QA-name filter;
  any new unexpected wormhole type must still stop generation.
- `K162` deliberately remains a dogma-less far-side entry with no invented
  destination attributes.
- Later map hosts should import a feature's `widget.tsx` surface and constrain
  it through the container box rather than passing pixel dimensions.

## Verification summary

- SC-1: Builder and real-Postgres asset suites passed directory projection,
  version stamping, reciprocal sorted adjacency, and gateless-system behavior.
- SC-2: Loader tests passed one-fetch memoization, lookup fallbacks,
  failure reset, and one-manifest-refetch retry behavior.
- SC-3: Route tests passed manifest no-store/503 and versioned immutable
  200/stale-404 behavior.
- SC-4: Codex suites passed all six attributes, shared loader behavior,
  unpublished K162, exact QA exclusions, and remaining fail-loud cases.
- SC-5: The live report passed SDE version 3444265, Jita/Perimeter reciprocity,
  class counts, B274, regeneration-bearing A009, K162, and raw/gzip sizes.
- SC-6: Dataset census passed all three asset declarations, and the widget
  convention is recorded at its required path.
- SC-7: Source inspection confirmed the widget composes the existing
  `SiteCard` and introduces no second renderer.
- SC-8: The final diff contains no existing sites-page or wormhole-site file
  changes; desktop and mobile `/sites` and `/sites/3` sweeps passed.
- SC-9: Desktop and mobile fixed-container widget captures passed with no
  browser errors and received operator approval before PR creation.
- SC-10: The origin/main-pinned `pnpm verify` checkpoint passed typecheck,
  zero-warning lint, 4,365 tests, coverage, and Fallow on 35 changed files.
