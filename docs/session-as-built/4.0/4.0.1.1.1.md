# Session 4.0.1.1.1 As-Built — Map access foundation and corp membership resolution

**Record status:** Final
**Recorded:** 2026-07-27
**Contract:** `docs/session-contracts/4.0/4.0.1.1.1.md`
**Contract digest:** `sha256:f2225715fe9490b0b90ff16213932ff40cc7b4f672b4400ca728d4de881842ef`
**Plan:** `docs/session-plans/4.0/4.0.1.1.1.md`
**Plan digest:** `sha256:7b884674793d1895708d9dfb8ef3bf47feaa8e596dd77f1ce20bd8f25af23edb`
**Branch:** `lifecycle/4.0.1.1`
**PR:** `#316`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Maps now have durable ownership and delegated access in Neon, while one composed
gate resolves owner, direct-character, and refreshed-corporation authority into
the role capabilities later map sessions can consume.

## Divergences from plan

- The operator authorized the recently updated adversarial-review workflow to
  ride with this session's pull request. The rider was reviewed together with
  the planned implementation; its accepted findings were corrected and covered
  by focused fail-closed runner tests. Authority: operator direction.

## Final surfaces

- `src/data/maps/schema.ts` — owns durable map identity, grantee shape, and role
  storage.
- `src/data/maps/access.ts` — owns role precedence and the authoritative
  role-to-capability record.
- `src/composition/map-access.ts` — exposes the sole composed
  membership-aware `getMapAccess` gate.
- `src/data/maps/purge.ts` — removes character grants and user-owned maps at the
  credential purge tier.
- `docs/workflows/adversarial-review.md` — owns the four-seat Cursor review,
  receipt, evidence, and fail-closed execution contract carried as the approved
  rider.

## Discovered work

None.

## Successor notes

- Later map behavior must consume `getMapAccess`; it must not recreate access
  resolution in routes, features, or Convex.
- Corporation grants use only refreshed affiliation records, while a direct
  character grant remains independent of affiliation freshness.

## Verification summary

- SC-1: Migration `0051_tired_talkback` applied locally and the reflected-schema
  suite passed for both tables, keys, foreign keys, and unique grantee identity.
- SC-2: The composed access suite passed owner, editor, viewer, unrelated-user,
  stale-corporation, and direct-character cases; source search found no second
  access-check path.
- SC-3: The pure role suite passed character, corporation, empty-principal,
  precedence, and deny cases.
- SC-4: The disposable-schema role-extension proof and capability-record test
  both passed without rewriting existing rows or introducing rank logic.
- SC-5: The existing affiliation suite passed its fresh-cache and stale-refresh
  gates.
- SC-6: Dataset declaration and registry suites passed with the 3,600-second
  affiliation cache contract and both map tables registered.
- SC-7: Real-Postgres purge, account-deletion, owner-transfer, and orchestrator
  suites passed for direct grants, corporation grants, owned maps, and
  credential-tier teardown.
- SC-8: Fallow reported zero issues and the final diff contains no `convex/`
  changes.
- SC-9: The pinned `pnpm verify` checkpoint passed typecheck, zero-warning lint,
  4,326 tests with one skip, coverage, and Fallow.
