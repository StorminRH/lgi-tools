# Session 4.0.4.1.2 As-Built — Connection intelligence and collapse

**Record status:** Final
**Recorded:** 2026-08-04
**Contract:** `docs/session-contracts/4.0/4.0.4.1.2.md`
**Contract digest:** `sha256:df568edeb7b36a040bb6fabba15c0fbf085749417b28af6252732cd1e4ff5d64`
**Plan:** `docs/session-plans/4.0/4.0.4.1.2.md`
**Plan digest:** `sha256:89c9517901613414d2b5d0047a450e5a069de0a0d1c4ccb75946b5ce5987cd27`
**Branch:** `lifecycle/4.0.4.1`
**PR:** `#353`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

A typed connection now explains itself: picking a wormhole type fills the card
from the codex (mass, per-jump limit, size, lifetime) and locks what the type
decides; life-stage reports narrow one shared death window every client counts
down from; mass shows as an honest clamped range. Severing runs one computed
collapse decision — dead empty branches remove undoably, branches with k-space
exits or a loop home are retained marked severed — with a keyed Undo toast, a
newest-first map event ledger with editor-gated Restore, and a 15-minute purge
cron cleaning expired remnants. OW7 polish landed the dying-edge pulse,
viewport-bounded follower cards, and the frosted map chrome.

## Divergences from plan

None.

## Final surfaces

- `convex/mapAuthoring.ts` — gated authoring mutations; the sole
  destructive/restorative surface (`severConnection` /
  `restoreSeveredBranch` / `restoreConnection`), death-window validation,
  branch restore endpoint guard.
- `convex/lib/mapChainCleanup.ts` + `convex/crons.ts` — 15-minute chain
  tombstone/event purge (idempotency-registry entry).
- `src/data/maps/connection-lifetime.ts` — lifetime windows;
  `deathWindowFrom` is the single stored-pair→window owner.
- `src/data/maps/chain-collapse.ts` — pure collapse decision, one production
  caller.
- `src/data/maps/chain-events.ts` — event vocabulary/copy contract; schema
  validator derives from it.
- `src/mapper/authoring/` — connection card, codex intelligence, sever toast,
  home prompt, add-from-node menu.
- `src/mapper/log/MapEventLog.tsx` — despawn ledger with `canEdit`-gated
  Restore.
- `src/data/convex/use-mutation.ts` — optimistic-mutation door for mapper
  clients.

## Discovered work

None cut. Thirteen adversarial-review findings (three seats) were all absorbed
in close-out, including the branch-restore endpoint guard and the shared
death-window owner.

## Successor notes

- `restoreSeveredBranch` refuses `ENDPOINT_TOMBSTONED` when a later sever
  tombstoned a cut endpoint outside the shared-stamp set; without it a live
  connection with a dead endpoint wedges `severConnection` permanently after
  the purge cron. Keep the guard when 4.0.4.3 adds new collapse triggers.
- `mapConnections.eolAt` is vestigial (always null); lifetime truth is
  `deathEarliestAt`/`deathLatestAt`. Do not build ceiling-expiry on `eolAt`.
  The lifetime supersessions (no separate mark-EOL action; the persisted
  death-window pair) are recorded inside the approved plan itself, so they are
  not plan divergences.
- `decideCollapse` hardcodes `pilotsPresent: 'unknown'` until 4.0.4.2
  tracking exists; pilot-based retention is not yet reachable.
- `mapEvents.actor` display-name retention deliberately rides the ledger's
  7-day self-expiry, recorded in `docs/CONVEX.md`, not the `/n` teardown door.
- Never wrap a `<label>` around a Base UI Select (caption clicks spring the
  dropdown); `connection-fields.tsx` uses `<div>` + `ariaLabel`.

## Verification summary

- **SC-1:** `Passed` — G-1 two-client codex render accepted by operator
  (SC-1.1); `src/mapper/authoring` HC-1 tests green: typed connection renders
  no editable codex-owned control, K162 keeps the editable size select
  (SC-1.2).
- **SC-2:** `Passed` — connection-lifetime tests prove the 16h worked example,
  same-bucket narrowing, and contradiction reset (SC-2.1);
  `convex/mapAuthoring.test.ts` proves server narrowing/reset acceptance,
  malformed-pair refusal, and `lifeStageObservedAt` stamping with true no-op
  skip (SC-2.2); two-client narrowing accepted at G-1 (SC-2.3).
- **SC-3:** `Passed` — wormhole-contract tests pin threshold×variance bounds
  per shake state and null for regenerating holes (SC-3.1); mass-row tests
  render the interval as a variance-framed range, never a certain number
  (SC-3.2).
- **SC-4:** `Passed` — remove-outcome sever then restore round-trips `_id` and
  `_creationTime` identity via the server-derived shared stamp (SC-4.1);
  two-client sever/toast/ledger restore and dying-edge restore accepted at G-1
  (SC-4.2–4.4); equal re-pick returns the field to its prior value with the
  death window intentionally kept narrowed (SC-4.5).
- **SC-5:** `Passed` — collapse fixture matrix covers loop/k-space/pilot
  retention, wormhole-only removal with exact system and connection sets, and
  island-scoped evaluation (SC-5.1); the inspection test pins
  `decideCollapse`'s single production caller and the closed destructive
  surface (SC-5.2); retained-island positions are unchanged across
  dying→skeleton expiry with skeleton edges never rendered (SC-5.3); cleanup
  tests prove live-endpoint ties leave the purge range, re-armed dangling ties
  and expired events purge, and the cron is registered (SC-5.4).
- **SC-6:** `Passed` — ux-check captures and six authoring probes recorded
  clean with no console/page/network errors including the refused-mutation
  rider (SC-6.1); operator G-1 review completed with ACCEPT recorded before
  the PR opened (SC-6.2).
- **SC-7:** `Passed` — `FALLOW_AUDIT_BASE=$(git rev-parse origin/main)
  pnpm verify` exit 0 on the finalized session head (SC-7.1).
- **Adversarial review:** Subject: working tree at base 87eca0dae0ce8bdb7badfb1824e463a94ad98539 with patch sha256:1874dd473096a10d29ba1ec29a6a355b9e238be401975bd4a984d551ca8a9714 (134 files); Roles: holistic-reviewer, ownership-reviewer, interface-reviewer (operator-added); Runtime identity: requested=holistic fable and scoped opus per agent definitions, observed=Not observable; Verdict: CORRECTED; Disposition: 13 accepted root causes all fixed in-branch and re-verified by focused suites, 0 rejected.
