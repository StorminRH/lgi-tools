# Ordinary Work As-Built — Map purge claim and current projections

**Record status:** Final
**Recorded:** 2026-08-21
**Contract:** None.
**Contract digest:** None.
**Plan:** None.
**Plan digest:** None.
**Branch:** `development`
**PR:** `#11`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Once purge starts, the map stays archived. Restore and republish refuse it. Live access updates only when the projection is current. A stale teardown no longer counts as cleanup, so a revoked claim cannot come back from an older write.

- Changed: a map whose purge has started cannot be restored or republished
- Fixed: an older access projection cannot restore a revoked claim

## Divergences from plan

None.

## Final surfaces

- `src/composition/map-access-projection.ts` — empty-claims teardown fails when a newer projection already won
- `src/data/maps/lifecycle.ts` — exclusive purge claim blocks restore and republish
- `src/data/maps/schema.ts` — `purge_claimed_at` on maps
- `convex/mapAccessProjection.ts` — per-map projection watermarks

## Discovered work

None.

## Successor notes

- Dump CodeRabbit asked for JSDoc on capability, schema, and revision APIs. Left unfixed. This repo's no-comments rule wins.
- Dump nitpick to relax the exact import text in `src/app/api/same-origin-coverage.test.ts`. Left unfixed. The exact import is the gate.
- Dump nitpick to run `checkRateLimit` before `readJsonBody` on market refresh. Left unfixed. A malformed body stays 400.
- Greptile did not reply on dump PR 452. CodeRabbit did.
- `requireSoftFailLockClient` accepts `DATABASE_URL` or `DATABASE_URL_UNPOOLED`.

## Verification summary

- **Adversarial review:** Subject: frozen `f12bdf21ac9b12a1a817e6d38dea08a89d091c11`..`20432101` then corrected on later heads through `c99c6686728d645cbfbe6d5bc9078b2b79ec6f4a`; Roles: structure-reviewer, behavior-reviewer, thermos, no-comments; Runtime identity: requested=agent-file-pin, observed=`gpt-5.6-sol-medium` for the four review seats, `comment-sicko` launched as `gpt-5.6-sol-high` and no report returned; Verdict: `PASS`; Disposition: A1 stale Neon-to-Convex projections, A2 publish vs purge race, A3 stringly typed `indexedQuery`, and A4 positional registry helpers accepted and fixed.
