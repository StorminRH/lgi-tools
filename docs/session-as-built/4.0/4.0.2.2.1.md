# Session 4.0.2.2.1 As-Built — Chain schema and authorization gate

**Record status:** Final
**Recorded:** 2026-07-31
**Contract:** `docs/session-contracts/4.0/4.0.2.2.1.md`
**Contract digest:** `sha256:ab6b3111c1c2bec259b9c37843ed63f7cedb8783e67105f5cbc0d2ce04431392`
**Plan:** `docs/session-plans/4.0/4.0.2.2.1.md`
**Plan digest:** `sha256:7baea132cbb8910175dd3845cefe7707eb612c4168f784b9eaffb9fc534c16d4`
**Branch:** `lifecycle/4.0.2.2`
**PR:** `Deferred to 4.0.2.2.2`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Collaborative maps now have fine-grained, map-isolated Convex chain documents,
one gate-first fixture access path, reversible signature tombstones, and
payload-stable observation bookkeeping without a production consumer.

## Divergences from plan

- **Plan statement:** `readMap({ mapId, cursors })` performs four independent
  paginated collection reads in one function execution.
  **Built instead:** `readMapCollection({ mapId, collection, cursor })` gates
  and paginates exactly one selected collection per call, returning Convex's
  native page and continuation state.
  **Why:** Convex 1.42.3 permits only one paginated query per function execution,
  and convex-test 0.0.54 enforces that production limit.
  **Authority:** Operator: approved the collection-scoped replacement in-session
  without reopening the frozen plan.
- **Plan statement:** Session delivery stops after a local commit without a
  push or pull request.
  **Built instead:** The verified session commit was pushed and the shared
  sub-version pull request was opened as a draft, with review withheld.
  **Why:** The operator required a draft-PR pause for this run.
  **Authority:** Operator: directed the agent to pause after creating the draft
  pull request.

## Final surfaces

- `src/data/maps/access-contract.ts` — owns the role, precedence, and capability
  vocabulary shared by durable and collaborative authorization.
- `src/data/eve-data/wormhole-contract.ts` — owns canonical wormhole codes and
  jump-size classes shared by codex, wire, and chain boundaries.
- `convex/schema.ts` — owns map access, system, connection, signature, note, and
  signature-activity tables and indexes.
- `convex/lib/mapAccess.ts` — owns the sole collaborative map authorization gate.
- `convex/mapFixtures.ts` — owns collection-scoped reads, system upserts, and
  internal invariant fixtures without a production consumer.
- `convex/lib/mapEntityContracts.ts` and
  `convex/lib/mapSignatureCleanup.ts` — own entity validation, monotonic
  signature merge, and bounded expiry cleanup.

## Discovered work

None.

## Successor notes

- Production access claims remain absent until Session 4.0.2.2.2 projects
  durable authority; fixture APIs therefore fail closed outside controlled tests.
- Collection callers must paginate systems, connections, signatures, and notes
  independently because Convex allows one paginated query per function execution.

## Verification summary

- **SC-1:** `Passed` — schema/index queries covered all six tables, the focused
  maps and codex regressions passed, and repeated authorized system upserts
  returned one document ID with one stored row.
- **SC-2:** `Passed` — internal fixtures round-tripped canonical connection,
  signature, and typed note state; rejected invalid codes, endpoints, EOL,
  and cross-map targets; and preserved join-key-only payloads.
- **SC-3:** `Passed` — source-contract and behavior proofs established gate-first
  public handlers, signed-out/forbidden rejection, capability-union access,
  map isolation, and independent continuation through every collection.
- **SC-4:** `Passed` — sub-threshold activity no-opped, elapsed activity changed
  only its companion row, tombstone restore preserved payload identity,
  tombstoned observations no-opped, and 129/exact-128 cleanup cases proved the
  bounded continuation contract without companion lookups.
- **SC-5:** `Passed` — baseline and watch reporters returned no errors; AF-008
  remained below its trigger and the baseline was unchanged.
- **SC-6:** `Passed` — Convex codegen completed with backend typechecking,
  generated bindings include the map modules, policy checks and 408 tooling
  tests passed, and the final diff had no whitespace errors.
- **SC-7:** `Passed` — the origin/main-pinned `pnpm verify` checkpoint passed
  typecheck, zero-warning lint, 4,453 tests, coverage, and Fallow with zero issues.
- **Adversarial review:** Subject: Session 4.0.2.2.1 working-tree diff from `cfd5f0ef74b416f23897bfa682a833bfe790c81f`; Roles: holistic, reliability-reviewer, contract-reviewer; Runtime identity: requested=unspecified, observed=Not observable; Verdict: CORRECTED; Disposition: accepted canonical-ownership and proof gaps were corrected and verified; the proposed note-backed tombstone ban was rejected as unsupported scope expansion that conflicts with bounded cleanup.
