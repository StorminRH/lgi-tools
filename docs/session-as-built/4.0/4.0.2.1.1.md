# Session 4.0.2.1.1 As-Built — Map shell and host layer

**Record status:** Final
**Recorded:** 2026-07-28
**Contract:** `docs/session-contracts/4.0/4.0.2.1.1.md`
**Contract digest:** `sha256:769dee643a0fe200d7ac322107e7890d4ea0fd45c75bc8ae3324ef6ed909c082`
**Plan:** `docs/session-plans/4.0/4.0.2.1.1.md`
**Plan digest:** `sha256:90c74d9df12b2df42cd3bbb4e54ad89b761d24bfad4c6bd76f713ad7de915f0f`
**Branch:** `lifecycle/4.0.2.1`
**PR:** `#325`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Administrators can open a full-viewport empty atlas canvas behind a server-enforced development wall, with floating tool menu, reserved search slot, and account control composed above a new mapper host layer that features may never import.

## Divergences from plan

- Plan interface `AccountMenu({ session, anchor })` with a Base UI `MenuAnchor` function; built `anchorSelector?: string` defaulting to `.app-header` so a Server Component map chrome can pass `.map-chrome` without serializing a function across the RSC boundary. Authority: discovered evidence.
- Adversarial review found segment `notFound()` paths lost the root `metadata.title: 'Not found'` after the split; `(site)/not-found.tsx` now exports the same metadata. Authority: discovered evidence.

## Final surfaces

- `scripts/route-presence.mjs` — strips route-group segments when deriving classification keys.
- `src/components/composition/SiteFrame.tsx` / `NotFoundContent.tsx` — shared site chrome and 404 body.
- `src/app/(site)/` — existing routes under site chrome; group `error.tsx` and `not-found.tsx`.
- `src/app/(map)/` — admin-gated map frame, `/atlas` page, and map error boundary.
- `src/components/composition/map/` — floating MapChrome and MapMenu.
- `src/mapper/` — public MapCanvas surface and boundary probe.
- `.fallowrc.json`, `eslint.config.mjs`, `docs/architecture-boundaries.md`, architecture-map artifacts — mapper zone, allow list, and `@xyflow/react` rail.

## Discovered work

- Movable/hideable map chrome — recorded in `docs/backlog.md`.

## Successor notes

- `/atlas` stays out of the public nav registry until the version audit completes (D13).
- Camera pan bounds and fit behavior wait for real nodes in 4.0.3.1.
- The reserved centre slot stays empty until a later search control session.

## Verification evidence

- `pnpm assert:routes-present` → 88 keys.
- Focused vitest for route-presence, NotFoundContent, FeedbackButton, map layout/chrome, mapper canvas/boundary, architecture-map censuses.
- `FALLOW_AUDIT_BASE=$(git rev-parse origin/main) pnpm verify` on `2f2e25ae`.
- `pnpm ux-check` `/atlas` `/` `/sites` `/industry` `/changelog` plus `atlas-wall` probe; operator G-1 approved.
