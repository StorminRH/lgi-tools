# Session 3.10.2.2.1 As-Built — Mutation order and same-origin enforcement

**Record status:** Final
**Recorded:** 2026-07-24
**Contract:** `docs/session-contracts/3.10/3.10.2.2.1.md`
**Contract digest:** `sha256:ca971dc3471c92f8acd41eea7165203e27c1bb7f0f50f02990f4ebae137a1fe3`
**Plan:** `docs/session-plans/3.10/3.10.2.2.1.md`
**Plan digest:** `sha256:035f029e48425581e3f5a2cc422f31ae42c05e154f4695e0564c21c0f994fa38`
**Branch:** `lifecycle/3.10.2.2`
**PR:** `#303`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Browser mutations now reject explicit Origin or Referer mismatches with a typed
RFC 9457 403 while preserving same-origin, missing-provenance, gate-order, cron,
and service behavior; every mutating route must declare its same-origin
classification.

## Divergences from plan

- SC-1 named an operator-run Neon query because production was assumed
  agent-unreadable; at the operator's direction, the authenticated Neon CLI ran
  the same query in an explicit read-only transaction and returned zero rows.

## Final surfaces

- `src/platform/auth/same-origin.ts` — owns provenance normalization, canonical-origin matching, mismatch telemetry, and the typed `cross_origin` decision.
- `src/app/api/mutation-route.ts` and the five direct guarded routes — own the established gate positions and map the failure through the existing problem response seams.
- The six affected `api-contract.ts` owners — declare `cross_origin` on all 15 guarded endpoints with closed response unions.
- `src/app/api/same-origin-coverage.test.ts` — classifies all mutating routes as pipeline, direct, or exempt-with-reason.
- `docs/security/disposition-register.md` and `docs/backlog.md` — close LGI-03 and retain only the triggered missing-provenance follow-up.

## Discovered work

None.

## Successor notes

- The reviewed observation point was after authorization on 21 of 22 guarded routes; zero rows therefore means no authenticated cross-origin mutation reached the observer during the window.
- Post-release `cross_origin_mutation` events mean rejected attempts. Missing-provenance requests remain deliberately allowed.
- The auth endpoint contract remains in the Convex value bundle; this session added response values but no import to that cross-runtime chain.

## Verification summary

- **SC-1:** The production Neon query ran under `BEGIN READ ONLY` for 2026-07-13 through 2026-07-24 and returned zero rows.
- **SC-2:** The pipeline owner contains the intended and live gate orders, CSRF rationale, missing-provenance decision, and provenance-policy deferral.
- **SC-3:** Same-origin, pipeline, preferences, feedback, and direct-admin mapper suites passed the three mapped 403 classes and same-origin success paths.
- **SC-4:** Preferences and feedback missing-provenance requests retained their successful behavior.
- **SC-5:** A seeded unclassified mutating route failed with its exact path; after removal, the 17 pipeline + 5 direct + 12 exempt inventory passed.
- **SC-6:** LGI-03 is closed in the disposition register, and only the missing-provenance policy remains in the backlog.
- **SC-7:** The watch-trigger checker returned no errors or warnings; AF-008 still matches exactly three files.
- **SC-8:** `FALLOW_AUDIT_BASE=b90f1138b6405233ceed38ff8e64cdf43a2ae252 pnpm verify` passed TypeScript, zero-warning ESLint, 3,739 tests with 95 skips, coverage, and zero Fallow issues across 27 changed files.
