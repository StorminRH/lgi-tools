# Session 3.10.2.1.1 As-Built — Typed API primitives and feedback slice

**Record status:** Final
**Recorded:** 2026-07-24
**Contract:** `docs/session-contracts/3.10/3.10.2.1.1.md`
**Contract digest:** `sha256:66c8ef13b4e415d7eac7cbe3ed59abdde1ee85a1b84d4bc8ac8d6312e858b69c`
**Plan:** `docs/session-plans/3.10/3.10.2.1.1.md`
**Plan digest:** `sha256:0fa3bce9d30490c90326f6d54e0c44f83754366f196143d11e57be9fe367abb4`
**Branch:** `lifecycle/3.10.2.1`
**PR:** `#299`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

LGI.tools now has one typed application-failure vocabulary, one safe RFC 9457 problem translation, endpoint-declared request and response contracts, closed client outcomes, and typed request-body and rate-limit cores; the feedback endpoint proves that chain end to end without changing its admission order, statuses, or visible copy.

## Divergences from plan

- **Close-out delivery metadata:** the resolver originally evaluated this session's frozen pre-restructure delivery unit in isolation and rejected its own PR number. The operator authorized a lifecycle repair; delivery is now resolved sub-version-wide so later per-session contracts preserve prior frozen prompts while making `PR: #299` valid.

## Final surfaces

- `src/lib/failure.ts` — owns the eight application-failure categories and constructors.
- `src/lib/problem.ts` — owns safe problem bodies, status mapping, correlation ids, and HTTP serialization.
- `src/transport/endpoint.ts` — owns endpoint request schemas, response codecs, literal statuses, and derived outcome types.
- `src/transport/api-client.ts` — executes v2 contracts into declared API, protocol, or network outcomes while preserving the v1 path.
- `src/transport/api-response.ts` — binds route responses to one endpoint's declared status and codec.
- `src/transport/route-body.ts` and `src/lib/rate-limit.ts` — expose typed failure cores beneath byte-equivalent legacy wrappers.
- `src/features/feedback/api-contract.ts` and `src/app/api/feedback/route.ts` — define and serve the representative 204/400/429/502/503 contract.
- `src/features/feedback/components/feedback-view.ts` — owns the existing visible error-copy mapping for typed feedback outcomes.
- `.agent-local/resolve_development_state.py` and the canonical contract, as-built, and close-out procedures — treat a per-session delivery declaration as sub-version-wide.

## Discovered work

- Session `3.10.2.1.2`: prevent GET endpoint contracts from declaring request bodies before migrating GET callers.
- Session `3.10.2.1.2`: revisit the executor's broad decode catch only if the route migration needs programmer errors distinguished from network failures.

## Successor notes

- Feedback's leading-slash path rule intentionally remains in the request schema before rate limiting; the post-sanitisation `path_invalid` check is a defense-in-depth backstop.
- The v1 and v2 endpoint paths deliberately coexist until session `3.10.2.1.3` deletes the old path.

## Verification summary

- **SC-1:** Failure and problem suites passed with all eight categories mapped to complete problem bodies.
- **SC-2:** The seeded unexpected-failure proof confirmed that causes and stack content do not serialize.
- **SC-3:** Transport suites passed across declared, protocol-drift, stream-failure, abort, and network outcomes.
- **SC-4:** `npx tsc --noEmit --incremental false` exited successfully with all negative type proofs active.
- **SC-5:** Feedback route tests passed across 400, 429, 502, 503, and bodyless 204 paths.
- **SC-6:** Request-body and rate-limit wrapper-equivalence coverage passed without changing unmigrated callers.
- **SC-7:** The import-aware API-contract classifier accepted owned helper calls and rejected seeded contractless, mixed-response, comment, string, unused-import, shadowed-helper, and wrong-module fixtures.
- **SC-8:** The branch changes only the approved production surfaces, their tests, and lifecycle records; feedback is the only route implementation changed.
- **SC-9:** The origin-main-pinned checkpoint passed typecheck, zero-warning lint, 3,515 tests with 95 environment-dependent skips, coverage, and Fallow across all 30 then-changed files. After review fixes, the required final `pnpm verify` passed typecheck, zero-warning lint, 3,615 tests with one skip, coverage, and Fallow across the current review delta.
- **Lifecycle repair:** All 48 resolver tests pass, and the live resolver accepts this record before selecting read-only planning for session `3.10.2.1.2`.
