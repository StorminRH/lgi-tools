# Session 3.10.4.1.1 As-Built — Universal primitive adoption and rails

**Record status:** Final
**Recorded:** 2026-07-26
**Contract:** `docs/session-contracts/3.10/3.10.4.1.1.md`
**Contract digest:** `sha256:0071f5f606104eb7f87f02e2f9ca098dbd82e22987693077e8ee61c5c52ccd37`
**Plan:** `docs/session-plans/3.10/3.10.4.1.1.md`
**Plan digest:** `sha256:2cb6c811544b295082e2703f0a5da2b5e68fb4da9bcebf3d26aef4731f9fa8d7`
**Branch:** `lifecycle/3.10.4.1`
**PR:** `#309`
**Record standard:** `docs/workflows/schema/session-as-built.md`

## Delivered outcome

Every A1 row in the approved Phase 4 survey now renders through its owning shared primitive
or feature tone map. The primitive layer covers the missing control and presentation shapes,
roughly 650 lines of page-scoped CSS and the one-shot audit script are gone, and a
registry-backed census plus exact lint rails make a newly hand-built bypass fail verification.

## Divergences from plan

- Contract `HC-1` and plan `DC-3` began with byte-for-byte look preservation as the default.
  During execution the operator made Base UI interaction behavior authoritative for migrated
  controls and accepted small visual differences, then approved the resulting local browser
  review on 2026-07-26. The implementation kept that behavior and corrected the specific
  sites, change-log, navigation-status, planner-adjuster, and active-job alignment regressions
  identified in review. Authority: operator direction.
- Plan step 9 expected roughly 70–90 files and named a split checkpoint. The coherent batch
  reached 112 files because the approved primitive tests, census fixtures, lifecycle artifacts,
  and exact exemption proofs landed with their owners. The operator explicitly directed that
  the work remain batched, so no multi-PR exception was taken. Authority: operator direction.

## Final surfaces

- `src/components/ui/` — owns the widened primitive vocabulary, including ChipToggle,
  StaticTable, Prose, and the new Dot, Stepper, Segmented, Card, SectionHeader, and
  SectionLabel capabilities.
- `src/composition/ui-adoption-registry.ts` — declares the primitive census, exact exemptions,
  and temporary CSS-family handoff for later Phase 4 slices.
- `eslint.config.mjs` — rejects production UI bypass syntax and grants each necessary native
  seam only to its exact owner file.
- `src/esi-datasets/ui-adoption.test.ts` and `scripts/ui-adoption-rail.test.mjs` — enforce the
  live registry, CSS-family census, seeded bypass failures, and neighboring-family red proofs.
- `docs/version-audits/3.10/PHASE_4_ADOPTION_SURVEY.md` — records every A1 row as Delivered and
  preserves the approved A2, backlog, and exemption dispositions for the re-audit.

## Discovered work

- A cleared planner build location is posted as a null preference even though the storage
  contract requires a value. Recorded in `docs/backlog.md` as an S-sized industry-planner item;
  not changed because it is a pre-existing data-write behavior outside this presentation slice.

## Successor notes

- `3.10.4.2` owns the temporary `.account-menu-*`, `.nav-tool*`/`.nav-menu*`, and
  `.content-browser-*` census families and must shrink that list as it refines those surfaces.
- Necessary raw element and semantic seams live in exact flat-config blocks, not source
  suppressions. Each block must continue to re-list every neighboring syntax rail because flat
  config replaces rule options rather than merging them.
- Base UI behavior is the adopted control contract. Later appearance work may refine presentation
  but must not restore the removed local interaction models.

## Verification summary

- `SC-1` — Primitive-focused tests passed, including ChipToggle, StaticTable, Prose, Dot,
  Stepper, Segmented, Card, Button, SectionHeader, and SectionLabel.
- `SC-2` — The survey delivery ledger marks every A1 row Delivered with file-level evidence and
  preserves every later-slice or exemption disposition.
- `SC-3` — The adoption census passed with retired CSS families absent and the declared
  keep/temporary families pinned.
- `SC-4` — The UI adoption rail suite passed every production and preview seed, every exact
  exemption-home green proof, and every neighboring-family red proof; lint reported zero warnings.
- `SC-5` — The registry-backed adoption census passed live and the superseded one-shot audit
  script is absent.
- `SC-6` — Preview source retains its prior field rails plus the new adoption rails, and
  `CONTRIBUTING.md` now names the enforcement that actually exists.
- `SC-7` — The desktop/mobile route sweep reported no console, page, request, or HTTP failures,
  and the operator approved the corrected local presentation before PR #309 opened.
- `SC-8` — `FALLOW_AUDIT_BASE=$(git rev-parse origin/main) pnpm verify` exited 0 on the finalized
  application head: 4,136 tests passed with 105 expected skips, coverage completed, and Fallow
  found zero issues across 112 changed files. PR #309 carries the reconciled 3.10.4.1 release
  state; current-head CI, review settlement, merge, and production proof remain owned by the
  continuing close-out after this immutable record is written.
