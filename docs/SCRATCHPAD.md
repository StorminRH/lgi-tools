# SCRATCHPAD — LGI.tools

> Agent session-to-session memory. Skimmable in ~1 min. Full upkeep procedure in `docs/workflows/close-out.md`.
> **Compacted 2026-07-11 at the 3.7→3.8 boundary — reset to a near-blank slate (operator-directed).** The entire pre-3.8 record — every shipped sub-version one-liner, the 3.3→3.7 arc durable-findings blocks, and the full ~250-line Standing-notes gotcha list — is preserved byte-for-byte in `SCRATCHPAD_3.7_pre-compaction.md` (Document Archive) and is one `grep` away when a past detail is needed. Nothing was lost — it's referenced, not inlined. (The earlier per-boundary compaction blockquotes, 3.3→3.7, live in that archive too.)

## Where durable knowledge lives — look here before the archive

- **Rules & invariants that apply every session** → the active agent guide:
  `CLAUDE.md` for Claude Code or `AGENTS.md` for Codex.
- **Per-topic gotchas + per-feature forensics + ship history (PR#s, prod)** → the agent memory files, indexed in `MEMORY.md` (recalled by relevance).
- **The exception-elimination ledger (zero waivers)** → `fallow-baselines/README.md`.
- **User-facing + internal ship history** → `content/changelog/` (one file per master version; every notable change since 3.7.36.1); full git history in the repo.
- **Everything pre-3.8 in full** (Standing-notes gotchas, 3.3→3.7 arc findings, the whole Shipped ledger) → `SCRATCHPAD_3.7_pre-compaction.md`, Document Archive.
- **Local dev bring-up** → `CLAUDE.md` / `AGENTS.md` (`pnpm dev:all` = Docker Postgres :5433 + Convex :3210 + Next :3000). Redis/Upstash is cloud-only, disabled in dev (the `[esi] KV… not set` warning is expected). Symptoms/quirks: [[lgi-local-dev-convex-auth]] · [[lgi-local-dev-macos-firewall]].

## Now

**CURRENT = v3.10 "Hull Integrity + SKIN". The architecture movement is shipped through sub-version/session `3.10.1.2` / `3.10.1.2.1`: cross-slice server orchestration now lives in `src/composition`, app-shell UI composition in `src/components/composition`, reusable auth/ESI/owner-sync/search/purge/page-settings capabilities in `src/platform`, and request mechanics in `src/transport`; configuration, online-status data, and executable scripts have explicit homes. The deny-by-default Fallow map has zero exceptions, dependency cycles are blocking, client code cannot import server-only roots, and `docs/architecture-boundaries.md` remains the single prose ownership map. The remaining Phase 2–3 movement is preserved in six operator-approved session contracts through `3.10.3.3`; `3.10.2.2` remains the only planned production behavior change and must ship alone after its observe-only telemetry gate. Phase 4 (presentation) stays deferred to its own survey-gated `plan-version` run. NEXT = re-enter through `start-session` for session `3.10.2.1.1` / sub-version `3.10.2.1`; the resolver owns the exact planning action.**

**⭐ Update-watch cloud-routine gotchas (3.9.3.5, memory-worthy):** the Claude cloud environment has **no `gh` CLI** — the collector lists issues via the GitHub REST API over `urllib` (optional `GH_TOKEN`/`GITHUB_TOKEN`); a missing `git`/`pnpm` binary is a named failure, never a crash. The cloud clone's origin remote is a **local git-proxy URL** (`http://local_proxy@127.0.0.1:<port>/git/<owner>/<repo>`) so the repo-identity parser accepts that shape. Cloud GitHub egress is **scoped to the routine's own repo**, so the EVE-docs source watches the rendered **docs sitemap** (`developers.eveonline.com/docs/sitemap.xml`), not the esi-docs GitHub atom feed (403). **"Run now" works while the routine is paused** (no resume/re-pause dance needed). The routine (`trig_01RZu9TrBC64AtJpS5XNKL4P`) stays **paused** at 3:07 AM EDT pending the operator's explicit unpause; the seeded `clsx` delta was absorbed to major 2, and demo issue #269 is closed. Advisory-cleanup of the five acknowledged dev-tooling advisories is a chosen follow-up (operator: "clean up later").

### 3.9.3.4.2 sample-mode gate — session-final readout

The counterbalanced sequence (`after-1`, `sample-1`, `sample-2`, `after-2`,
`after-3`, `sample-3`) completed on 2026-07-18 with all six runs healthy, the
normal catalogue fixed at 69 cards, the sample catalogue fixed at 20 cards, and
no cold request above 60 seconds.

| Metric | Normal mode | Sample mode |
| --- | ---: | ---: |
| Cold `/sites` median | 1.361 s | 1.091 s |
| Cold range | 1.353–1.386 s | 1.087–1.113 s |
| Warm `/sites` median | 519 ms | 241 ms |
| Warm range | 513–534 ms | 216–278 ms |

The sample cold median is 80.2% of normal, inside the required 110% ceiling,
and the historical watcher/swap stall did not recur. The authoritative raw
suite is the ignored local artifact
`docs/ux-check/profiles/sites-suite-20260719T025802308Z.json`; only this compact
readout is durable repository state.

### 3.9.3.4.1 current dev profile — session .2 "before" readout

The bounded profiler ran twice on 2026-07-18 with local Postgres reachable,
Next 16.2.6, zero starting swap, normal macOS memory pressure, and >9 GiB
free+inactive memory. The interrupt proof terminated the owned process group
with SIGTERM (no SIGKILL), then independently confirmed the group gone and port
3000 closed. Both full runs returned `status: ok`; the historical watcher/swap
melt was **not reproduced**.

| Metric | June 15 healthy proof | Session .2 before — as found | Session .2 before — clean `.next` |
| --- | ---: | ---: | ---: |
| `.next` at start | clean | 45.4 MiB | 0 B |
| Idle process-group RSS / CPU | 330 MiB / 0% | 480 MiB avg / 2.6% avg | 755 MiB avg / 13.6% avg |
| Cold `/sites` full-body time | ~1.7 s | 1.34 s | 1.32 s |
| Cold dev-log split | app 1.417 s | next 442 ms / proxy 2 ms / app 893 ms | next 414 ms / proxy 2 ms / app 896 ms |
| Warm `/sites` full-body time | 1.1–1.4 s | 526 ms / 497 ms | 511 ms / 498 ms |
| Warm `application-code` | ~1.1 s | 520 ms / 491 ms | 506 ms / 491 ms |
| Overlapped homepage probe | not recorded | passed, 558 ms full body | passed, 512 ms full body |
| Peak process-group RSS | 2.9 GiB | 2.19 GiB | 2.36 GiB |
| Swap growth | 0 | 0 | 0 |
| Detail-only sentinels in collapsed `/sites` HTML | not recorded | Wave Spawns 0 / No Sleeper presence 0 | Wave Spawns 0 / No Sleeper presence 0 |

Interpretation: `.next` hygiene is currently healthy and the old >60 s stall is
absent. Warm request cost is now ~0.5 s and remains almost entirely the
`application-code` bucket; the existing per-request live-price overlay's two
batched reads remain candidates only, not attributed causes and not `.1` fix
scope. The committed both-surfaces structural probe is the authoritative lazy
contract; sentinel counts are secondary evidence. Master-plan §3.9.3.4 still
states the superseded eager-render root cause and requires a later
operator-approved `plan-version` truth reconciliation; session `.2` planning
must reconcile its cross-references plan-only before implementing sample mode.

**⭐⭐ NEW durable gotcha (memory: [[convex-bundles-lib-esi-no-next-imports]]):** Convex bundles the ESI-gate chain (`convex/onlineStatusSync.ts` → `@/lib/esi` → dispatch → everything it imports) into its isolate. ANY `next/server` import in that chain fails ONLY the post-merge production Convex push (`__dirname` in Next's compiled ua-parser-js) — invisible to `pnpm verify`/CI. That is exactly how the first 3.9.2.2 deploy (`dpl_27dx4yFQKHCYRKNBfW4jCbeRSomL`) failed; production kept serving the prior Ready build throughout. Also: two Greptile P2s resolved (marker scheduling reverted-by-design with the constraint documented at the write site; affiliations `outcome:'refreshed'` label justified as preserved behavior — Greptile accepted). CI-only CRAP gotcha confirmed live: a function whose only test is a `*.db.test.ts` suite scores 0% coverage in Postgres-less CI (fix = add off-database unit coverage in the sibling unit suite).

**Historical trial only (superseded):** 3.9.2.10 was implemented by a headless `codex exec` GPT-5.6-sol agent. That trial informed the now-retired execution-delegation workflow; it is not current authorization to delegate implementation.

---

_Previous session (historical — full record in the shipped ledger + memory files):_

**3.8.2.3.1 Component System v1, Phase 2 (Part C / C2) — COMPLETE & SHIPPED.** PR #220, squash `775c78b`, Greptile 5/5 (2 findings: 1 fixed + 1 justified), prod `lgi-tools-jl47yyzet`. Overlays + controls consolidated: FeedbackModal → the existing Base UI `dialog.tsx` (deleted the second modal, `modal.tsx`); new `select.tsx` (Base UI Select) supersedes the C1 native `<select>` — 4 consumers migrated (RigSupply, BuildLocationSelector, ReactionStructureSelect, CustomStructureBuilder; `StructureOptgroups`→data `structure-options.ts`), the `<select>` lint exemption DELETED; `stepper.tsx` rebuilt on Base UI NumberField (deleted `stepper-math.ts` + test); menu/popover/select popups share one `panelSurface` (bg-deep/border-idle/shadow-dd) via new `dropdown-panel.ts`. `pnpm verify` green; ux-check + open-state probes clean. Full forensics → [[feature-3-8-2-3-1-component-system-c2]]. APP_VERSION→3.8.2.3.1.

- **⭐ Base UI Select needs the value→label `items` MAP on `Select.Root`, else the trigger shows the RAW value** (item labels register only when the popup mounts, so a freshly-loaded select would show its encoded value). Went data-driven: one `items` prop feeds both the popup rows AND the Root label-map (`labelMapOf`). Confirmed in source `internals/resolveValueLabel.js` (record-map form: `items[value] ?? fallback`) + live screenshots. Empty-string `""` is a real "none" option and resolves `items[""]` fine because the primitive passes NO `placeholder` (SelectValue then never takes the absence-of-selection branch) — Greptile P2, justified.
- **⭐ Base UI NumberField emits `onValueChange(null)` MID-EDIT when the field is cleared** (`number-field/input/NumberFieldInput.js:230-232` `setValue(null, inputClear)`), NOT only on blur — so coercing null→min in `onValueChange` repopulates the field before retype (Greptile P1). FIX = skip null in `onValueChange`, coerce null→min in `onValueCommitted` (blur/commit only; a valid public Root prop).
- **⭐ The global `:focus-visible` ring (`globals.css` — 2px green + 2px offset, unlayered a11y indicator) STACKS on the field's own `border-isk-sub`** → a doubled green border on every focused field / dropdown trigger. FIX = `.field-own-focus:focus-visible{outline:none}` (unlayered, higher specificity than the bare `:focus-visible`, so it wins) + a `field-own-focus` marker on `fieldVariants` + `innerControl`; ring stays on buttons/links. ⚠️ **programmatic `.focus()`/`.click()` does NOT trigger `:focus-visible`** — verify focus rings with KEYBOARD (Tab) probes, not `.focus()` (a programmatic probe falsely showed a single border first time).
- **Menus stay header-flush + full-width-row** (hard-constraint #3, no layout redesign): they take the `panelSurface` atom (bg/border/shadow) only, NOT the full `dropdownPanel` (its `rounded-card` + `p-[5px]` would break the flush header seam + the full-bleed rows). Item hover retinted row-hover→row-active. Popover green tone kept (semantic data-quality tint).
- **Search surface is Phase 3, DEFERRED (Ryan-confirmed).** The lab (`docs/lgi-component-kit-lab.html` Kit 4) intends search fields + dropdowns to share the bg-deep recessed surface, and cards to be `section` (one step lighter). Cards already match (`bg-section`); search (`.dropdown`/`.dd-*` in globals.css + `terminal-search.tsx` on `bg-bg`) is still on the old lighter surfaces → intended mismatch until Phase 3.

## Open carry-forwards

Live operator chores, not yet done (delete when done):
- **`DISCORD_ALERT_WEBHOOK_URL`** (Prod+Preview, dedicated ops channel, separate from feedback's `DISCORD_WEBHOOK_URL`) — drives the price-degradation alert. Unset = skipped silently, telemetry still records. Optional, not blocking.
- **Vercel Speed Insights** — paid on this plan, not enabled yet; `<SpeedInsights/>` stays wired but reports nowhere. No code change when/if turned on.

## Backlog (no version assigned)

Deferred/someday work lives in `docs/backlog.md` — grouped, un-prioritized. One home.

## Standing notes (durable gotchas)

_Reset at the 3.7→3.8 boundary._ Durable gotchas now live in their permanent homes — the active `CLAUDE.md` / `AGENTS.md` rules, agent memory (per-topic), and code comments. Add one here only if it's genuinely session-scoped and not yet homed; promote anything load-bearing to its permanent home rather than letting it live here. The full pre-3.8 gotcha list is in the archive.

## Shipped ledger

_Reset at the 3.7→3.8 boundary._ Per-sub-version ship history is in `content/changelog/` + git + the `feature-*.md` memory files; the complete pre-3.8 ledger is in `SCRATCHPAD_3.7_pre-compaction.md` (Document Archive). New 3.8 sub-versions get a one-liner here as they ship.

- **3.10.0.2** — Canonical contract, session-plan, code-health-baseline, and changelog-entry forms now own exact machine-enforced record shapes; the resolver validates stable IDs, contract-plan mappings, and pre-dispatch release consistency, while the drift gate enforces the strict baseline. The obsolete lifecycle narratives are retired. `pnpm verify` passed 3,553 tests plus one skip; fresh full-Postgres coverage passed all 3,554 tests at 86.90/84.23/82.84/87.90, and exact-`origin/main` coverage-backed Fallow was clean across 65 changed files. PR #276, squash `e9e9ba3`, reached current-head Greptile 5/5 after one prompt-preservation P1 was fixed and the deliberate fail-closed repair interlock was accepted; both threads resolved, CI and Semgrep green. Exact-SHA production `dpl_GtGAEEm6uEzVQNfqUEr8MBLwywC7` is Ready on `lgi.tools`; browser-first smoke confirmed the version, changelog, `/sites`, `/industry`, signed-out admin gate, and zero console warnings/errors. Deployment logs reproduced the already-backlogged non-blocking `/devlog` shared-cache 413 while the route returns 200; the known merge-helper resolved-thread defect required the evidence-preserving direct squash path. The attached rider batch then shipped package/update-watch hardening in PR #279 (`ee16f03`), the rider resolver and per-skill reconciliation ledger in PR #280 (`95a1fe2`), and Codegraph 1.4.1 graph-first tooling in PR #278 (`56d729f`) without another version bump; each rider passed fresh review and main CI, exact-deployment readiness, log inspection, and browser smoke.
- **3.10.0.1** — Generic document-reference checks now distinguish active guidance from historical/lifecycle records, while purpose-built validators continue to own record truth; stale agent, Convex, lint, worktree, and release map facts were corrected, v3.10 opened, and the shared Claude/Codex guidance shipped together at policy revision 27. `pnpm verify` passed 3,553 tests plus one skip; fresh full-Postgres coverage passed all 3,554 tests at 86.90/84.23/82.84/87.90, and exact-`origin/main` coverage-backed Fallow was clean. PR #275, squash `523009b`, reached current-head Greptile 5/5 with zero inline findings after the required large-PR bypass; CI and Semgrep were green. Exact-SHA production `lgi-tools-jqt9boqfl-stormins-projects.vercel.app` is Ready on `lgi.tools` at v3.10.0.1 with no error-level runtime logs; browser-first smoke confirmed the changelog, all 69 sites, the signed-out industry surface, the signed-out admin gate, and zero console warnings/errors.
- **3.9.5.2** — Real behavioral coverage now exercises the four AF-013 modules through their existing seams: skill saves, SDE stream/mappers, NPC station-name batching/failure tolerance, and queued skill-name collection. The four production modules are byte-unchanged; AF-013 is Delivered and the version-start-pinned Fallow gate is clean without a waiver, suppression, threshold, or baseline change. `pnpm verify` passed 3,553 tests plus one skip; fresh full-Postgres coverage passed all 3,554 tests at 86.90/84.25/82.84/87.90, with exact-`origin/main` and version-start Fallow clean. PR #274, squash `f35cdb3`, reached current-head Greptile 5/5 after one numeric-sort P2 was fixed and resolved; CI and Semgrep were green. Exact-SHA deployment `dpl_9Yz6WGi7SmWRq2dUiAQiKGXupHDJ` is Ready on `lgi.tools` at v3.9.5.2 with no error-level runtime logs; browser-first smoke confirmed the changelog, all 69 sites, the settled signed-out industry view, and zero console warnings/errors. One handled ESI scoreboard timeout appeared during static generation without a failed route or production error.
- **3.9.5.1** — The v3.9 cycle-one audit record now states the measured local-development stall cause, the README reserves production builds for CI/Vercel, and the constitution cites live exemplars; one lower-layer owner now defines the full 32–1024 EVE image-size vocabulary and snapping policy, while the saved-plan render verdict is private and distinctly named from its exported client controller. AF-010/011/012/014/015 are Delivered. `pnpm verify` passed 3,543 tests plus one skip; fresh full-Postgres coverage passed all 3,544 tests at 85.57/83.22/81.78/86.50 and origin/main-pinned Fallow was clean. PR #273, squash `d0eae4f`, reached current-head Greptile 5/5 with zero findings; CI and Semgrep were green. Exact-SHA production deployment `dpl_BsAddxW8a2gWrLjPKQsobF6GJ39G` is Ready on `lgi.tools` at v3.9.5.1 with no recent error-level logs; browser-first smoke confirmed all 69 sites, the settled signed-out industry view, and zero console warnings/errors.
- **3.9.4.1** — All twelve findings from an external security deep-research review were verified against live code and recorded in a new disposition register, with the eleven non-LGI-08 findings routed to the backlog and no behavior change; the one acted-on finding is a least-privilege runtime database role (`lgi_runtime`) plus grants migration `0049` and a `resolveMigrationUrl` seam (`DATABASE_MIGRATION_URL`, empty→`DATABASE_URL` fallback), drilled on a disposable production child branch (full positive/negative probe matrix, then explicit teardown) and documented in a new DB-privilege runbook; it ships inert with production credential cutover deferred by operator decision. The review-effort rider changed mandatory planning adversarial review from xhigh to high (policy revision 26). `pnpm verify` passed 3,536 tests plus one skip; fresh full coverage passed at 85.56/83.21/81.74/86.49 and origin/main-pinned Fallow was clean across all 43 changed files; drift green at revision 26. PR #272, squash `ef2e7df`, reached current-head Greptile 5/5 after both P2 findings were justified — one a verified false positive (drizzle's history table lives in the `drizzle` schema, not `public`) — with CI and semgrep green. Production `lgi-tools-5lmig91vr` is Ready on `lgi.tools` at v3.9.4.1 with no error-level runtime logs; the browser-first smoke confirmed the version and changelog entry with a clean console, and the historical browser-runtime failure did not reproduce. This is the final v3.9 row; every roadmap row is now terminal and the next action is the version-close audit.
- **3.9.3.8** — The privacy page now explains stored site/EVE data, retention, deletion boundaries, rate limiting, browser storage, and character-transfer access in operator-approved plain language; README, CONTRIBUTING, `.env.example`, and the PR template now match the live five-tool app and authoritative data/scope owners. `pnpm verify` passed 3,532 tests plus one skip; fresh full-Postgres coverage passed all 3,533 tests at 85.55/83.19/81.73/86.48, and origin/main-pinned Fallow was clean. PR #271, squash `7babdd3`, reached current-head Greptile 5/5 after its one provider-enumeration request was justified, accepted, and resolved; CI was green. Exact-SHA production deployment `dpl_9GXKo6jBxL4e57nBqgivD6Ua9QLn` is Ready on `lgi.tools`; real-browser review confirmed v3.9.3.8, `/legal`, the release entry, the Under the Hood link, signed-out character/admin gates, and zero console warnings/errors. Deployment logs exposed a pre-existing `/devlog` shared-cache 413 despite its 200 response, and the merge helper falsely counted Greptile's acceptance reply as a second finding; both follow-ups are backlogged.
- **3.9.3.5** — A daily report-only update watch now compares dependency majors, security advisories, and platform/EVE-developer announcements against a committed acknowledged-state baseline through a deterministic fail-closed collector, and opens a single GitHub digest issue only for unreported deltas; the same PR shipped a burst-render hotfix (content-nav `prefetch={false}`, `use cache: remote` on the EVE-status and devlog reads) and the plain-English-first planning policy (revision 25). `pnpm verify` passed 3,532 tests plus one skip; fresh full-Postgres coverage passed at 85.55/83.21/81.73/86.48 and origin/main-pinned Fallow was clean across all 43 changed files; drift green at revision 25 with 54 Python fixtures. The routine ran four pinned demonstrations (report → correct digest #269; suppressed-quiet; zero-candidate-quiet after absorption; three fail-closed refusals across cloud-environment gaps). PR #270, squash `141e914`, reached current-head Greptile 5/5 after one P2 (refused-run candidate undercount) was fixed and re-reviewed clean; CI green. Production `dpl` (`lgi-tools-ex7n1p40q`) is Ready at v3.9.3.5; real-browser reconciliation confirmed the release entry, both content routes healthy, zero background prefetch in production, and a clean console, and a post-merge routine run against the exact merge SHA reached a clean `quiet` verdict. The routine stays paused pending Ryan's explicit unpause.
- **3.9.3.4** — Local wormhole-site development now has a bounded profiler, a counterbalanced six-run normal/sample comparison, a durable both-surfaces lazy-detail probe, and an opt-in development-only 20-of-69 representative catalogue; planning workflows also present a plain-English summary before approval under policy revision 24. `pnpm verify` passed 3,532 tests plus one skip; fresh full-Postgres coverage passed all 3,533 tests at 85.55/83.19/81.73/86.48, and origin/main-pinned Fallow was clean across all 48 changed files. PR #268, squash `70734a0`, reached current-head Greptile 5/5 after its regex-escaping P2 was fixed, with all review threads resolved and CI green. Production `dpl_HyHA9nLq4PmxZm9RAXKjRLQp8wMp` is Ready from the exact merge SHA; real-browser review confirmed v3.9.3.4, the full 69-site catalogue with no sample marker/banner, the release entry, signed-out admin gating, and zero console warnings/errors. Two best-effort telemetry inserts initiated by the smoke returned transient Neon `fetch failed` errors while their user requests still returned 200; no shipped surface failed.
- **3.9.3.3** — Unknown wormhole-site ids now stop at the proxy-owned closed-catalogue boundary and render the shared 404/noindex response without resuming a partial-render shell; valid site and social-image routes are unchanged. The same operator-approved rider removed `drive-session`, routes every Claude subagent through headless `gpt-5.6-sol` at the equivalent high/medium/low effort, and gives each complete planning draft a fresh xhigh adversarial review while preserving the session-terminal planning boundary. `pnpm verify` passed 3,461 tests plus one skip; fresh full-Postgres coverage and origin/main-pinned Fallow were clean, and all 14 DB suites passed 86 tests. PR #267, squash `a2a6a4f`, reached current-head Greptile 5/5 with zero findings and green CI. Production `dpl_AJjrHqvjxZP2R5T3K1cy1qfYFPho` is Ready on `lgi.tools` with no error-level runtime logs; signed-in Safari smoke confirmed v3.9.3.3, the valid detail route, the clean shared 404, and the admin gate without React #419.
- **3.9.3.2** — One shared per-intent EVE type-image resolver (`src/data/eve-data/type-images.ts`) now owns every rendition decision; the two drifted call-site decisions are deleted, `/industry` landing rows upgraded from monograms to images (★ moved to a name suffix), search rows carry a source-owned resolved descriptor, and a variant-literal lint rail with a red/green fixture guards recurrence. Executed by the delegated sol@high workflow in three rounds; the orchestrator acceptance gate caught a vacuous rail-fixture owner test (JSX at a `.ts` path parse-errors and the filter hid it) and a wrong-id recents icon (blueprint id fed to the item intent — blueprints serve no icon rendition), and the operator re-specced blueprint-listing surfaces to the `bp` scroll mid-review (amendment recorded in the plan). `pnpm verify` green (3,444 tests); origin/main-pinned coverage-backed Fallow clean across 44 changed files; DB suites (14 files / 86 tests) green natively — the codex sandbox silently skips them. PR #266, squash `d094393`, Greptile 5/5 on the first pass with zero findings; CI green. Production `dpl_5mBJz1C21VeacGF6L6TDN2oHzceH` Ready on `lgi.tools` with all-200 runtime logs; browser smoke again unavailable (Chrome extension not connected) — CLI evidence plus the pre-merge operator preview review stand in.
- **3.9.3.1** — The development backlog was re-verified end to end: five stale entries whose work already shipped were deleted with named supersessions in per-deletion commits, entries owned by planned 3.9 slices now name their owning sessions as triggers, and the closed fallow code-health record collapsed to an archive pointer; the paired `drive-session` skill (policy revision 22) shipped in the same PR. Implementation was executed by the delegated sol@high codex workflow with orchestrator-owned acceptance. `pnpm verify` passed 3,423 tests plus one skip with the DB suites on local Postgres; fresh full coverage passed at 85.33/83.36/81.34/86.23 and origin/main-pinned coverage-backed Fallow was clean across all 32 changed files; baseline-claims and watch-trigger checkers reported zero warnings. PR #265, squash `a719a7b`, reached current-head Greptile 5/5 after its one cosmetic finding (manifest trailing newline) was fixed and its thread resolved; CI was green. Production `dpl_6WRunL4CQkyGWtakVbSVzXoMHntC` is Ready and aliased on `lgi.tools` with zero runtime errors and all-200 logs; browser smoke was unavailable (Chrome extension not connected; deployment URLs SSO-gated; public edge 429s scripted clients) — 3.9.4.1 scope.
- **3.9.2.10** — The internal token-vend reply now carries only the access token; the divergent whitespace-only scope parser is deleted and `scope-health.ts` remains the sole stored-scope decoder. PL-013 is Delivered. `pnpm verify` passed 3,423 tests plus one skip; the concurrency DB suite passed 4/4 against local Postgres; fresh full-Postgres coverage passed at 85.33/83.36/81.34/86.23 and exact-`origin/main` coverage-backed Fallow was clean. PR #264, squash `f1e29d9`, reached current-head Greptile 5/5 after its one P2 (stale baseline delta notes) was fixed and re-reviewed clean; CI was green. Production `dpl_ABxcnsUkaKjT4WMpYHyRqYYCm6N4` is Ready on `lgi.tools` with clean all-200 runtime logs and no error-level entries; browser smoke was unavailable this session (Chrome extension not connected). Implementation was executed by the delegated `codex exec` GPT-5.6-sol workflow trial with the Claude session as orchestrator/acceptance gate; sol's code drew zero Greptile findings.
- **3.9.2.9** — Base UI and sonner imports now have one package-level ESLint rail: the exact Base UI wrapper allowlist and sole toast owner retain required access, the deprecated package is banned, and eight durable virtual-path fixtures protect the seam. `pnpm verify` passed 3,423 tests plus one skip; fresh full-Postgres coverage passed at 85.34/83.37/81.35/86.24, and exact-`origin/main` coverage-backed Fallow was clean. PR #263, squash `3dfceab`, reached current-head Greptile 5/5 after its lifecycle P2 was justified, accepted, and resolved; CI was green. Production `dpl_L3UgA13WPxismcA9QnWpnSjDjpGn` is Ready on `lgi.tools`; real-browser review confirmed v3.9.2.9, its release entry, the signed-out admin gate, and zero console warnings/errors, with no deployment-targeted error or HTTP-500 logs.
- **3.9.2.8** — Planner row-confidence and aggregate-staleness verdicts now share the exact inclusive boundary owned by the ESI freshness leaf; the planner still confirms the full price set on view, and its confidence labels, reasons, rendering, and AF-005 concern contexts are unchanged. `pnpm verify` passed 3,415 tests plus one skip; fresh full-Postgres coverage passed all 3,416 tests at 85.34/83.35/81.35/86.24, and exact-`origin/main` coverage-backed Fallow was clean. PR #262, squash `1234379`, reached current-head Greptile 5/5 with zero findings and green CI. Production `dpl_Cg4X3Kvvt9hDXfa6jKgvSBC3MJ8x` is Ready on `lgi.tools`; real-browser review confirmed v3.9.2.8, its release entry, the signed-out admin gate, and zero console warnings/errors, with no deployment-targeted error or HTTP-5xx logs.
- **3.9.2.7** — The living primitive ledger maps 52 decision-owning surfaces across UI, API, data, infrastructure, agent workflow, and trust boundaries, records each enforcement rail or deliberate absence, and closes the required overlap, pass-through, and zero-consumer judgments. Ryan approved PL-011–PL-013 as separately planned 3.9.2.8–10 slices; this audit implemented none of them. `pnpm verify` passed 3,410 tests plus one skip, fresh full-Postgres coverage passed all 3,411 tests at 85.34/83.35/81.34/86.23, and exact-`origin/main` coverage-backed Fallow was clean. PR #261, squash `a638f17`, reached current-head Greptile 5/5 with zero findings and green CI. Production `dpl_4LBvRPthh4UzLPc7xi5VLGutwuHd` is Ready on `lgi.tools`; real-browser review confirmed v3.9.2.7, its release entry, the signed-out admin gate, and zero console warnings/errors, with no deployment-targeted error or HTTP-500 logs.
- **3.9.2.6** — The judged Keep implementation preserves separate privacy, growth, and ESI placement owners while one shared schema census reports every missing applicable declaration as a complete checklist; a new rail also rejects unsanctioned user/character foreign-key names before they can bypass purge coverage. The seeded missing-declaration probe failed with the expected growth, purge, and ESI findings, while the permanent gate is green. `pnpm verify` passed 3,410 tests plus one skip, fresh full-Postgres coverage passed the same suite at 85.34/83.37/81.34/86.23, and exact-`origin/main` coverage-backed Fallow was clean. PR #260, squash `cbefaac`, reached current-head Greptile 5/5 with zero findings and green CI. Production `dpl_FT4WC6NTR1ixoCUmErFvULsgXTAk` is Ready on `lgi.tools` from the exact merge SHA; real-browser review confirmed v3.9.2.6, its release entry, the signed-out admin gate, and zero console warnings/errors, with no deployment-targeted error or HTTP-500 logs.
- **3.9.2.5** — One shared Playwright runner now executes 16 durable probe definitions in isolated desktop/mobile contexts with standard diagnostics, named checks, screenshots, and a combined report; all 28 standalone launchers were removed, and the revision-21 drift rail warns about scratch probe scripts left outside the definitions layout. The final full runner pass completed 25/25 definition/viewport runs with only two report-only network findings; `pnpm verify` passed 3,404 tests plus one skip, fresh full-Postgres coverage passed all 3,405 tests at 85.08/83.32/80.48/85.95, and exact-`origin/main` coverage-backed Fallow was clean. PR #259, squash `0959509`, reached current-head Greptile 5/5 after both P2 findings were fixed and resolved; CI was green. Production `dpl_2rSnCRoAm1T5Tb6V76aHh14zzR5s` is Ready on `lgi.tools` from the exact merge SHA; real-browser review confirmed v3.9.2.5, its release entry, the signed-out admin gate, and zero console warnings/errors. Deployment logs showed handled timeout signals on successful 200 home-page requests, whose full content still rendered.
- **3.9.2.4** — The existing API-contract audit now proves every app-owned route consumes an owning-contract schema or declares exactly one `// input: none | query` marker, pins in-route JSON responses to owning-contract types, and pairs with lint that rejects inline `apiFetch` endpoint objects. The full tree passes at 31 schema consumers, 18 no-input handlers, and two query/path handlers; seeded contractless, missing-marker, and inline-endpoint violations all failed the intended gates. `pnpm verify` passed 3,404 tests plus one skip, fresh full-Postgres coverage passed all 3,405 tests at 85.08/83.32/80.48/85.95, and exact-`origin/main` coverage-backed Fallow was clean. PR #258, squash `9d851ae`, reached current-head Greptile 5/5 after both lifecycle-ordering P2 findings were justified, accepted, and resolved; CI was green. Production `dpl_FZGZhXDaaLbcg7U2jp1vWyG6ybFD` is Ready on `lgi.tools` from the exact merge SHA; real-browser review confirmed v3.9.2.4, its release entry, all 69 sites, a working detail page, the signed-out admin gate, and zero console warnings/errors. Exact-deployment API reads returned the 69-site list and site 3, with no deployment-targeted error or 5xx runtime logs.
- **3.9.2.3** — One typed registry now owns placement, upstream freshness, refresh ownership, durable mirrors, and runtime static windows for all 13 externally refreshed datasets; a fail-closed junction gate and two lint rails prevent undeclared or duplicated policy. The three trigger paths remain separate by judged design, while pricing-feature staleness consumption stays a Proposed 3.9.2.7 audit candidate. Fresh full-Postgres coverage passed all 3,398 tests at 85.08/83.32/80.48/85.95 and exact-`origin/main` Fallow was clean. PR #257, squash `eefc59f`, reached current-head Greptile 5/5 after both P2 findings were fixed and resolved; CI was green. Production `dpl_CbazTA4SyC46BG77bCGFAXmv9yp2` is Ready on `lgi.tools`; real-browser review confirmed v3.9.2.3, its release entry, the signed-out admin gate, and zero console warnings/errors, with no deployment-targeted error or 5xx logs.
- **3.9.2.2** — One `defineCronRoute` shell owns cron auth → idle probe → pre-lock gate → advisory lock → work → telemetry ordering for all seven routes, with wake classes, recording policy, and schedule/import rails; the drain's Redis pending-work signal, exhaustion marker, and 12:00 UTC heal slot make both `*/15` crons zero-Neon on idle runs, and `runCronJob` plus the route-local telemetry logger are deleted. `pnpm verify` and fresh full-Postgres coverage passed 3,360 tests + 1 skip; origin-main-pinned coverage-backed Fallow was clean with zero duplication groups. PR #255 squash `47fab31` reached current-head Greptile 5/5 (one P2 fixed-then-reverted by design, one P2 justified and accepted); its first production deploy failed at the Convex push (next/server in the bundled ESI gate) and hotfix PR #256 squash `22b13cb` (Greptile 5/5, zero findings) restored it. Production `dpl_BTkr7XiD1fQfnVQuioyVxdxBnDCS` Ready on `lgi.tools` with zero runtime errors; browser smoke unavailable (Chrome extension not connected).
- **3.9.2.1** — All 14 real-Postgres suites now share one lifecycle-owning harness with schema steering, identity seeds, cleanup, and environment-aware skipping; direct Postgres clients and embedded connection URLs in DB suites are lint-banned. `pnpm verify` passed 3,322 tests plus one skip, fresh full-Postgres coverage passed all 3,323 tests at 88.07/84.75/84.34/89.11, and DB-backed plus database-free coverage-backed Fallow runs were clean. PR #254 reached current-head Greptile 5/5 with zero unresolved threads and merged as squash `76094ce`. Production `dpl_3RxK7SQtmegu5X4woxr5M1MERTCe` is Ready; real-browser review confirmed v3.9.2.1 and its release entry with zero console warnings/errors, and deployment-targeted error logs were empty.
- **3.9.1.7** — Every exported production surface now has an authored interface contract, with scoped lint requiring those comments, validating TSDoc syntax, and rejecting source TODO/FIXME markers. A token audit proved 703 of 704 changed production TypeScript files were comment-only and the remaining file changed only `APP_VERSION`; a seeded uncommented export failed the permanent rule. `pnpm verify` passed 3,322 tests plus one skip; fresh coverage passed 3,237 tests with 86 environment-dependent skips at 85.60/83.92/80.15/86.55, and exact-`origin/main` Fallow was clean. PR #253 used the approved comments-only file-cap review exception, merged with green CI and no inline findings as squash `a8660bc`. Production `dpl_4CyfbcAYE6KXRLQbv8yfDi3niGYm` is Ready from the exact merge SHA; real-browser review confirmed v3.9.1.7, the release entry, signed-out admin gating, and zero console warnings/errors, with no deployment-targeted error logs.
- **3.9.1.6** — The agent drift gate now derives all 31 active contract paths from the lifecycle resolver's index parser and all 20 paired runtime skill files from the paired-skill registry, with reverse orphan detection and narrower sentence-bounded phrase guards at policy revision 20. Eleven focused fixtures and a 19-group live seeded matrix preserve every affected fail-closed behavior. `pnpm verify` passed 3,322 tests plus one skip; fresh coverage passed 3,237 tests with 86 environment-dependent skips at 85.60/83.92/80.15/86.55, and exact-`origin/main` Fallow was clean. PR #252, squash `f16af79`, reached current-head Greptile 5/5 after its one P2 double-resolution finding was fixed and resolved; CI was green. Production `dpl_CcvjGTiCptZx8QjqsGzBeWDCgfQc` is Ready and Promoted from exact SHA `f16af79`; real-browser review confirmed v3.9.1.6, the release entry, the signed-out admin gate, and zero console warnings/errors, with no deployment-targeted error or 5xx logs.
- **3.9.1.5** — Release identity, public PR privacy, version-archive fidelity, and resolver-selected reconciliation branching are now mechanical close-out gates shared by both runtime adapters at policy revision 19. `pnpm verify` passed 3,322 tests plus one skip; fresh coverage passed 3,237 tests with 86 environment-dependent skips at 85.60/83.92/80.15/86.55, and exact-`origin/main` Fallow was clean. PR #251, squash `162b6c2`, reached current-head Greptile 5/5 after four findings were fixed and all threads resolved; CI was green. Production `dpl_AWMhu4KZFRV7LMVY2KDFuXvkJSkV` is Ready and Promoted from exact SHA `162b6c2`; real-browser review confirmed v3.9.1.5, both deferred/current changelog entries, the signed-out admin gate, and zero console warnings/errors.
- **3.9.1.4** — Close-out now recomputes the code-health baseline's cheap factual claims and evaluates every fenced Watch trigger through one shared measurement owner; stale counts and missing targets are anchored findings, while tripped triggers report `promote AF-NNN` without auto-promotion. Durable workflow docs, paired skills, hooks, and checker utilities are tracked, with machine-local settings and credentials still ignored; the exposed Neon key was rotated and revoked without entering Git. `pnpm verify` passed 3,322 tests plus one skip; fresh full-Postgres coverage stayed at 88.06/84.80/84.34/89.06 and pinned Fallow was clean. PR #250, squash `f8e79c5`, reached Greptile 5/5 after all three tooling findings were fixed and auto-resolved; CI was green. Production reconciliation completed through PR #251 after the prior GitHub deployment incident cleared.
- **3.9.1.3** — The workflow drift gate now runs shared checker fixtures and live environment-example, documentation-reference, and lifecycle-evidence checks, with source-line findings and warning/error severity; policy revision 16 keeps both runtime skill trees and required local tooling in sync. All 22 checker fixtures and live reports passed; `pnpm verify` passed 3,322 tests plus one skip, fresh coverage passed all 3,323 tests at 88.06/84.80/84.34/89.06, and pinned Fallow was clean. PR #249, squash `247c84a`, Greptile 5/5 zero findings; exact-SHA production `dpl_AdxcHpTzNTgRonYYpz97bdXRSCZT` Ready at v3.9.1.3 with clean homepage/changelog/admin-gate browser smoke and no error-level runtime logs.
- **3.9.1.2** — The lifecycle resolver now treats roadmap terminal states as exact tokens, enforces the 3.9 `Execution status` / `Baseline effect` / `UX gate` vocabularies with legacy exemptions, carries `UX gate: Yes` into the execution pause, and adds opt-in warn-only git snapshot checks for branch naming, dirty plan-mode worktrees, and local-main drift. All 24 fixtures passed; live no-flag output retained SHA-256 `88514da…e02`; drift stayed green at policy revision 15. `pnpm verify` passed 3,322 tests plus one skip; full-Postgres coverage passed all 3,323 tests at 88.06/84.80/84.34/89.06 and pinned Fallow was clean. PR #248, squash `6d3386a`, Greptile 5/5 zero findings; production `dpl_6Uk8B2jSyGtzehQuVsKCANFy92mN` Ready at v3.9.1.2 with exact-artifact 200/version/changelog proof and no error-level or 5xx logs. Browser-first smoke was unavailable because this runtime exposed no Browser-control tool.
- **3.9.1.1** — Every lifecycle decision gained one owning document and a machine-readable form: exact marker vocabularies (`UX gate` header, `Baseline effect`, `Execution status`, closed terminal-status set), fenced `watch-trigger` blocks (trip-form `exports|files|clones` grammar; AF-006–AF-009 live in the baseline), the planning-terminal rule across the lifecycle docs + both skill trees at policy revision 15 (manifest-enforced via `session-terminal`), the public-documents truth loop (SELF_REVIEW §5 + audit Step 3), and the specified TSDoc-lite comment standard (AGENTS.md; enforcement = 3.9.1.7). Dead references fixed (VERSION_AUDIT known-wide commands → generic, PRE_PR tripwires, DEV_PERF archive pointer); doc-ref sweep clean. `pnpm verify` green (3,322 tests + 1 skip); fresh coverage 88.06/84.80/84.34/89.06 + pinned Fallow clean; drift green at revision 15. PR #247, squash `c094dd2`, Greptile 5/5 zero findings; production `dpl_DyLrZLP5Q8baZ2XAETRoC7ztfLxv` Ready at v3.9.1.1 with no error-level runtime logs. Browser smoke unavailable (Chrome extension not connected — 3.9.4.1 scope). The UI_SYSTEM_AUDIT deletion rider shipped in the same PR.

- **3.8.5.5** — The monolithic 52-field planner context and `usePricing` were replaced by five separately memoized concern contracts with no façade; all 11 component consumers now import only their concerns, templates compose their sanctioned non-market state, and provider orchestration fell 180 LOC. `pnpm verify` passed 3,322 tests plus one skip; fresh full-Postgres coverage and pinned Fallow were clean, and the operator approved the manufacturing/reaction desktop/mobile sweep. PR #246, squash `291ee78`, Greptile 5/5 with zero findings; production `dpl_CufNdR9TsEcSWYeUgsm3KKffm7yP` Ready and promoted at v3.8.5.5 with release/planner requests clean and no error-level or 5xx logs. Production Browser control was unavailable.

- **3.8.5.4** — The 35-export auth query hub was replaced by focused linked-character, affiliation, admin-user, owner-transfer, account-purge, and verification-retention owners with direct imports and no compatibility façade; five real-Postgres suites protected all 25 migration scenarios, and Greptile's one P2 malformed-account-id loop finding was fixed and resolved. `pnpm verify` passed with 3,287 tests plus one skipped; fresh coverage-backed Fallow was clean apart from the inherited gate-excluded cron clone. PR #245, squash `8447fc1`, Greptile 5/5; production `dpl_5qndPZ2yZY2RBdy4gfWbHe29raqs` Ready at v3.8.5.4 with version/changelog/auth-gate requests clean and no error-level or 5xx logs. Browser smoke was unavailable because this runtime exposed no Browser-control tool.

- **3.8.5.2** — Personal jobs, corporation jobs, and corporation structures now share `planRead` for conditional-read outcomes; the two corporation datasets also share one neutral descriptor builder while retaining distinct durable keys, consent, projections, persistence, and role-loss behavior. `pnpm verify` passed with 3,254 tests plus one skipped; fresh coverage-backed Fallow was clean and full-range Fallow removed both AF-002 clone IDs with zero duplication groups remaining. PR #243, squash `39704e7`, Greptile 5/5 with zero findings; production `dpl_5rJMztHk9nh4u4G3uMk4whpuNU8A` Ready at v3.8.5.2 with clean signed-out jobs/industry browser smoke and no error-level runtime logs.

- **3.8.5.1** — Seventeen account/admin mutations now share one app-layer authorize → same-origin observation → optional parse → handler pipeline while route-local guards, schemas, responses, rate limits, redirects, and telemetry remain visible; five non-campaign mutations remain direct and all 22 observation paths are pinned. `pnpm verify` passed with 3,259 tests, fresh coverage-backed Fallow was clean, and full-range Fallow removed all seven AF-001 clone IDs. PR #242, squash `b5f2f37`, Greptile 5/5 with zero findings; production `dpl_43gjbWNM3NuPPPHL1kQ8JCrpLKem` Ready at v3.8.5.1 with clean version/changelog/auth-gate browser smoke and no error-level runtime logs.

- **3.8.4.8** — Four independently degrading admin operations panels surface the shared ESI budget, durable refresh queue/dead letters, cost/fallback lens, and recent domain events; one same-origin admin action safely returns a dead letter to the normal guarded queue. `pnpm verify` passed with 3,246 tests, fresh coverage-backed Fallow and 30 real-Postgres integration tests were clean, and signed-in local review verified the view/retry path. PR #241, squash `5e7222a`, Greptile 5/5 with zero findings; production `dpl_4X4obRXWka36rhKYjUmoC2JqK3gP` Ready at v3.8.4.8 with clean browser smoke and no error-level runtime logs.

- **3.8.4.7** — A dedicated Neon event ledger records completed/degraded price refreshes, persisted corporation-asset snapshots, CAS-confirmed character-token custody transitions, durable deferred-worker outcomes, and sustained public ESI budget exhaustion with closed privacy-safe metadata. Next request-lifetime scheduling keeps best-effort inserts alive after responses, and daily housekeeping retains the append-only history for 400 days. `pnpm verify` passed with 3,206 tests; fresh coverage-backed Fallow, route presence, drift, local migration, and real-Postgres recovery/retention boundaries were green. PR #240, squash `9cee5e0`, reached Greptile 5/5 after its dropped-insert P1 was fixed and the existing recovery-summary contract was accepted. Production `dpl_9AjHdCjhHXz5Gz2ju7PUep3c4tjW` is Ready at v3.8.4.7; real-browser review confirmed the release entry, signed-out admin protection, and zero console errors, with no Vercel error logs.

- **3.8.4.6** — The shared ESI scoreboard remains the sole dispatch authority while cost-facing market refreshes, all seven owned-data APIs, planner stages, and Neon retries emit in-house timing, volume, and source telemetry. Price fallback remains canonical on `cron_prices`; market history records stored-source outcomes without a second fallback tracker, and completed 15-minute windows aggregate sustained public gate refusals into one alert. Full coverage passed with 3,149 tests on PR #238 and 3,150 on hotfix PR #239; real-Postgres telemetry coverage, pinned-base Fallow, route presence, and drift checks were green. PR #238, squash `2fdcc61`, and hotfix PR #239, squash `1364562`, both reached current-head Greptile 5/5 with green checks. Production `dpl_8L8t7sw4xAezLixLRhGgzsuEiJC2` is Ready with v3.8.4.6 rendered on `/` and `/industry/691` and no error-level or 5xx logs. Browser smoke was not completed because the Browser runtime could not initialize (`Cannot redefine property: process`).

- **3.8.4.5** — All five slow owner-sync datasets now defer budget-blocked refreshes into a Neon-backed, coalescing queue drained by a bounded 15-minute cron through the existing ESI gate. Retries, interrupted-run recovery, dead-letter alerting, per-job failure isolation, and terminal retention are covered. `pnpm verify` passed with 3,168 passing tests plus one skipped; DB-less CI-equivalent and full local-Postgres coverage-backed Fallow passed, including concurrency, retention, and interrupted-attempt boundaries. PR #237, squash `f5b24a6`, Greptile 5/5 after both findings were fixed; production `dpl_HHU75y5wSNfMLqYPVJwFDwzxjWNU` Ready with migrations, 77 route classifications, and Convex deployment green and no runtime error logs. Browser smoke was not completed because the Browser runtime could not initialize (`Cannot redefine property: process`).

- **3.8.4.4** — Fresh corporation-asset refreshes now preserve an encrypted raw ESI snapshot, link current asset rows to their source, and prune seven-day history while retaining newest and referenced snapshots; stored data can be replayed with zero ESI calls. `pnpm verify` passed with 3,154 tests, CI-equivalent coverage-backed Fallow passed, the real-Postgres retention boundary passed, and all 76 routes were present. PR #236, squash `8243394`, Greptile 5/5 with zero findings; production `dpl_DR58L2vebh2eYfte3hpNF4V5cqKn` Ready with no runtime error logs. Browser smoke was not completed because the Browser runtime could not initialize (`Cannot redefine property: process`).

- **3.8.4.3** — Every durable Postgres table now declares a bounded, pruned, or purge-managed growth story, with automatic schema discovery and purge-registry validation preventing undeclared tables from shipping. Daily housekeeping keeps GSC and corp-access history to 400 days and clears abandoned OAuth state one day after expiry; Greptile's P1 moved cleanup ahead of fallible GSC work. `pnpm verify` passed with 3,143 tests plus a real-Postgres retention boundary test. PR #235, squash `96fb2b5`, Greptile 5/5; production `dpl_6HXFd32MByGZgFSSv5kHm7iuMHeu` Ready with no runtime error logs. Browser smoke was not completed because the Browser runtime could not initialize (`Cannot redefine property: process`).

- **3.8.4.2** — Token refresh hardening. Refresh failures now emit privacy-safe reason telemetry; one `invalid_grant` preserves encrypted custody and starts a five-minute quiet window before one confirmation attempt, with successful refreshes and OAuth relinks resetting the strike and CAS races favoring successful token rotation. Greptile's one P1 finding led retryable confirmation failures to re-arm the quiet window, preventing post-grace provider storms. Focused coverage passed 33/33 including four real-Postgres concurrency cases; `pnpm verify` passed with 3,136 tests and clean Fallow. PR #234, squash `d31bd72`, Greptile 5/5; production `dpl_9vJyCY8kGs1pFuGcco35JZ4GfAuW` Ready. Browser smoke confirmed v3.8.4.2 and the signed-out settings gate with zero console messages and no runtime error logs.

- **3.8.4.1** — Security hardening. All 21 browser mutation routes now share log-only, privacy-safe same-origin telemetry, including preview custom-domain handling; the internal EVE token route requires user-plus-character ownership before vending an access token, with Convex passing both identifiers. `pnpm verify` and all 76 route-presence checks were green. PR #233, squash `8eb468c`, Greptile 5/5 after one finding fixed and one deployment-order concern withdrawn; production `dpl_ACNrb9LChpu6idfHJDgat3riNdyj` Ready. Browser smoke confirmed v3.8.4.1, clean public detail rendering and signed-out settings/admin gates with no console or runtime errors. The invalid-site React #419 candidate failed its production-preview gate, was reverted, and lives only in the Sites backlog.

- **3.8.3.4** — GSC coverage dashboard. The daily sync now inspects the canonical sitemap URL set with validation, quota safeguards, retry-safe daily history, and partial-failure handling; the admin dashboard shows indexed/not-indexed trends, reason distribution, and latest state for every URL. Production GSC credentials are configured. PR #232, squash `9c6ceaf`, reached Greptile 5/5 after all five P1 findings were fixed; GitHub checks were green and production deployment `dpl_3HBSEFMNZPGeo8KZoMi6y1zduo2i` is Ready at v3.8.3.4. Browser smoke confirmed `/sites/3` and the signed-out admin gate, while the correct 404/noindex response for `/sites/100` still emits React #419; its bounded 3.8.4.1.1 candidate failed and the issue now lives in the Sites backlog.

- **3.8.3.2** — Social cards and structured data. Every shared page now carries page-specific OG/Twitter metadata; devlog documents emit Article schema, industry and site detail pages emit breadcrumbs, and the hardcoded catalogue count was removed from metadata. Default and per-site cards render as 1200×630 PNGs using the bundled static JetBrains Mono font. `pnpm verify` passed with 3,062 tests, route presence and both SEO verification scripts were green. PR #230, squash `7e5f5dc`, reached Greptile 5/5 after both P2 findings were fixed. Its first production build exposed that Next 16 classifies the image file conventions as dynamic runtime handlers; hotfix PR #231, squash `e03b2ad`, corrected the route contract and passed Greptile 5/5 with no findings. Production deployment `dpl_8y2trepfw5GUevLjyyKAkaAPqq4h` is Ready; real-browser review confirmed page metadata, Article and breadcrumb JSON-LD, both 1200×630 images, the signed-out admin gate, and zero warnings/errors on normal app routes. Vercel reported no production error logs in the post-deploy window.

- **3.8.3.1** — Crawl unblockers. Every catalogue card now exposes a server-rendered detail link, each detail page cross-links three deterministic related sites, and the sitemap includes `/contact` while using only source-backed modification dates. `pnpm verify` and the desktop/mobile UX sweep were green; Ryan reviewed the catalogue and detail layouts locally. PR #229, squash `9cd19f6`, Greptile 5/5 after one P2 date-validation finding was fixed; GitHub checks green. Production asset deployment `dpl_2StiPs6WXjaar1dJB38wgmMmhPAT`; real-browser smoke confirmed v3.8.3.1, 69 unique detail links on `/sites`, and three related links on live production entry `/sites/3` with no console warnings/errors. The broad Vercel CLI status sweep stalled and was terminated, so the browser-delivered deployment asset is the recorded production evidence.

- **3.8.2.8** — Final UI-system audit + future primitives (Part E). Generated findings evidence closed the raw component `rgba()` loophole, tokenized remaining accidental visual values, removed dead `.flow-*` CSS, and preserved the protected chart/devlog exceptions. Twelve shared primitives shipped with an admin-gated `/preview/primitives` reference and the planned production migrations. `pnpm verify`, route presence, policy drift, and the 27-shot UX sweep were green; Ryan reviewed after the PR opened. PR #228, squash `3df76eb`, Greptile 5/5 with no findings, prod `lgi-tools-8pq96ljl3` (`dpl_AYcJt54FmGRKks2YrCpNsU3GUMVo`) Ready; real-browser production smoke confirmed v3.8.2.8 and the unauthenticated preview redirect.
- **3.8.2.7** — Admin chart legibility (Part D). The admin **chart dialect** (reused by 3.8.3.4 GSC coverage + 3.8.4.8 ops view): `MetricTable` (replaces the KpiCard grid), `AnnotatedDailyChart` (daily bars + 7d MA + prior-period reference + dim-blue weekend bars + changelog deploy markers + end label; replaces the sessions area chart), `MultiplesGrid` (GSC clicks/impr/position side-by-side, position inverted, the "More search detail" Collapsible deleted), `DistributionBars` (count+% for top lists + the visit-frequency histogram), `StackedShareBar` (returning-vs-new + referred-vs-direct). All analytics server-side; `KpiCard`/`buildKpiCards` retired; `bar-chart.tsx` kept (StatusStrip). ⭐⭐ Fallow scores **CRAP per-function** & the src suite has **no `.test.tsx`** → decompose chart components + extract geometry to tested `.ts` helpers (`daily-chart-geometry.ts`, `end-label.ts`). ⭐ Greptile round-3 exposed 2 edge cases my round-1 fixes surfaced (share denominator must be all-clicks not top-10 subtotal AND guard a missing GSC total row; the mobile end-label needs an always-visible readout outside the scroller). PR #227, squash `77eab72`, Greptile 5/5 (6 findings all fixed), prod `lgi-tools-j1vni5dz5` Ready. → [[feature-3-8-2-7-admin-charts]].
- **3.8.2.6** — EveImage pipeline. One shared `EveImage` wrapper (next/image + custom CCP-direct loader, snap-to-ladder sizing, optimizer bypassed) for every EVE portrait/logo/item-icon; `TypeIcon` relocated to `components/`; raw `<img>` + out-of-wrapper `next/image` lint-banned; `imageSizes` pinned to CCP's ladder. Above-the-fold portraits preload (Next 16 `preload`, replacing deprecated `priority`). ⭐ Greptile P1 "empty-src crashes loader" was a false positive (next/image skips the loader for falsy src) — justified + accepted, why-comment added. Also re-synced the local Codex skill tree to shared-policy rev 3 and cleared 13 stale local/remote branches. PR #226, squash `962f9fb`, Greptile 5/5 (1 P1 justified), prod `lgi-tools-jja5c5jvo` Ready. → [[feature-3-8-2-6-eve-image]].
- **3.8.2.5.1** — Content sources segmented; shared sticky/scrollable document browser adopted by Under the Hood and a new master-version changelog with titled navigation + stable history URLs. PR #225, squash `2a39d29`, Greptile 5/5 with no findings, prod `lgi-tools-l1ipvair1` Ready.
- **3.8.2.5.0.1** — EVE-status build resilience. A cold scoreboard/ESI/payload failure is converted to the existing offline nav state inside the Cache Components boundary; degraded results revalidate after 5 seconds while retaining a prerender-safe 5-minute expiry. Added healthy + three-path failure coverage. PR #224, squash `91d9261`, Greptile 5/5 after one P2 test-mock finding fixed (third racing pass also clean), prod `lgi-tools-e031ipwuf` Ready.
- **3.8.2.5.0** — Claude Code + Codex local workflow separation. Operator-specific `.agents`, `.claude`, `.codex`, and `.agent-local` configuration is ignored; canonical shared policy, runtime-tailored skills, hook parity, memory, and the drift checker remain local. PR #223, squash `d30ce50`, Greptile 5/5 with no findings; production delivered in the subsequent `.0.1` deployment after its merge exposed the pre-existing ESI-status build blocker.
- **3.8.2.4** — Devlog IDE excerpts (Part B; shipped OUT OF ORDER after 3.8.2.5, independent of the Component arc). `/devlog` code excerpts render with server-side Shiki syntax highlighting (fine-grained `createHighlighterCore` + JS regex engine, no WASM; 9 grammars + `github-dark-default`), a sticky line-number gutter (real numbers for a clean single range, relative 1..N for multi-range), and optional pinned-SHA "view on GitHub" permalinks. Highlight runs in `loadDevlog` (`'use cache'`) → **zero client Shiki** (`.next/static` CLEAN, no `.wasm`); ~112ms cold for 242 excerpts. NO APP_VERSION bump (stayed `3.8.2.5`); CHANGELOG in numeric slot; operator ⌘-glyph/renumber edits committed separately. ⭐⭐ per-token `style={{color}}` = one inline `eslint-disable no-restricted-syntax` (color is a var; 0 CSP). ⭐⭐ pnpm strict → `@shikijs/langs`+`@shikijs/themes` need DIRECT install. ⭐⭐ the former no-Upstash header-status build failure was fixed in 3.8.2.5.0.1. ⭐ permalinks require a full 40-char SHA (Greptile P2). PR #222, squash `e3ed34c`, Greptile 5/5 (1 P2 fixed), prod `lgi-tools-j0sim4ddw`. → [[feature-3-8-2-4-devlog-excerpts]].
- **3.8.2.5** — Combobox unification (Component System v1, Phase 3 / C3 — arc FINALE). One `combobox.tsx` on Base UI Autocomplete (not Combobox) under both search surfaces; `terminal-search.tsx` (planner/structure system pickers, public API preserved) + `GlobalSearch.tsx` (header ⌘K) migrated; `.dropdown`/`.ns-*`/`.dd-*` (~201 CSS lines) + `global-search-aria.ts` deleted; search on `bg-deep` well. Also fixed the `<label>`-wraps-Select/Input far-click bug on /structures. ⭐⭐ `<label>` around a Base UI Select opens the dropdown on label-click (trigger button = first labelable el) → heading = `<div>` + `ariaLabel`. ⭐ InputGroup focuses input on mousedown anywhere in the well (`handleInputPress`). ⭐ external filter = `filter={null}` + controlled value + flat `items`. ⭐ highlight-ref goes stale → clear on query-change AND popup-close (Greptile P2). ⭐ icon tones via exported `pillToneClasses` + sites `CLASS_TONE`. APP_VERSION→`3.8.2.5` (match branch, drop `.N.1`). PR #221, squash `5d16c05`, Greptile 5/5 (1 P2 fixed), prod `lgi-tools-a8ngjfr4e`. **Component System arc COMPLETE.**
- **3.8.2.3.1** — Component System v1, Phase 2 (overlays/controls). FeedbackModal→Base UI `dialog.tsx` (`modal.tsx` deleted); new `select.tsx` Base UI Select (data-driven `items`, trigger reuses `fieldVariants`, popup on shared `dropdown-panel.ts`) supersedes the C1 native `<select>` — 4 consumers migrated + `<select>` lint exemption deleted; `stepper.tsx`→Base UI NumberField (`stepper-math.ts` deleted); menu/popover/select share one `panelSurface`. ⭐ Base UI Select needs the value→label `items` map or the trigger shows the raw value. ⭐ NumberField emits null MID-EDIT on clear → coerce null→min in `onValueCommitted`, not `onValueChange` (Greptile P1). ⭐ global `:focus-visible` ring doubled the field border → `.field-own-focus:focus-visible{outline:none}`; verify focus rings with KEYBOARD probes (programmatic `.focus()` skips `:focus-visible`). APP_VERSION→3.8.2.3.1. PR #220, squash `775c78b`, Greptile 5/5 (P1 fixed + P2 justified), prod `lgi-tools-jl47yyzet`.
- **3.8.2.2.1** — Component System v1, Phase 1 (Kit 4 "Inset Instrument"). Radius (`--radius-ctl` 4 / `--radius-card` 6) + 6 elevation tokens; `Button` (primary/secondary/ghost/danger × md/sm, exports `buttonVariants`, ref-forwarding) + `Input`/`Select`/`Textarea` (one `fieldVariants` inset well) + `Card` unified (absorbs `PANEL_CLASS` + `.sites-card`, `hover`/`font`/`data-*`); 32 `rounded-[Npx]`→2 tokens (2 allowlisted inner indicators); all 3 `inputClass` + `.feedback-btn` deleted; lint guards on `rounded-[` / `<select>` / `inputClass` (input.tsx select-exempt). One feature per commit (18 commits). ⭐ twMerge misfiles `--shadow-*` as shadow-COLOR → register `shadow`+`rounded` groups in `cn.ts`; ⭐ cva `size` variant collides with native `<input>/<select>` `size` attr → `Omit<…,'size'>`. Contrast gate resolved (inset-well shadow defines the field on dialogs). APP_VERSION→3.8.2.2.1. PR #219, squash `f17d6d6`, Greptile 5/5 (0 findings), prod `lgi-tools-6kvm6eaa7`.
- **3.8.2.1.1** — Patch after the type work (2 riders, not the C1 build). (1) `globals.css` Tailwind scan scoped to `src/` via `source(none)` + `@source "../"`, structurally closing the config/doc content-scan hole 3.8.2.1 found; verified on a real dev-server render (styles intact + a poison comment no longer 500s). (2) Changelog chapter titles bumped `--text-lead`→`--text-h2` to match the version size. APP_VERSION→3.8.2.1.1. PR #218, squash `219f247`, Greptile 5/5 (0 findings), prod `lgi-tools-f3er06xp2`.
- **3.8.2.1** — Type System v2 (Part A). Semantic `--text-*` scale (micro 10/label 11.5/ui 13/body 15.5/lead 17/h3 19/stat 26/h2 26/display clamp→42 + hero, line-height bundled per tier via Tailwind v4 `--text-*--line-height`); IBM Plex Mono retired → JetBrains Mono the sole mono face (4→3 fonts, weights 400–800); ~333 `text-[Npx]` + 64 globals.css font-sizes swept role-by-role (fanned out over 13 slice agents, reviewed per-diff); `cn.ts` `extendTailwindMerge` registers the tokens as font-sizes (else pills lose color); new-`text-[` lint guard. APP_VERSION→3.8.2.1. PR #217, squash `e42f345`, Greptile 5/5 (0 findings; auto-review skipped for the file count → `@greptileai` trigger), prod `lgi-tools-2iwfs8i0u`.
- **3.8.1.3** — Neon platform adoption (Phase 1 Rails, final rail). Committed `neon.ts` branch-config policy (`@neondatabase/config@^0.4.2`, `/v1` import, registered as a fallow `entry`): prod protected + compute pinned 0.25/2; new `preview/` branches get ttl 3d + min 0.25/max 1 + suspend 1m; other branches `{}`. Applied LIVE — prod `main` now protected on Neon. `neonctl`→`neon` CLI rename (alias kept). ENV = BESIDE (no `@neondatabase/env`; `src/lib/env.ts` untouched). APP_VERSION→3.8.1.3. PR #216, squash `6c29218`, Greptile 5/5 (1 fix + 1 justified/withdrawn), prod `lgi-tools-oeb9f1atx`.
- **3.8.1.2** — `noUncheckedIndexedAccess` ON (Phase 1 Rails, rail 2). Flag flipped in committed tsconfig; 117 source + 228 test + 24 gitignored margin-audit fixes across 2 clusters/1 branch, + a 952-line dead-code delete. 3-site `!` audit: bearerMatches→`?? 0`+new test (−2), ingestUnderLock/splitEntries→KEEP by-construction. Source `!` net +16 (down from 18). ⭐ `pnpm fallow` DOES gate CRAP AND reads coverage-final.json (stale coverage over-estimates). APP_VERSION→3.8.1.2. PR #215, squash `0b18ec3`, Greptile 5/5 (0 findings), prod `lgi-tools-k9ug89ctm`.
- **3.8.1.1** — Toolchain tightening (first v3.8 slice, Phase 1 Rails). Lint gate `--max-warnings 0`; `tsconfig` target ES2017→ES2022; AnyPgDb single-home verified (no refactor). Opened the `v3.8 — Undock Checklist` changelog chapter + APP_VERSION→3.8.1.1. PR #214, squash `c74c2cb`, Greptile 5/5, prod `lgi-tools-h3tzmy3u8`.
