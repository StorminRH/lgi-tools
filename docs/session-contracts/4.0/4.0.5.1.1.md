## Session 4.0.5.1.1 — Cycle-1 audit floss

**Sub-version:** 4.0.5.1
**Master plan:** `docs/VERSION_4_0_PLAN.md` §4.0.5.1
**UX gate:** No
**Execution profile:** Frontier autonomous coding agent
**Delivery unit:** One agent session, one shared sub-version branch, one sub-version PR
**Roadmap coverage:** §4.0.5.1 complete — AF-010 unused popover export, AF-011 shared Convex HTTP service door, AF-012 ux-check layout citation and ledger diagnosis
**Internal phases:** 1. Prove or finish the AF-010 unused-export end-state; 2. Prove or finish the AF-011 shared-door end-state; 3. Retarget or drop the AF-012 layout-table path and correct its ledger diagnosis; 4. Mark AF-010, AF-011, and AF-012 Delivered and publish the sub-version changelog; 5. Integration and proof
**Split triggers:** Remaining slice door wrappers still form a Fallow clone that cannot share the existing transport helper without changing slice HTTP contracts; or making the document-reference checker warning-free requires changing that checker's archive-reference rule rather than retargeting or dropping the one layout-table row

## 1. Objective

Close the three cycle-1 Floss findings — AF-010, AF-011, and AF-012 — so the
version-close audit can restart against current `main` with no open actionable
rows. This is the sole session of the approved remediation sub-version 4.0.5.1.

## 2. Current context and dependencies

- **DEP-1:** The cycle-1 ledger in `docs/version-audits/4.0/PLAN.md` records AF-010, AF-011, and AF-012 as the only open Floss findings and maps them to 4.0.5.1.
- **DEP-2:** Live `main` may already satisfy AF-010 and AF-011 through PR #429; the session must prove those end-states or finish the remaining work.
- **DEP-3:** `docs/contributing/end-to-end-testing.md` exists as a live file; `check-doc-refs` treats a `../` citation as a repo-root archive path, which is why the ux-check layout table still warns.
- **DEP-4:** Watch findings AF-006, AF-007, and AF-008 remain Watch and are not part of this bundle.

## 3. Done conditions

- **DC-1:** `src/components/ui/popover.tsx` exports only consumed primitives, and `PointerPopover` is absent from the export surface and the import graph (AF-010).
- **DC-2:** One shared helper owns Convex HTTP transport, status, JSON, and contract parse; jump-resolver and signature-elimination own only path, body, schema, and error class; Fallow does not report clone group `dup:50ee3d46` unless a documented accepted clone is the only remaining honest option (AF-011).
- **DC-3:** The `docs/ux-check/README.md` layout table names the live repo-root path `docs/contributing/end-to-end-testing.md` or that row is dropped, and `check-doc-refs` is warning-free on that line (AF-012).
- **DC-4:** The AF-012 ledger diagnosis names the checker's `../` archive-reference rule instead of claiming the target file was deleted.
- **DC-5:** The delivering pull request marks AF-010, AF-011, and AF-012 Delivered.

## 4. In scope

- **IS-1:** The unused popover export, or proof that AF-010 is already true on live `main`.
- **IS-2:** The shared Convex service-door owner and any remaining slice wrappers, or proof that AF-011 is already true on live `main`.
- **IS-3:** The ux-check layout-table citation and the AF-012 ledger diagnosis correction.
- **IS-4:** The three Delivered markers, the sub-version changelog entry, and the application-version bump.

## 5. Out of scope

- **OOS-1:** Watch findings AF-006, AF-007, and AF-008, their triggers, or their dispositions.
- **OOS-2:** Baseline Current-cell refresh and Code-ref advance left from the cycle-1 measurement pass.
- **OOS-3:** Master-plan AF-009 citation drift, the mapper unreachable-Fallow warning, and the `SectionHeader` CRAP signal.
- **OOS-4:** Carry-forwards named in the master plan (D1 product validation, Convex Pro durability, membership source, OCC, EVE-Scout succession).
- **OOS-5:** Redesigning `check-doc-refs` to resolve relative paths from the source file.

## 6. Hard constraints

- **HC-1:** This session maps only AF-010, AF-011, and AF-012 and absorbs no unaudited scope.
- **HC-2:** Slice HTTP contracts — path, body, schema, and error class — stay with their composition owners; the shared door owns transport only.
- **HC-3:** A warning-free document-reference check must come from retargeting or dropping the one layout-table row, not from changing the checker's archive-reference rule.
- **HC-4:** A documented accepted clone is allowed only after a real shared transport owner still cannot satisfy Fallow without changing slice HTTP contracts.

## 7. Decisions the session plan must resolve

- **PD-1:** Whether AF-010 is already true on live `main` or still needs a code change.
- **PD-2:** Whether AF-011 is already true on live `main`, and whether remaining slice door wrappers still form a Fallow clone.
- **PD-3:** Whether AF-012 retargets the layout-table path to the live repo-root file or drops the row.

## 8. Acceptance criteria

- **AC-1:** The popover primitive's public surface contains no `PointerPopover` symbol and no importer references it — proving DC-1.
- **AC-2:** Fallow reports no `dup:50ee3d46` clone, or a documented accepted clone remains only under HC-4, and slice doors do not re-own transport — proving DC-2 and HC-2.
- **AC-3:** `check-doc-refs` emits no warning for the ux-check layout-table line — proving DC-3 and HC-3.
- **AC-4:** The AF-012 ledger row's principle diagnosis names archive-reference semantics rather than a deleted file — proving DC-4.
- **AC-5:** The delivering pull request's audit ledger marks AF-010, AF-011, and AF-012 Delivered — proving DC-5.

## 9. Verification

- **V-1:** Export-surface and import-graph evidence for the popover primitive.
- **V-2:** Shared-door ownership evidence and Fallow clone-group output for the two composition doors.
- **V-3:** Document-reference checker output for the ux-check layout line, plus the corrected AF-012 ledger diagnosis.
- **V-4:** The repository's standing definition-of-done gate.

## 10. UX/operator gates

This session changes no user-facing behavior or appearance, so its `UX gate`
marker is `No` and no operator browser review is required before the pull
request opens. The standing external review gate on the pull request still
applies.

## 11. Baseline/hotspot boundary

Expected baseline effect is Neutral. Planning must confirm the work does not
widen Watch surfaces AF-006, AF-007, or AF-008, does not grow `convex/engine.ts`
or the mapper host layer, and treats Fallow clone/unused-export signals as
proof rather than as a reason to add threshold overrides.

## 12. Close-out behavior

This session is the only session of sub-version 4.0.5.1, so it completes the
sub-version: it commits on the shared lifecycle branch, opens the one
sub-version pull request, and may become `Complete` only once that pull request
passes the external review gate and merges. The delivering pull request marks
AF-010, AF-011, and AF-012 Delivered before merge. After merge, the resolver
restarts the full version-close audit against current `main`.
