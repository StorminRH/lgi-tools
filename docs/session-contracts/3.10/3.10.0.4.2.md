## Session 3.10.0.4.2 — Migrate design review & decompose DESIGN_PRINCIPLES

**Sub-version:** 3.10.0.4 (Phase 0 — Documentation & lifecycle consolidation; session B of two, one branch)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.4
**UX gate:** No

## 1. Objective

Make design review one canonical procedure the skill adapts over, and decompose
`DESIGN_PRINCIPLES.md` to its destinations — its red-flag smells become concrete
steps, its philosophy collapses to a one-paragraph creed, and only its §5
repo-map remnant survives (folded away in 0.6).

## 2. Current context and dependencies

- **DEP-1:** Depends on session A (`3.10.0.4.1`, same branch): the planning procedures exist
  and the `docs/workflows/` pattern is established.
- **DEP-2:** `DESIGN_PRINCIPLES.md` is the constitution today; Phases 1–3 reference its
  principles, so the creed must preserve the load-bearing philosophy while the
  smells move to STEP and the metric-caught smells move to CHECK.

## 3. Done conditions

- **DC-1:** `PRE_PR_DESIGN_REVIEW` + `DESIGN_PRINCIPLES §3` (the red-flag smells) become
  `docs/workflows/pre-pr-design-review.md`, with each smell written as a concrete
  step (scan → if found → split/extract) and its principle named inline as
  rationale; smells Fallow already detects are stated as "the gate catches this"
  (CHECK), not re-described (STEP).
- **DC-2:** `DESIGN_PRINCIPLES §2/§6` collapses to a one-paragraph creed at the head of the
  design procedure — the only philosophy that survives as prose.
- **DC-3:** The `pre-pr-design-review` skill (both trees) is thinned to an adapter.
- **DC-4:** `PRE_PR_DESIGN_REVIEW.md` is deleted; `DESIGN_PRINCIPLES.md` is reduced to its
  §5 repo-map remnant (folded into the AGENTS map and deleted in 0.6);
  manifest/refs updated; revision bumped.

## 4. In scope

- **IS-1:** The `pre-pr-design-review.md` procedure, the smell-to-step rewrite, the design
creed, the skill thinning, the `PRE_PR_DESIGN_REVIEW.md` retirement, the
`DESIGN_PRINCIPLES.md` reduction to §5, the manifest updates, and the
sub-version's single PR.

## 5. Out of scope

- **OOS-1:** The §5 repo-map fold and final `DESIGN_PRINCIPLES.md` deletion (0.6).
- **OOS-2:** Planning migration (session A); close-out (0.3); version-audit (0.5).

## 6. Hard constraints

- **HC-1:** The design creed preserves the load-bearing philosophy (P1–P10 intent) that
  Phases 1–3 rely on; nothing normative is silently dropped.
- **HC-2:** Smells Fallow enforces are CHECK, not restated as manual STEP.
- **HC-3:** Steps live once; no design step duplicated into a skill body.
- **HC-4:** No application code changes; green resolver/drift/doc-ref at merge.

## 7. Decisions the session plan must resolve

- **PD-1:** Which smells are "Fallow already catches this" (CHECK) vs genuine judgment
  steps (STEP).
- **PD-2:** The exact contents and home of the one-paragraph design creed.

## 8. Acceptance criteria

- **AC-1:** Proves DC-1 and DC-3: `pre-pr-design-review.md` exists; the design skill points to it and carries
  only runtime mechanics.
- **AC-2:** Proves DC-1 and DC-2: `rg` shows zero feature-code paths in the design procedure (the timeless-doc
  rule).
- **AC-3:** Proves DC-4: `PRE_PR_DESIGN_REVIEW.md` is gone; `DESIGN_PRINCIPLES.md` is reduced to its §5
  remnant; references redirect.
- **AC-4:** Proves DC-1 through DC-4: `check_agent_drift.py` is green at the bumped revision.

## 9. Verification

- **V-1:** `check_agent_drift.py` green; `check_doc_refs.py` clean.
- **V-2:** `rg` confirms no feature-code path and no orphaned constitution reference.
- **V-3:** `pnpm verify` green.
- **V-4:** Changelog entry + `APP_VERSION` bump (3.10.0.4) ride this PR.

## 10. UX/operator gates

**UX gate: No.** Workflow/skill/constitution-decomposition change; no user
surface.

## 11. Baseline/hotspot boundary

Improves (collapses the multi-copy terminal/checkpoint rules toward single
owners; retires the most-polluted timeless doc). No hotspot contact.

## 12. Close-out behavior

Session B closes sub-version 3.10.0.4: full close-out on the shared branch — the
single PR for 3.10.0.4, Greptile loop, clean merge, production reconciliation.
Roadmap row 3.10.0.4 → terminal on merge evidence.
