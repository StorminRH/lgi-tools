## Session 3.10.0.3.1 — Author the canonical close-out procedure

**Sub-version:** 3.10.0.3 (Phase 0 — Documentation & lifecycle consolidation; session A of two, one branch)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.3
**UX gate:** No

## 1. Objective

Merge the whole close-out workflow into one canonical `docs/workflows/close-out.md`
written as actionable steps in the real sequence, with the recorded close-out
decisions baked in and the hand-rolled poll/merge recipes replaced by script
citations.

## 2. Current context and dependencies

- **DEP-1:** Depends on 3.10.0.2 (the `docs/workflows/` frame and canonical-form pattern
  exist). Session A of sub-version 3.10.0.3; session B (`3.10.0.3.2`) cuts over
  the skills and deletes the sources on the same branch.
- **DEP-2:** Today the close-out flow is spread across `SESSION_END`, `SELF_REVIEW`, and
  `PR_REVIEW`, with a hand-rolled Greptile poll and merge recipe duplicating
  `poll_pr_gate.py` / `merge_clean_pr.py`.

## 3. Done conditions

- **DC-1:** `docs/workflows/close-out.md` exists and merges `SESSION_END` + `SELF_REVIEW` +
  `PR_REVIEW` into one canonical procedure written as actionable steps in the
  real sequence: end-of-session gates, the judgment review (SELF_REVIEW's checks
  as mandatory numbered steps, not self-declared companion material), and the PR
  loop.
- **DC-2:** The hand-rolled Greptile poll and merge recipes are replaced by citing
  `poll_pr_gate.py` / `merge_clean_pr.py`; the duplicated coverage-backed Fallow
  re-run at PR open is stated once (required iff a commit intervened); the
  3.0/3.7-era narratives shrink to one line plus an archive pointer.
- **DC-3:** Recorded decisions are baked in: the contract `UX gate` marker is the pause
  authority with judgment as the off-lifecycle fallback (A2); `merge_clean_pr.py`
  is named the gate of record and the Greptile/CI/mergeability checklist stops
  being restated in prose (A3); final sessions mark `Execution status: Complete`
  only after merge (A10).

## 4. In scope

- **IS-1:** The three-into-one canonical procedure, the poll/merge script citations, the
A2/A3/A10 wording, and the changelog-form reference from 3.10.0.2. Source docs
are **not** deleted in this session (that is session B).

## 5. Out of scope

- **OOS-1:** The skill adapter split and the three source-doc deletions (session B).
- **OOS-2:** Planning/design migration (0.4); the `pre-pr-design-review` internals (0.4) —
  close-out references that skill as it stands.
- **OOS-3:** Any map/policy change (0.6).

## 6. Hard constraints

- **HC-1:** Steps live **once** in `docs/workflows/close-out.md`; no workflow step is
  duplicated into a skill body (Phase 0 non-goal).
- **HC-2:** Script-owned mechanics are cited, never re-described in prose.
- **HC-3:** No application code changes; green resolver/drift/doc-ref at merge.

## 7. Decisions the session plan must resolve

- **PD-1:** Whether the `version-audit` procedure or `close-out.md` owns the
  targeted-baseline-overwrite reconciliation step (currently duplicated across
  PRE_PR and VERSION_AUDIT).
- **PD-2:** The split line between "shared step" and "runtime mechanic" for the trickiest
  close-out sections (finalized in session B, previewed here).

## 8. Acceptance criteria

- **AC-1:** Proves DC-1: `docs/workflows/close-out.md` exists and contains the SELF_REVIEW judgment
  checks as mandatory numbered steps.
- **AC-2:** Proves DC-2: the Greptile/CI/mergeability checklist is stated once via script citation, not
  restated prose.
- **AC-3:** Proves DC-3: A2/A3/A10 wording is present and unambiguous.
- **AC-4:** Proves DC-1, DC-2, and DC-3: `check_agent_drift.py` / `check_doc_refs.py` green (sources still present,
  still referenced).

## 9. Verification

- **V-1:** `check_agent_drift.py` green; `check_doc_refs.py` clean.
- **V-2:** `pnpm verify` green.
- **V-3:** Manual read confirms one canonical sequence with no duplicated poll/merge
  recipe.

## 10. UX/operator gates

**UX gate: No.** Workflow-doc authoring; no user surface.

## 11. Baseline/hotspot boundary

Improves (a single canonical owner begins replacing three docs). No hotspot
contact; no-campaign decision stands.

## 12. Close-out behavior

Session A of a two-session, single-branch sub-version: commit in-branch with
`pnpm verify`; marks its plan `Execution status: Complete`; does not open the PR
or flip the roadmap row (session B does).
