## Session 3.10.5.1.1 — Documentation-truth remediation (AF-016)

**Sub-version:** 3.10.5.1
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.5.1
**UX gate:** No
**Execution profile:** Frontier autonomous coding agent
**Delivery unit:** One agent session, one shared sub-version branch, one sub-version PR
**Roadmap coverage:** §3.10.5.1 — the complete AF-016 correction set, the two operator-admitted related observations, and the cycle-1 audit records it carries to production
**Internal phases:** 1. Correct the five committed public-document claims and deliver the two admitted related observations in the same documents; 2. Correct the workspace citation and the two intra-zone claims through their owning generator and source comment, then regenerate; 3. Commit the cycle-1 audit records and publish the sub-version's release records
**Split triggers:** A correction turns out to require a behavior, configuration, boundary, or rendered-output change rather than a truthful restatement; a claim's live reality is genuinely ambiguous and needs an operator ruling before wording can be chosen

## 1. Objective

Version 3.10's thesis was that intended design becomes enforced design, and its
close audit found the residue that thesis predicts: eight committed claims that
describe a system the repository no longer is. This session closes AF-016 by
making every one of those claims state live reality at the site that owns it,
delivers the two related observations the operator admitted alongside it, and
carries the cycle-1 audit's own records to production, so the version can reach
a clean re-audit. It is its own sub-version because the version's delivery
roadmap was already terminal when the audit reopened it, and because a
documentation-truth correction set must ship as one reviewable unit.

## 2. Current context and dependencies

- **DEP-1:** `docs/version-audits/3.10/PLAN.md` (Approved 2026-07-27, cycle 1, audited ref `ba4828841a53de992e995c034a470efed50e3d6d`) Step 4 owns the confirmed AF-016 set; its eight enumerated claims are the complete scope, and its recorded required outcome is the acceptance bar. It is a frozen prompt: where it states a live figure, live code supersedes it — its schema-location census ("8 of 18 in features, 9 in `src/data/`") is already stale, and the live layout is 8 feature slices, 10 `src/data/<name>/` slices, and `src/db/auth-schema.ts`, re-exported as 19 modules by `src/composition/drizzle-schema.ts`, with no `src/platform` schema.
- **DEP-2:** Every delivery row of Phases 0–4 is SHIPPED and the version-final PR (#311) is merged; this sub-version reopens a terminal roadmap solely for audit remediation and schedules no elective work.
- **DEP-3:** The cycle-1 audit's own outputs exist as uncommitted working state on `main` — the replaced `docs/CODE_HEALTH_BASELINE.md`, the untracked cycle-1 `docs/version-audits/3.10/PLAN.md`, the reconciled `docs/backlog.md`, and the `docs/SCRATCHPAD.md` Now block. The audit plan §7 assigns them to the standard PR pipeline, so this sub-version's branch and PR are their delivery path. Because the audit plan is an untracked sole copy, the session's first commit on the branch packs those outputs together with the Phase 5 roadmap row, the contract and index rows, and the ledger and status mutations, before any worktree hygiene step that could delete untracked files.
- **DEP-4:** 3.10.3.3.1 already corrected the intra-zone wording in the rendered devlog legend row (`src/features/devlog/zone-map-geometry.ts`) and the SVG `<title>` (`ZoneMap.tsx`), each with regression coverage; the two surviving instances are the generator's mermaid legend comment and the `ZoneMap.tsx` header comment.
- **DEP-5:** `docs/architecture-map.md` and `src/features/devlog/architecture-map.generated.ts` are generated artifacts owned by `src/scripts/architecture-map.ts` and pinned by a byte-for-byte drift test; they are never hand-edited.
- **DEP-6:** The four v3.11 opening obligations and the six rationale-free carried suppressions are recorded in `docs/backlog.md` by the audit and are not this session's work.

## 3. Done conditions

- **DC-1:** The five committed public-document claims state live reality: `CONTRIBUTING.md`'s UI-primitive import rule, `.env.example`'s migration-script attribution, `.env.example`'s `EVE_SCOPES` home, `README.md`'s schema-source location claim, and `README.md`'s `pnpm build` audience.
- **DC-2:** No live document cites the v3.7 scaling-audit findings document as a readable live source; `docs/CONVEX.md` either cites a live home for that content or names it as archived in a form the reference checker sanctions.
- **DC-3:** Across live guidance documents, generated artifacts, and source comments, no statement claims the deny-by-default rule forbids imports within a single zone; every surviving statement is qualified to pairs of different zones. Frozen RECORD documents are excluded by OOS-7 and are not part of this state.
- **DC-4:** The architecture-map correction reaches its artifacts through the generator and a regeneration run, with the existing byte-for-byte drift test green and the generated module's emitted content otherwise unchanged.
- **DC-5:** No application behavior, configuration value, boundary rule, rendered user-facing string, or public API changes anywhere in the session.
- **DC-6:** The cycle-1 audit records — the replaced baseline at the audited ref, the cycle-1 audit plan with its evidence and its `Remediation in progress` status, the reconciled backlog, and the SCRATCHPAD state — are committed on the branch and truthful at PR open.
- **DC-7:** The two operator-admitted related observations are delivered: `README.md`'s architecture overview names the v3.10 responsibility layers (`src/composition/`, `src/platform/`, `src/transport/`), and `CONTRIBUTING.md`'s invariant list carries the typed problem contract and the same-origin mutation rail.
- **DC-8:** The sub-version's release records exist before the PR opens: the terminal `3.10.5.1` roadmap row, the matching `APP_VERSION`, the published changelog entry with any pending fragments absorbed, the session's `Complete` execution marker, and its as-built record.

## 4. In scope

- **IS-1:** The two committed public documents carrying wrong claims — `README.md` and `CONTRIBUTING.md` — and the committed environment template `.env.example`, at the claim sites AF-016 names.
- **IS-2:** `docs/CONVEX.md`'s citation of the absent scaling-audit document.
- **IS-3:** `src/scripts/architecture-map.ts`'s emitted legend line, the regeneration of both declared output artifacts, and any drift-test expectation the corrected text requires.
- **IS-4:** `src/features/devlog/components/ZoneMap.tsx`'s header comment.
- **IS-5:** The cycle-1 audit records listed in DEP-3, committed unchanged in substance except for the ledger and status mutations this remediation requires.
- **IS-6:** The two related observations the operator admitted to this bundle: `README.md`'s architecture-overview layer list and `CONTRIBUTING.md`'s invariant list.
- **IS-7:** The sub-version's own lifecycle and release records.

## 5. Out of scope

- **OOS-1:** The four v3.11 opening obligations (adoption-time `Version-start` capture, new wide-surface baseline rows, the `Source suppressions` metric split, the `verify_archive.py` `session-as-built` bundle-list extension) — all barred mid-version by the baseline's write-once rule and owned by the v3.11 opening.
- **OOS-2:** The six rationale-free carried `no-explicit-any` suppressions.
- **OOS-3:** Any Watch finding (AF-006/AF-007/AF-008) and any change to their baseline carriers or triggers.
- **OOS-4:** Any product API, schema, route, UI, or configuration change, and any refactor of the modules whose comments or docs are corrected.
- **OOS-5:** Documentation improvement beyond the AF-016 claim set and the two admitted observations in DC-7 — no reorganization, no new sections, no rewrite of surrounding prose.
- **OOS-6:** The cycle-2 re-audit and the version archive, which the resolver selects after this sub-version merges.
- **OOS-7:** Frozen RECORD documents — `docs/session-plans/**`, `docs/session-contracts/**`, `docs/version-audits/**` narrative history, `content/changelog/**`, and assembled `content/devlog/**` history — are never rewritten to satisfy a truth sweep, even where they quote a claim this session corrects elsewhere. `docs/session-plans/3.10/3.10.3.3.1.md`'s quoted pre-correction devlog wording is the known instance.

## 6. Hard constraints

- **HC-1:** Every correction is a truthful restatement of live reality, never a change to the system that makes the stale claim true.
- **HC-2:** Generated artifacts change only through their generator plus a regeneration run; a hand edit to `docs/architecture-map.md` or `architecture-map.generated.ts` is a defect even when it produces identical bytes.
- **HC-3:** The already-correct rendered legend row and SVG `<title>` keep their current wording and their regression coverage.
- **HC-4:** Any statement of deny-by-default anywhere in the change is scoped to pairs of different zones; the diagonal is never described as forbidden.
- **HC-5:** The baseline's frozen `Version-start` column stays byte-identical to `origin/main`; this session's only baseline change is whatever its own diff genuinely moves.
- **HC-6:** The AF-016 ledger row reaches `Delivered` only with terminal merge evidence; the audit status stays `Remediation in progress` until then.

## 7. Decisions the session plan must resolve

- **PD-1:** The exact truthful replacement wording for each of the eight claims, including how precisely `README.md` should describe the real schema layout — built from a re-run live census, never from the audit plan's stale figure — without turning a slice-boundary overview into a file inventory that will restale.
- **PD-2:** Whether `docs/CONVEX.md`'s scaling-audit citation points to an archived-document reference form already sanctioned by `check_doc_refs.py` or drops the path and keeps the distilled content as the live authority.
- **PD-3:** The placement and wording of the two admitted observations — which layers and invariants are named, at what altitude, and in which existing list — given that the operator ruled on 2026-07-27 that both ride this bundle and that neither may become a rewrite of its document.
- **PD-4:** Whether the corrected generator legend line requires any change to `src/scripts/architecture-map.test.ts`'s pinned expectations, and whether the `ZoneMap.tsx` comment correction is covered by an existing regression test or needs none.
- **PD-5:** The changelog composition for this sub-version, including how a remediation-only entry describes internal work, and which pending fragments exist at cutoff.

## 8. Acceptance criteria

- **AC-1:** Proves DC-1 and DC-2: each of the six corrected document claims is quoted in its new form beside the live evidence that makes it true — the `boundaries.rules` entry that actually denies the `ui` zone's imports, the migration script's real path, the `EVE_SCOPES` definition site, the aggregator's live schema census, the CI workflow's build absence, and the scaling-audit document's live status.
- **AC-2:** Proves DC-3 and DC-4: a repository-wide search for the deny-by-default claim, with only the OOS-7 RECORD paths excluded and that exclusion list shown, returns solely zone-qualified statements; and the architecture-map artifacts match a fresh generation run with the drift test green.
- **AC-3:** Proves DC-5: the session's complete diff contains no behavior, configuration, boundary, rendered-string, or public-API change, and `pnpm verify` passes with no test expectation loosened.
- **AC-4:** Proves DC-6: the branch carries the cycle-1 audit records, with the baseline-claims and watch-trigger checkers clean and release consistency green.
- **AC-5:** Proves DC-7: `README.md`'s overview lists the three named layers and `CONTRIBUTING.md`'s invariant list names the problem contract and the same-origin rail, each verified against the live owner it describes, with no other line of either document changed beyond the AF-016 corrections.
- **AC-6:** Proves DC-8: the terminal roadmap row, matching application version, published changelog entry, `Complete` marker, and as-built record all exist on the branch before the PR opens.

## 9. Verification

- **V-1:** Per-claim evidence pairs: the corrected or added text and the live observation that establishes it, covering the eight AF-016 claims and the two admitted observations.
- **V-2:** A regeneration run of the architecture map plus its drift test, showing no diff against the committed artifacts.
- **V-3:** A repository-wide deny-by-default wording sweep covering live guidance, generated artifacts, and source comments, with the OOS-7 RECORD exclusions listed explicitly rather than silently filtered.
- **V-4:** Lifecycle-record evidence: baseline-claims, watch-trigger, doc-reference, pending-changelog, release-consistency, and agent-drift checkers.
- **V-5:** The standard typecheck, zero-warning lint, coverage, and Fallow definition-of-done bundle, one `origin/main`-pinned final run, and current-head CI with a settled review gate.

## 10. UX/operator gates

- **G-1:** UX gate is No: no user-facing behavior or appearance changes, so no `ux-check` sweep or browser-review pause is required. A change that would alter a rendered string is a split trigger, not a gate to satisfy.
- **G-2:** The operator's PD-3 ruling on the two related non-finding observations is required before those observations may be touched.
- **G-3:** The merge gate stays operator-reserved; the session stops at an open, settled PR.

## 11. Baseline/hotspot boundary

Neutral by construction: comment and prose corrections plus one generator string
cannot move a metric. The session's expected baseline direction is unchanged
values apart from whatever the audit records it carries already state, and its
frozen `Version-start` column must remain byte-identical to `origin/main`.
Planning verifies against the current baseline that no hotspot, Watch carrier,
suppression count, or clone group moves, and that the committed cycle-1 baseline
still matches its audited ref.

## 12. Close-out behavior

This is the only session in sub-version 3.10.5.1 and it completes the
sub-version. Standard planned close-out applies on the shared
`lifecycle/3.10.5.1` branch: the session commits in-branch, publishes the
terminal roadmap row, `APP_VERSION`, and the changelog entry before the PR
opens, sets its own `Complete` execution marker only when that truthful state
exists on the branch, and authors the PR-numbered as-built record. Close-out
also marks AF-016 `Delivered` once terminal merge evidence exists. After the
merge the resolver selects the cycle-2 re-audit against the advanced `main`;
that audit and the version archive are outside this contract.
