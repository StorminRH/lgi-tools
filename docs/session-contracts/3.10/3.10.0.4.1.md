## Session 3.10.0.4.1 — Migrate planning to canonical procedures

**Sub-version:** 3.10.0.4 (Phase 0 — Documentation & lifecycle consolidation; session A of two, one branch)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.4
**UX gate:** No

## 1. Objective

Make planning one (or two-entry-point) canonical procedure the skills adapt over:
`SESSION_PLANNING` becomes `docs/workflows/plan-version.md` and
`plan-session.md`, with the recorded planning decisions baked in and the plan
skills thinned to adapters.

## 2. Current context and dependencies

- **DEP-1:** Depends on 3.10.0.2 (frame exists). Session A of sub-version 3.10.0.4; session
  B (`3.10.0.4.2`) migrates design review on the same branch. May run parallel to
  3.10.0.3 (disjoint procedures) though sequential keeps revision bumps clean.

## 3. Done conditions

- **DC-1:** `SESSION_PLANNING` becomes `docs/workflows/plan-version.md` and
  `plan-session.md` (its version/session halves) as actionable steps.
- **DC-2:** Recorded decisions are baked in: the "data/plumbing before UX" ordering
  heuristic lands in `plan-session.md`, decoupled from numbering language (A4-b);
  the Fallow-threshold-override escape hatch (former DESIGN_PRINCIPLES §4.1) is
  carried **nowhere** — split-or-cover is the only response to a complexity
  finding (A1).
- **DC-3:** The `plan-version` and `plan-session` skills (both trees) are thinned to
  adapters over the new procedures.
- **DC-4:** `SESSION_PLANNING.md` is deleted; `policy-manifest.json` `canonicalGuides`/
  `pairedSkills` and `check_doc_refs` allowlist are updated; the revision is
  bumped.

## 4. In scope

- **IS-1:** The `plan-version.md` + `plan-session.md` procedures, the A4-b + A1 wording, the
plan-skill thinning (both trees), the `SESSION_PLANNING.md` retirement, and the
manifest updates.

## 5. Out of scope

- **OOS-1:** Design-review migration + DESIGN_PRINCIPLES decomposition (session B).
- **OOS-2:** The §5 repo-map fold and final DESIGN_PRINCIPLES deletion (0.6).
- **OOS-3:** Close-out (0.3); version-audit (0.5).

## 6. Hard constraints

- **HC-1:** Removing §4.1 reintroduces no replacement pressure valve (A1 is explicit —
  none); a complexity finding is answered by split-or-cover, never a waiver.
- **HC-2:** Steps live once; no planning step duplicated into a skill body.
- **HC-3:** Both plain-English planning checkpoints (shape-first, summary-before-approval)
  survive the migration intact.
- **HC-4:** No application code changes; green resolver/drift/doc-ref at merge.

## 7. Decisions the session plan must resolve

- **PD-1:** Whether `plan-version` and `plan-session` are two procedures or one with two
  entry points.
- **PD-2:** Confirmation that removing §4.1 leaves no orphaned reference expecting a
  threshold-override path.

## 8. Acceptance criteria

- **AC-1:** Proves DC-1 and DC-3: `plan-version.md` + `plan-session.md` exist as actionable procedures; the plan
  skills point to them and carry only runtime mechanics.
- **AC-2:** Proves DC-2: no surviving canonical file names a Fallow-threshold-override escape hatch.
- **AC-3:** Proves DC-4: `SESSION_PLANNING.md` is gone and its references redirect.
- **AC-4:** Proves DC-1 through DC-4: `check_agent_drift.py` is green at the bumped revision.

## 9. Verification

- **V-1:** `check_agent_drift.py` green; `check_doc_refs.py` clean.
- **V-2:** `rg` shows no threshold-override escape-hatch language in the canonical set.
- **V-3:** `pnpm verify` green.

## 10. UX/operator gates

**UX gate: No.** Workflow/skill change; no user surface.

## 11. Baseline/hotspot boundary

Improves (collapses the multi-copy checkpoint rule toward a single owner). No
hotspot contact; no-campaign decision stands.

## 12. Close-out behavior

Session A of a two-session, single-branch sub-version: commit in-branch with
`pnpm verify`; marks its plan `Execution status: Complete`; does not open the PR
or flip the roadmap row (session B does).
