## Session 3.10.0.7 — Enforcement follow-through & anti-duplication check

**Sub-version:** 3.10.0.7 (Phase 0 — Documentation & lifecycle consolidation; one session)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.7
**UX gate:** No

## 1. Objective

Make the drift/parity machinery match the consolidated set, and add a check that
makes single-owner mechanical so the sprawl cannot silently regrow. **Phase 0
exit:** an agent's close-out reading path is one dispatched skill → one canonical
procedure in `docs/workflows/`, and no rule has two owners.

## 2. Current context and dependencies

- Depends on 3.10.0.3–0.6: the final consolidated set (procedures, thin adapters,
  map docs) must exist to police it.
- Each earlier Phase 0 slice already reconciles `policy-manifest.json`
  incrementally in its own change (the bootstrapping discipline); this slice
  performs the **final whole-set audit/rewrite** of `canonicalGuides`/
  `pairedSkills` against the settled corpus and adds the anti-duplication check.

## 3. Done conditions

- `policy-manifest.json` `canonicalGuides` and `pairedSkills` are rewritten
  against the `docs/workflows/` procedures, the thin adapters, and the map set;
  `bump_policy_revision.py` sets a fresh revision across all markers; the
  `check_doc_refs.py` historical/future allowlist is pruned to what the
  consolidated tree still needs.
- A new drift-gate check fails when the same normative sentence (a hash over
  normalized text) appears in more than one canonical file — spanning the
  `workflows/` procedures, the maps, and the two skill adapters.
- All `.agent-local/test_*.py` fixtures are updated to match.

## 4. In scope

The manifest rewrite, the revision bump, the allowlist prune, the
anti-duplication check plus its tests, and the fixture updates.

## 5. Out of scope

- Any further content change (the corpus is settled by 0.6).
- Architecture-phase work (Phases 1–3).

## 6. Hard constraints

- Exactly one new anti-duplication drift check — no other new parallel
  doc/checker machinery (Phase 0 non-goal).
- The uniqueness check must not trip on shared rule *names* or adapter
  boilerplate (false-positive tolerance is a resolved decision, §7).
- No application code changes; green resolver/drift/doc-ref at merge.

## 7. Decisions the session plan must resolve

- The normalization rule for the duplicate-sentence hash (whitespace/case/
  punctuation folding) and its false-positive tolerance.
- Which files are in-scope for the uniqueness check vs deliberately shared
  boilerplate.

## 8. Acceptance criteria

- A seeded duplicate normative sentence across two canonical files fails
  `check_agent_drift.py`.
- The full checker suite is green on the consolidated tree.
- `policy-manifest.json` references only the consolidated set; the allowlist is
  pruned.
- **Phase 0 exit condition holds:** the close-out reading path is one skill → one
  `docs/workflows/` procedure, and no rule has two owners.

## 9. Verification

- Seeded-duplicate → `check_agent_drift.py` red; consolidated tree → green.
- Full `.agent-local/test_*.py` suite green.
- `pnpm verify` green.
- Changelog entry + `APP_VERSION` bump (3.10.0.7).

## 10. UX/operator gates

**UX gate: No.** Checker/manifest change; no production surface.

## 11. Baseline/hotspot boundary

Neutral-to-Improves (checker code only; no production surface). No hotspot
contact; no-campaign decision stands.

## 12. Close-out behavior

Single-session sub-version and Phase 0 finale: full close-out —
`pre-pr-design-review`, PR, Greptile loop, clean merge, production
reconciliation. Roadmap row 3.10.0.7 → terminal on merge evidence. On its merge,
the resolver's first incomplete row advances to 3.10.1.1, re-dispatching
`plan-version` for the Phase 1–3 contracts.
