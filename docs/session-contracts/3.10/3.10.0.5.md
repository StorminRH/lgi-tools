## Session 3.10.0.5 — Migrate version-audit & de-duplicate self-contained skills

**Sub-version:** 3.10.0.5 (Phase 0 — Documentation & lifecycle consolidation; one session)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.5
**UX gate:** No

## 1. Objective

Make the remaining substantive workflow (version-audit) canonical, and make the
skills 0.5 owns — the audit trio plus the currently self-contained
`update-watch`/`ux-check`/`triage-issue` — thin adapters over one procedure. (The
whole-set "every skill is thin" state is Phase 0's exit condition, checked in 0.7.)

## 2. Current context and dependencies

- **DEP-1:** Depends on 3.10.0.2 (the strict baseline template it emits). Independent of
  0.3/0.4 procedure content.
- **DEP-2:** `update-watch` is byte-identical across both skill trees today (pure waste);
  `ux-check` and `triage-issue` are also inlined in both trees.

## 3. Done conditions

- **DC-1:** `VERSION_AUDIT.md` becomes `docs/workflows/version-audit.md` (its steps; the
  baseline template it emits was extracted in 0.2). The procedure writes the
  baseline in the **strict two-column metrics schema** from 0.2 — it updates the
  current column and records findings/rationale in the version-tagged audit
  report, never as prose in the baseline; at version close it captures the *next*
  version's frozen version-start snapshot (the one sanctioned write of that
  column).
- **DC-2:** The `version-audit`, `plan-version-audit`, and `plan-audit-remediation` skills
  are thinned to adapters.
- **DC-3:** The three currently-inlined skills are de-duplicated: `update-watch`,
  `ux-check`, and `triage-issue` have their shared procedure extracted to
  `docs/workflows/` with only genuine per-runtime mechanics (capture tooling,
  native syntax) left in each adapter.
- **DC-4:** `VERSION_AUDIT.md` is deleted; manifest/refs updated; revision bumped.

## 4. In scope

- **IS-1:** The version-audit procedure, the audit-skill thinning, the self-contained-trio
de-duplication, the `VERSION_AUDIT.md` retirement, and the manifest updates.

## 5. Out of scope

- **OOS-1:** Maps/policy (0.6); the anti-duplication check (0.7).
- **OOS-2:** Any change to the baseline *schema* (owned by 0.2; this only consumes it).

## 6. Hard constraints

- **HC-1:** The version-audit procedure emits only the strict data-only baseline; findings
  and rationale live in the version-tagged audit report, never as baseline prose.
- **HC-2:** Steps live once; both runtime adapters keep behavior parity.
- **HC-3:** No application code changes; green resolver/drift/doc-ref at merge.

## 7. Decisions the session plan must resolve

- **PD-1:** Whether `ux-check`/`triage-issue` retain enough genuine per-runtime difference
  to justify separate adapter bodies or collapse to near-empty pointers like
  `update-watch`.
- **PD-2:** The home of the audit's re-rank/classify steps that overlap `repo_measures.py`
  output (the STEP vs CHECK boundary).

## 8. Acceptance criteria

- **AC-1:** Proves DC-2 and DC-3: the skills 0.5 owns are thin adapters over their `docs/workflows/` procedure:
  `version-audit`, `plan-version-audit`, `plan-audit-remediation`, and the
  self-contained trio `update-watch`, `ux-check`, `triage-issue`. (The whole-set
  "every skill in both trees is thin" is Phase 0's **exit** condition, verified in
  0.7 once 0.3/0.4/0.6 have also landed — not asserted here, since 0.5 does not
  depend on 0.3/0.4.)
- **AC-2:** Proves DC-1: `docs/workflows/version-audit.md` emits the strict two-column baseline and
  routes findings to the version-audit report.
- **AC-3:** Proves DC-2 and DC-3: total words across both skill trees drop materially from today's 10,318.
- **AC-4:** Proves DC-4: `VERSION_AUDIT.md` is gone; references redirect; `check_agent_drift.py` green
  at the bumped revision.

## 9. Verification

- **V-1:** `check_agent_drift.py` green (parity + no orphan refs); `check_doc_refs.py`
  clean.
- **V-2:** Skill-tree word count recorded (materially below 10,318).
- **V-3:** `pnpm verify` green.
- **V-4:** Changelog entry + `APP_VERSION` bump (3.10.0.5).

## 10. UX/operator gates

**UX gate: No.** Workflow/skill change; no user surface.

## 11. Baseline/hotspot boundary

Improves (removes the last skill-tree duplication). No hotspot contact;
no-campaign decision stands.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR, Greptile
loop, clean merge, production reconciliation. Roadmap row 3.10.0.5 → terminal on
merge evidence.
