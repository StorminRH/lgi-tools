## Session 3.10.0.1 — Green the gate honestly (near decision-free)

**Sub-version:** 3.10.0.1 (Phase 0 — Documentation & lifecycle consolidation; one session)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.1
**UX gate:** No

## 1. Objective

Make the drift gate green on a fresh checkout by fixing what is genuinely wrong
and teaching the reference checker not to police RECORD (frozen history **and**
living state). The drift gate aggregates `check_doc_refs` and
`check_release_consistency`, so "green the gate" spans both: clear the doc-ref
errors, and clear the release-consistency error by opening this version's
changelog and bumping `APP_VERSION` (normal first-sub-version work). No
restructuring; one narrow, self-contained checker-scope decision (which RECORD
roots the reference checker exempts) — hence *near* decision-free rather than
strictly so.

## 2. Current context and dependencies

- First 3.10 session; v3.9 is archived. No dependency on any other 3.10 slice.
- **Measured entry state (this session's target):** `check_agent_drift.py`
  reports **21 errors**. Twenty are `check_doc_refs` errors, **all inside the
  active roadmap `docs/VERSION_3_10_PLAN.md`** — forward references to not-yet-
  created homes (`docs/workflows/`, `docs/templates/`, `docs/workflows/*.md`) and
  references to archived/frozen history (`docs/session-plans/3.9/3.9.1.4.md`,
  `docs/session-contracts/**`, `docs/version-audits/**`). One is
  `check_release_consistency`'s `content/changelog/v3.10.md: missing parseable
  changelog entry`, which the drift gate aggregates.
- The reference checker also flags negative/"do-not-create" path mentions (e.g.
  `src/CLAUDE.md`'s `.claude/rules/` sentence) and the `.claude/worktrees/`
  reference.
- Several map-doc facts have drifted from live code (enumerated in Done).

## 3. Done conditions

- `check_doc_refs.py` exempts RECORD paths from reference-policing, covering both
  **frozen history** (`docs/session-plans/**`, `docs/session-contracts/**`,
  `docs/version-audits/**`, `content/changelog/**`, `docs/SCRATCHPAD.md`) **and
  living state** (the active `docs/VERSION_*_PLAN.md`, `docs/backlog.md`,
  `docs/CODE_HEALTH_BASELINE.md`). Living state is exempted because it legitimately
  describes future/archived destinations (e.g. `docs/workflows/` before 3.10.0.2
  creates it); this clears all 20 doc-ref errors — every one is inside the active
  roadmap — without editing a single historical or roadmap file.
- The checker no longer treats *negative* path references as broken (or
  `src/CLAUDE.md`'s sentence is reworded) so a "do not create `.claude/rules/`"
  mention stops registering as a dead path.
- The `.claude/worktrees/` reference resolves — either made legal (`.gitignore` +
  manifest `ignoredPaths`) or removed.
- `content/changelog/v3.10.md` is created with the 3.10.0.1 entry and `APP_VERSION`
  is bumped to 3.10.0.1, so `check_release_consistency` reaches its `pre-pr`
  signature and its error (aggregated into the drift gate) clears. This is normal
  first-sub-version release work, not a rewrite of frozen history.
- Genuinely stale map-facts are corrected in their owning files: the non-existent
  `@convex-dev/eslint-plugin` "lint-enforced" claim (`CONVEX.md`); the superseded
  `engine.ts:208-211` "latent breach" note (the code is now a capped object
  literal); the "enforcement lands with 3.9.1.7" future-tense note (`AGENTS.md`);
  the eslint "gitignored working docs" comment and the type-scale lint message
  that omits `text-hero` (both **comment/message-only** edits in
  `eslint.config.mjs`; executable lint behavior is unchanged).
- The `update-watch` skill is added to the `AGENTS.md` skill catalog.

## 4. In scope

The `check_doc_refs.py` behavior changes (RECORD exemption spanning frozen
history + living state; negative-reference handling), the one real
`.claude/worktrees/` fix, the enumerated map-fact corrections in their owning
files (including the two **comment/message-only** edits in `eslint.config.mjs`),
the missing `update-watch` catalog entry, and the first-sub-version release
bootstrap (`content/changelog/v3.10.md` + `APP_VERSION` → 3.10.0.1). Same-change
`policy-manifest.json` touch only if the worktrees fix or exemption is
manifest-declared.

## 5. Out of scope

- AF-013 (owned by the v3.9 close-out audit that overwrote the baseline — not
  this version).
- The already-fixed DESIGN_PRINCIPLES exemplars (`auth/queries.ts` /
  `PricingContextValue` no longer appear in the tree — dead scope inherited from
  the audit's earlier HEAD).
- Any doc merge, any recorded Group-A decision, any skill restructure (those are
  3.10.0.2+).

## 6. Hard constraints

- **Frozen history is never rewritten to satisfy a checker.** Prior versions'
  `docs/session-plans/**`, `docs/session-contracts/**`, `docs/version-audits/**`,
  historical `content/changelog/*.md`, and `docs/SCRATCHPAD.md` history are
  exempted, not edited. The active version's own changelog
  (`content/changelog/v3.10.md`) is created/updated as normal release work — that
  is not a history rewrite.
- No application behavior changes. The only code touched is `.agent-local/`
  checkers and **comment/message-only** edits (map docs + `eslint.config.mjs`);
  no executable lint rule changes.
- Shared-policy changes bump `.agent-local/policy-manifest.json` and both skill
  trees' `shared-policy-revision` markers in the same change.

## 7. Decisions the session plan must resolve

- Whether the RECORD exemption (frozen-history + living-state roots) is a checker
  allowlist or a manifest-declared `recordPaths` set the checker reads, and the
  exact living-state root set (active `VERSION_*_PLAN.md` + `backlog.md` +
  `CODE_HEALTH_BASELINE.md`).
- Whether `.claude/worktrees/` becomes legal (gitignore + `ignoredPaths`) or the
  reference is removed.

## 8. Acceptance criteria

- `python3 .agent-local/check_doc_refs.py` returns zero errors on a fresh
  checkout.
- After this session's changelog + `APP_VERSION` land,
  `python3 .agent-local/check_agent_drift.py` is green (both aggregated lifecycle
  checkers pass; the 21 entry-state transients are cleared).
- No frozen-history file is rewritten; the active v3.10 changelog creation is the
  only `content/changelog/**` change.
- Each enumerated stale map-fact is corrected in its owning file.

## 9. Verification

- `check_doc_refs.py` zero errors; after the changelog + `APP_VERSION` bump,
  `check_release_consistency` is `pre-pr`-green and `check_agent_drift.py` is
  green.
- `pnpm lint` behavior unchanged (the `eslint.config.mjs` edits are
  comment/message text only).
- `pnpm verify` green (docs/checker/release-bootstrap change; still required
  before commit).

## 10. UX/operator gates

**UX gate: No.** Checker + map-doc facts only; the session self-finishes through
close-out with no operator pause.

## 11. Baseline/hotspot boundary

Neutral; touches no measured production surface and no hotspot. The version's
explicit no-campaign decision stands (empty queue; AF-006/007/008 remain
Watch-only, untouched here).

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR, Greptile
loop, clean merge, production reconciliation. Roadmap row 3.10.0.1 → terminal on
merge evidence. This session opens the v3.10 changelog and clears the benign
new-version release-consistency transient.
