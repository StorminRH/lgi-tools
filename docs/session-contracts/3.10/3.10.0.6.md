## Session 3.10.0.6 — Maps & policy single-owner

**Sub-version:** 3.10.0.6 (Phase 0 — Documentation & lifecycle consolidation; one session)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.6
**UX gate:** No

## 1. Objective

Make the surviving map/policy layer state each rule once and execute the recorded
doc dispositions, including the final deletion of `DESIGN_PRINCIPLES.md`.

## 2. Current context and dependencies

- Depends on 3.10.0.2–0.5: the precedence order references the final set, and
  `DESIGN_PRINCIPLES`'s steps must have migrated first (0.4).
- Root `AGENTS.md` currently restates source/UI policy that `src/AGENTS.md` owns.

## 3. Done conditions

- `src/AGENTS.md` becomes the sole owner of source/UI policy; the render-mode
  ladder, UI-wrapper list, route-registration, and hex/CSSOM rules are removed
  from root `AGENTS.md` and pointed at src.
- The seat→effort mapping gets one owner (`CLAUDE.md` as runtime mechanics; root
  AGENTS points there).
- The A7 global precedence order (AGENTS → workflow procedures/skills, with the
  design creed as the architecture constitution) is stated once in root AGENTS.
- `DESIGN_PRINCIPLES §5` folds into the AGENTS codebase map, and
  `DESIGN_PRINCIPLES.md` is deleted.
- `CONTRIBUTING.md` keeps its human-facing role; only drifted shared rules are
  aligned — the A6 data-slice hedge (plus a pointer to `.fallowrc.json` as the
  exception registry), the A8 removal of the React Flow / dnd-kit list (defer to
  `src/AGENTS.md`), and the understated CI-gate list (B13).
- `PRIMITIVE_LEDGER.md` is archived (its per-primitive rails already live in
  `eslint.config.mjs` / `.fallowrc.json`; confirm no going-forward doc needs its
  ownership rows first). `DATA_SOURCES.md` and `AGENT_TOOLING.md` are left
  unchanged (Ryan-deferred); `docs/security/*` untouched.

## 4. In scope

Root/src AGENTS dedup, the seat→effort single-owner move, the A7 precedence
paragraph, the §5 fold + `DESIGN_PRINCIPLES.md` deletion, the three CONTRIBUTING
alignments, the `PRIMITIVE_LEDGER.md` archive, and the manifest updates.

## 5. Out of scope

- `DATA_SOURCES.md` / `AGENT_TOOLING.md` (Ryan-deferred); any CONTRIBUTING
  slimming beyond drifted rules; `docs/security`.
- The anti-duplication check (0.7).

## 6. Hard constraints

- Root AGENTS no longer restates any rule `src/AGENTS.md` owns (single-owner).
- No reopening a resolved Group-A decision without a recorded reason.
- No application code changes; green resolver/drift/doc-ref at merge.

## 7. Decisions the session plan must resolve

- The exact precedence wording and its home paragraph in root AGENTS.
- Confirmation that `PRIMITIVE_LEDGER`'s per-primitive ownership rows are
  unneeded going forward before archiving.

## 8. Acceptance criteria

- Root AGENTS no longer restates src rules; the seat→effort mapping has one
  owner.
- `DESIGN_PRINCIPLES.md` and `PRIMITIVE_LEDGER.md` are out of the standing set
  and in the archive with references redirected.
- The three CONTRIBUTING alignments are made; nothing else in CONTRIBUTING is
  slimmed.
- `check_agent_drift.py` green at the bumped revision; `check_doc_refs.py` clean.

## 9. Verification

- `check_agent_drift.py` green; `check_doc_refs.py` zero dead references.
- `rg` confirms root AGENTS holds no src-owned rule copy.
- `pnpm verify` green.
- Changelog entry + `APP_VERSION` bump (3.10.0.6).

## 10. UX/operator gates

**UX gate: No.** Map/policy doc change; no user surface.

## 11. Baseline/hotspot boundary

Improves (removes the last large prose duplications; two fewer standing docs).
No hotspot contact; no-campaign decision stands.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR, Greptile
loop, clean merge, production reconciliation. Roadmap row 3.10.0.6 → terminal on
merge evidence.
