## Session 3.10.0.3.2 — Close-out skill adapters & source retirement

**Sub-version:** 3.10.0.3 (Phase 0 — Documentation & lifecycle consolidation; session B of two, one branch)
**Master plan:** `docs/VERSION_3_10_PLAN.md` §3.10.0.3
**UX gate:** No

## 1. Objective

Thin both runtime close-out skills to adapters over `docs/workflows/close-out.md`,
delete the three source docs, and update the enforcement machinery so the drift
gate is green on the consolidated set.

## 2. Current context and dependencies

- **DEP-1:** Depends on session A (`3.10.0.3.1`, same branch): `docs/workflows/close-out.md`
  exists with the canonical procedure.
- **DEP-2:** The `.claude` and `.agents` close-out skills differ by ~111 lines today; most
  of that is shared steps that now live in the canonical procedure.

## 3. Done conditions

- **DC-1:** The `.claude` and `.agents` close-out skills are split into thin adapters over
  the canonical procedure: shared steps point to `workflows/close-out.md`; only
  genuine per-runtime mechanics remain (native task-lists vs `codex exec`).
- **DC-2:** `SESSION_END.md`, `SELF_REVIEW.md`, and `PR_REVIEW.md` are deleted;
  `policy-manifest.json` `canonicalGuides`/`pairedSkills` and the
  `check_doc_refs.py` allowlist are updated; the revision is bumped.
- **DC-3:** No reference to the deleted docs survives outside frozen RECORD paths.

## 4. In scope

- **IS-1:** The skill adapter split (both trees), the three source-doc deletions, the
`canonicalGuides`/`pairedSkills` + `check_doc_refs` allowlist updates, the
revision bump, and the sub-version's single PR.

## 5. Out of scope

- **OOS-1:** Any further close-out *content* change (settled in session A).
- **OOS-2:** Planning/design migration (0.4); map/policy change (0.6).

## 6. Hard constraints

- **HC-1:** Both runtime adapters retain behavior parity; only per-runtime mechanics
  differ (no shared step duplicated into a skill body).
- **HC-2:** Shared-policy change bumps the revision and both trees' markers in the same
  change.
- **HC-3:** No application code changes; green resolver/drift/doc-ref at merge.

## 7. Decisions the session plan must resolve

- **PD-1:** The final "shared step" vs "runtime mechanic" boundary for the trickiest
  close-out sections.
- **PD-2:** Whether any close-out reference in other surviving docs needs a redirect vs
  deletion.

## 8. Acceptance criteria

- **AC-1:** Proves DC-1: both close-out skills point to `workflows/close-out.md` and carry only runtime
  mechanics.
- **AC-2:** Proves DC-2 and DC-3: `SESSION_END.md` / `SELF_REVIEW.md` / `PR_REVIEW.md` are gone and their
  references redirect (`check_doc_refs.py` clean).
- **AC-3:** Proves DC-1, DC-2, and DC-3: `check_agent_drift.py` green at the bumped revision;
  `python3 .agent-local/check_agent_drift.py` reports parity between trees.

## 9. Verification

- **V-1:** `check_agent_drift.py` green; `check_doc_refs.py` zero dead references.
- **V-2:** `pnpm verify` green.
- **V-3:** Changelog entry + `APP_VERSION` bump (3.10.0.3) ride this PR.

## 10. UX/operator gates

**UX gate: No.** Skill/enforcement change; no user surface.

## 11. Baseline/hotspot boundary

Improves (one canonical owner replaces three docs plus two skill copies;
~6.5k words of duplicated process removed). No hotspot contact.

## 12. Close-out behavior

Session B closes sub-version 3.10.0.3: full close-out on the shared branch — the
single PR for 3.10.0.3, Greptile loop, clean merge, production reconciliation.
Roadmap row 3.10.0.3 → terminal on merge evidence.
