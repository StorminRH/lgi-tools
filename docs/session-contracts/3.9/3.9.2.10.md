## Session 3.9.2.10 — Token-vend scope cleanup (PL-013 Delete)

**Sub-version:** 3.9.2.10 (Phase 2 — Primitive lifecycle, approved extension; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.2.10
**UX gate:** No

## 1. Objective

Delete the unused scopes field and divergent parser from the internal EVE
token-vending contract while preserving the authoritative stored-scope
health path.

**Verdict & evidence.** *Delete* (PL-013, approved 2026-07-17).
`EveTokenOkResponse.scopes` is returned by the internal token-vend route but
discarded by its only production consumer in `convex/lib/characterSync.ts`;
its private whitespace-only parser diverges from the comma-or-space
stored-scope decoder in `scope-health.ts`. Full Fallow found zero unused
files or exports, making this field-level dead contract the sole
zero-consumer candidate.

## 2. Current context and dependencies

- 3.9.2.7 recorded and Ryan approved PL-013. Independent of the other
  approved extension rows.

## 3. Done conditions

- `EveTokenOkResponse` and the internal token route vend only the access
  token data their Convex consumer reads.
- The token-service whitespace-only scope parser is removed.
- Route and contract tests pin the narrowed response.
- `scope-health.ts` remains the sole decoder for persisted comma-or-space
  scope strings and its behavior is unchanged.

## 4. In scope

The internal token response type, token service, route, Convex consumer
typing, and focused tests.

## 5. Out of scope

- Requested EVE SSO scopes and the stored account scope format.
- Token encryption or refresh behavior.
- Scope-health policy and any auth UI.

## 6. Hard constraints

- Auth trust boundaries unchanged: the route's user-plus-character
  ownership requirement and encrypted token custody are untouched.
- Characterize the contract before deletion; the narrowing must be proven
  consumer-safe, not assumed.
- Convex-bundling gotcha applies to any touched chain reachable from
  `convex/`: no Next runtime imports (the production Convex push is the
  only place that fails).

## 7. Decisions the session plan must resolve

- The exact contract characterization needed before deletion.
- Whether the Convex consumer can drop its response cast as part of the
  same narrowing.

## 8. Acceptance criteria

- Focused token-service, route, and scope-health tests green.
- Fallow reports no newly unused surface.

## 9. Verification

- `pnpm verify` + coverage-backed Fallow green.
- Changelog entry + APP_VERSION bump (3.9.2.10).

## 10. UX/operator gates

**UX gate: No.** Session self-finishes through close-out.

## 11. Baseline/hotspot boundary

Improves — deletes a zero-consumer contract field and one divergent
decoder; auth trust boundaries remain unchanged. No-campaign decision
preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.2.10 →
terminal on merge evidence; ledger row PL-013 → Delivered in the same
close-out.
