## Session 3.9.2.7 — Primitives-scoped audit & ledger

**Sub-version:** 3.9.2.7 (Phase 2 — Primitive lifecycle; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.2.7
**UX gate:** No

## 1. Objective

Map the system's full primitive surface, verify this arc's outcomes held, and
report the remaining create/combine/delete/expand verdicts — with Ryan
deciding what, if anything, extends the version.

## 2. Current context and dependencies

- **Last in the Phase 2 arc** — 3.9.2.1–6 delivered so the map records
  end-state, not mid-migration.
- Survey is read-only and Graphify-first.

## 3. Done conditions

- `docs/PRIMITIVE_LEDGER.md` exists as living overwrite-in-place state (the
  baseline's pattern): part 1 the **primitive map** — every primitive by area
  (UI/design, API/backend, data, infra/platform, agent tools/workflow,
  auth/trust) with its owning module and the decision it hides; part 2 the
  **verdict table** — stable `PL-NNN` rows
  (`| ID | Area | Class | Evidence | Proposed end-state | Est. size | Status |`)
  recording this arc's delivered verdicts as Delivered and every *newly found*
  gap/overlap as Proposed.
- The map section records each primitive's enforcement rail (or `none`), so
  primitive↔rail coverage is diffable by future audits and an unrailed
  primitive is a visible finding class.
- Required judgments run and recorded: the telemetry-vs-domain-events
  vocabulary boundary (expected Keep with the distinction written);
  `lib/sync-engine.ts` vs `convex/engine.ts` vs `lib/live-dataset.ts` overlap
  (one live-sync story or three?); the 3.9.2.3 trigger-layer verdict if it
  reported rather than implemented; any primitive with zero remaining
  consumers (Delete candidates); any pass-through wrapper (P3 sweep).
- The audit *reports*; it implements nothing. Proposed rows either (a) get
  Ryan's approval and append `3.9.2.N` rows via the master-plan-amendment
  path, (b) route to `docs/backlog.md` citing their `PL-NNN`, or (c) are
  rejected with reason in the ledger. **The session pauses on the report for
  Ryan's decision before any amendment.**

## 4. In scope

Read-only survey (Graphify-first) of `src/`, `convex/`, `scripts/`, and the
agent workspace; the ledger document; the approved disposition of every
Proposed row.

## 5. Out of scope

- Implementing any Proposed verdict.
- Re-auditing code health (the version-close audit owns that; this audit is
  primitives-scoped and does not touch the AF ledger or baseline schema).

## 6. Hard constraints

- The primitives audit never touches the AF ledger, the baseline schema, or
  the version-close audit's authority — it is scoped to the primitive map and
  `PL-NNN` verdicts.
- The ledger proposes; Ryan disposes. No row beyond 3.9.2.7 exists without his
  recorded approval of its `PL-NNN` verdict.
- Primitive verdicts require a concrete existing consumer or observed
  repetition; "the v4.0 mapper will want it" is never sufficient evidence.

## 7. Decisions the session plan must resolve

- Ledger granularity (what counts as one primitive vs. a family — propose:
  one row per exported surface an agent must choose between).

## 8. Acceptance criteria

- The committed ledger contains the complete area map with rail column, the
  arc's verdicts as Delivered (including AF-009's closure framing from
  3.9.2.2), and every Proposed row dispositioned by Ryan (approved / backlog
  with `PL-NNN` / rejected with reason).
- The required judgments each have a recorded verdict.

## 9. Verification

- The committed ledger; the recorded operator dispositions.
- `pnpm verify` green; drift gate green if guides/skills changed.
- Changelog entry + APP_VERSION bump (3.9.2.7).

## 10. UX/operator gates

**UX gate: No.** Operator pause: the session pauses on the Proposed-rows
report for Ryan's disposition before any master-plan amendment.

## 11. Baseline/hotspot boundary

Neutral; read-only against measured surfaces. No-campaign decision preserved;
any campaign-shaped Proposed row goes through Ryan's disposition, never
absorbed.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.2.7 →
terminal on merge evidence; approved extensions (if any) enter via the
master-plan-amendment path with new contracts from `plan-version`.
