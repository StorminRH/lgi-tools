## Session 3.9.2.3.2 — Runtime freshness gate & staleness collapse

**Sub-version:** 3.9.2.3 (Phase 2 — Primitive lifecycle; session 2 of 2, one branch)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.2.3
**UX gate:** No

## 1. Objective

One runtime freshness verdict derived from the registry entry serves every
staleness decision: the six mirrored gates collapse onto it, the
market-prices/history on-view paths derive from it, and the trigger-layer
question gets a recorded two-decomposition comparison.

**Verdict & evidence.** *Expand/Combine* the staleness gates onto the 3.9.2.3.1
registry. Six mirrored copies of the same gate exist — five per-feature
`staleness.ts` files (skill-queue, industry-jobs, owned-assets,
owned-blueprints, owned-structures) plus the affiliation gate in
`membership.ts` — each an `X_TTL_MS` constant and an `isXStale(refreshedAt,
now)` with identical null-handling, each comment declaring it "mirrors" the
previous one; plus two parallel on-view trigger implementations
(`market-prices/refresh-on-view.ts` 232 LOC, `market-history/` 142 LOC, and
their client hooks); plus three freshness models coexisting with no declared
chooser.

## 2. Current context and dependencies

- Depends on 3.9.2.3.1 (the registry and entries) on the same branch.

## 3. Done conditions

- **The runtime gate:** one clock-injected freshness verdict in `src/lib/`
  reading the entry's model + effective TTL; the six mirrored gates collapse
  onto it; the row-carried `stale_after` model consumes the entry at write
  time so there is no second source of truth.
- Zero per-feature staleness modules remain (grep-verified) and every gated
  read derives its verdict from the entry; behavior preserved — same TTL
  values, same refresh timing.
- **Judged tier — the trigger layer:** the SESSION_PLANNING Step 4
  two-decomposition comparison runs for the on-view trigger family
  (market-prices/history refresh-on-view vs. the owner-sync engine's trigger):
  one shared trigger primitive vs. Keep-with-written-distinction. Budget
  interaction and concurrency differ; if the comparison shows the unification
  is campaign-shaped, deliver the recorded comparison and report a Proposed
  row for the 3.9.2.7 audit instead of absorbing it.

## 4. In scope

The `src/lib/` verdict function, the five `staleness.ts` files + affiliation
gate migration, write-time `stale_after` derivation, the
market-prices/history derivation, the trigger comparison, rail parts 2–3.

## 5. Out of scope

- Moving datasets between stores; changing TTL values or refresh cadences.
- The pricing feature surface (AF-005 fence — `planner-contexts.tsx`,
  `PricingProvider` untouched). Migrating the pricing feature's staleness
  *consumption* onto the gate is a legitimate Proposed row for the 3.9.2.7
  audit, not this session's scope.
- Next.js `'use cache'` profiles.
- Implementing the trigger-layer unification if the comparison says
  campaign-shaped.

## 6. Hard constraints

- **Rail (parts 2–3):** (2) naming-pattern lint banning
  `*_TTL_MS`/staleness-window const declarations outside the leaf; (3)
  restricted-import so read paths import the verdict only from the leaf
  module. Part 1 (the gate test) stays green.
- Full pattern: migrate every consumer; zero old-path call sites at close; no
  compatibility façade.
- Behavior-preserving characterization before/after (P9).

## 7. Decisions the session plan must resolve

- The verdict's exact signature (one `isStale(entry, refreshedAt, now)` vs.
  per-dataset bound gates — P1 caller-knowledge count decides).
- The trigger-comparison scope fence (what makes it campaign-shaped).

## 8. Acceptance criteria

- Grep shows zero `*_TTL_MS` constants and zero `staleness.ts` files outside
  the registry/gate.
- Rails red on seeded violations (stray TTL constant; staleness import from a
  non-leaf path), green live.
- Unchanged-behavior characterization green before/after migration.
- The recorded trigger comparison exists with its verdict implemented (if
  Floss-sized) or reported as a Proposed row.

## 9. Verification

- `pnpm verify` + coverage-backed Fallow green; drift gate green.
- Grep evidence + seeded rail demonstrations in PR notes.
- Changelog entry + APP_VERSION bump (3.9.2.3).

## 10. UX/operator gates

**UX gate: No.** Session self-finishes through close-out.

## 11. Baseline/hotspot boundary

Improves — deletes six mirrored modules and their drift surface and adds two
rails. **Hotspot fence:** AF-005 pricing surfaces untouched; the pricing scope
fence is the hotspot guard. No-campaign decision preserved.

## 12. Close-out behavior

Slice-final session: full close-out — `pre-pr-design-review`, single PR for
the 3.9.2.3 branch, Greptile loop, clean merge, production reconciliation.
Roadmap row 3.9.2.3 → terminal on merge evidence.
