## Session 3.9.5.1 — Audit-hygiene remediation

**Sub-version:** 3.9.5.1 (Phase 5 — Audit remediation; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.5.1
**UX gate:** No
**AF findings:** AF-010, AF-011, AF-012 (documentation truth); AF-014, AF-015
(behavior-preserving code clarity).

> **Trivial-tier session (operator-directed).** This bundles three small,
> low-risk change axes to avoid disproportionate ceremony. The session plan is a
> lightweight confirmation of this contract, not a fresh design exercise; no
> heavy design-twice or adversarial-review apparatus is warranted for a
> mechanical, behavior-preserving diff. Session 3.9.5.2 keeps the full treatment.

## 1. Objective

Reconcile three stale documentation surfaces to shipped reality and resolve two
small code-clarity defects in one behavior-preserving PR, so the v3.9 archive
records truth and each concept has one authoritative home. Principle outcomes:
docs-truth (AF-010/011/012), P2 one-owner (AF-014), P2/P7 name clarity (AF-015).

## 2. Current context and dependencies

- Cycle-1 audit ledger AF-010/011/012/014/015; `docs/SCRATCHPAD.md` §3.9.3.4
  measured readout is the AF-010 evidence source.
- **AF-014.** `src/lib/eve-image.ts` owns `EveImageSize = 32|64|128|256|512` +
  `characterPortraitUrl`/`corporationLogoUrl`; `src/components/eve-image.tsx`
  separately owns `EVE_IMAGE_SIZES = [32..1024]`, its own `EveImageSize`,
  `MAX_EVE_IMAGE_SIZE`, `FAMILY_SIZES`, `snapEveImageSize`, `eveImageUrl`,
  `EveImage`. `src/components/character-portrait.tsx` is the dual-importer (lib
  `characterPortraitUrl` + component `EveImage`); `src/features/auth/eve-sso.ts`
  imports the lib type only. Test `src/components/eve-image.test.ts`; no lib
  test. `src/components` → `src/lib` imports are allowed.
- **AF-015.** `src/features/industry-planner/saved-plans-view.ts` exports the
  render-verdict type `SavedPlansState` (`blank|empty|list`) with **zero external
  importers**; `use-saved-plans.ts` exports the controller `SavedPlansState`.
- No dependency on 3.9.5.2. Strict-sequenced first in Phase 5.

## 3. Done conditions

- **AF-010:** `VERSION_3_9_PLAN.md` §3.9.3.4 narrative states the measured root
  cause (bloated `.next` watcher-spin + memory starvation), the already-shipped
  lazy render, and the delivered sample-mode outcome; roadmap status/scope
  unchanged.
- **AF-011:** `README.md` no longer presents `pnpm build` as a useful local
  command (removed or reframed Vercel/CI-only); `pnpm verify` remains local DoD.
- **AF-012:** `docs/DESIGN_PRINCIPLES.md` P7 cites only files/symbols verified
  present; principle prose byte-unchanged except the citations.
- **AF-014:** `src/lib/eve-image.ts` owns the single size ladder + per-family
  server-support map + pure snapping policy, cap derived from the ladder (no
  separate `MAX_EVE_IMAGE_SIZE`); `src/components/eve-image.tsx` keeps only the
  `next/image` adapter/rendering; grep shows one `EveImageSize`/ladder
  definition; all existing emitted URLs are byte-identical, locked by
  characterization tests written first.
- **AF-015:** the render-verdict type in `saved-plans-view.ts` is file-local (no
  `export`); the controller `SavedPlansState` is unchanged; no runtime change;
  existing view tests retained.
- APP_VERSION bump + `content/changelog/v3.9.md` entry.

## 4. In scope

`VERSION_3_9_PLAN.md` §3.9.3.4, `README.md`, `DESIGN_PRINCIPLES.md` P7;
`src/lib/eve-image.ts`, `src/components/eve-image.tsx`,
`src/components/eve-image.test.ts` + a new lib test, any importer whose
type-import path changes; `saved-plans-view.ts`; APP_VERSION + changelog.

## 5. Out of scope

Any user-visible behavior change; new image families/sizes; `next/image`
rendering behavior; renaming the controller type; archived v3.8 material; the P7
principle text; other §3.9.x narratives; any AF-013 (coverage) work; any
cross-axis cleanup beyond the five findings.

## 6. Hard constraints

- Behavior-preserving (P9). **Batching exception recorded:** this session
  deliberately bundles three change axes (docs / image ownership / saved-plan
  type) to avoid disproportionate ceremony. Ship isolated commits, run focused
  tests after each code step, and do no cross-axis cleanup: (1) docs;
  (2) AF-014 characterization tests; (3) AF-014 the explicit 1024 interface
  expansion on the lib builders (server-capability-justified — portraits/logos
  already support 1024 via `FAMILY_SIZES`); (4) AF-014 behavior-preserving
  ownership move; (5) AF-015 type-only removal.
- AF-014: import direction respected (lib owns server-capability facts —
  ladder, family support, snapping; the component consumes); characterize both
  URL builders' defaults and every supported size.
- AF-012: citation replacement only; does not cross the `docs/DESIGN_PRINCIPLES.md`
  §2 / P10 "amend the constitution only for a durable rule/number" bar.
- Docs edits change no roadmap status/scope; the 3.9.3.4 row stays SHIPPED.

## 7. Decisions the session plan must resolve

- AF-011: remove the `pnpm build` row vs reframe as Vercel/CI-only (recommend
  reframe with a one-line note; the script exists in `package.json`).
- AF-012: which live exemplars replace the citations (a rationale-dense module
  prologue + an interface-comment export, both verified present at plan time).
- AF-014: the exact lib/component boundary for `eveImageUrl`; whether a
  size-literal lint rail is warranted (default: no).
- AF-015: confirm zero external importers so file-local is safe (fallback:
  rename to `SavedPlansViewState`).

## 8. Acceptance criteria

- P7 exemplar paths resolve to existing files/symbols; README has no local
  production-build command; §3.9.3.4 matches the SCRATCHPAD readout.
- Exactly one `EveImageSize`/ladder definition (grep); characterization green
  with byte-identical URLs across both builders' defaults and all sizes.
- Render-verdict type unexported; no remaining `SavedPlansState` name collision;
  `tsc` green; view tests unchanged.
- `check_doc_refs.py` and the drift gate green.

## 9. Verification

- `pnpm verify`; `pnpm test:coverage` then
  `FALLOW_AUDIT_BASE=$(git rev-parse origin/main) pnpm fallow`.
- `pnpm exec tsc --noEmit --incremental false`.
- `python3 .agent-local/check_doc_refs.py --check`;
  `python3 .agent-local/check_agent_drift.py`;
  `python3 .agent-local/check_release_consistency.py --check --expect pre-pr`
  (and `--expect reconciled` for the post-merge lifecycle reconciliation commit).

## 10. UX/operator gates

**UX gate: No.** The two code changes are provably behavior-preserving (identical
emitted URLs; a type-only edit), so no browser review is required. Rendered
output is out of scope; **if the session plan finds any rendered-output change,
it stops for contract reapproval** rather than silently re-scoping this gate.

## 11. Baseline/hotspot boundary

Improves. Close-out reconciles the two baseline hotspot rows ("EVE image size
policy" AF-014; "Saved-plan state contracts" AF-015) to one owner each, and marks
AF-010/011/012/014/015 Delivered. No other hotspot row touched.

## 12. Close-out behavior

Standard close-out; isolated commits per §6; mark the five findings Delivered on
merge; update the two baseline hotspot rows.
