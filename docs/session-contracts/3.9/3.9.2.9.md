## Session 3.9.2.9 — UI wrapper import rail (PL-012 Expand)

**Sub-version:** 3.9.2.9 (Phase 2 — Primitive lifecycle, approved extension; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.2.9
**UX gate:** No

## 1. Objective

Mechanize the standing rule that feature and application code consume Base
UI and sonner only through `src/components/ui/`.

**Verdict & evidence.** *Expand* (PL-012, approved 2026-07-17). The current
tree keeps Base UI and sonner imports inside `src/components/ui/`, and
policy names that directory as the sole seam, but ESLint restricts only
direct `next/image`; no rail prevents a future feature import of Base UI or
sonner. The primitive ledger's UI-library row records package-import
exclusivity as `none (see PL-012)`.

## 2. Current context and dependencies

- 3.9.2.7 recorded and Ryan approved PL-012. Independent of the other
  approved extension rows.
- Precedent rails: the raw-`fetch`/raw-hex/`next/image` restricted-import
  patterns in `eslint.config.mjs`.

## 3. Done conditions

- Scoped restricted-import rules reject direct Base UI and sonner imports
  outside their shared wrappers, preserve the wrappers' required package
  access, and fail on seeded violations for both package families.
- Existing source remains green with no new exemption surface.

## 4. In scope

ESLint import restrictions, the exact wrapper exemptions, seeded rule tests
or equivalent lint evidence, and documentation references that name the
enforcement rail.

## 5. Out of scope

- Rebuilding UI primitives, changing wrapper APIs or appearance.
- Migrating already-compliant consumers.
- Adding a new UI library.

## 6. Hard constraints

- **Rail:** this slice *is* a rail — scoped restricted-import lint; its
  enforcement evidence is the seeded-red demonstration for both packages.
- `toast.tsx` remains the only sonner importer; the Base UI wrapper files
  remain the only `@base-ui/react` import seam (never the deprecated
  `@base-ui-components/react`).
- Zero-warning lint stays the gate; no new exemption surface.

## 7. Decisions the session plan must resolve

- The flat-config scope that allows only the true wrapper modules without
  exempting all of `src/components/ui/`.
- The durable seeded-red demonstration for each restricted package.

## 8. Acceptance criteria

- Both seeded direct-import violations fail lint.
- The live tree passes zero-warning lint.
- The primitive ledger's UI-library rail cell names the delivered rule.

## 9. Verification

- `pnpm verify` + coverage-backed Fallow green.
- Changelog entry + APP_VERSION bump (3.9.2.9).

## 10. UX/operator gates

**UX gate: No.** Policy enforcement only; session self-finishes through
close-out.

## 11. Baseline/hotspot boundary

Neutral-to-Improves; no measured source hotspot change expected.
No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.2.9 →
terminal on merge evidence; ledger row PL-012 → Delivered (rail cell
updated) in the same close-out.
