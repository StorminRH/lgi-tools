## Session 3.9.5.2 — Coverage remediation for untested modules

**Sub-version:** 3.9.5.2 (Phase 5 — Audit remediation; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.5.2
**UX gate:** No
**AF findings:** AF-013 (re-diagnosed Floss — coverage gap).

> **Re-diagnosis (operator-approved).** The cycle-1 audit diagnosed AF-013 as a
> Fallow comment-attribution defect. Reproduction proved otherwise: all six
> findings are coverage-driven CRAP (max cyclomatic 8 < 20; max cognitive
> 8 < 15), driven by genuinely zero test coverage in four modules; the 3.9.1.7
> comment migration merely pulled those files into the whole-version audit lens,
> where CRAP reads as "introduced" because the base ref has no coverage to
> attribute against. The honest fix is real coverage, not suppression. The audit
> ledger's AF-013 row is corrected to match.

## 1. Objective

Give the four genuinely-untested modules real behavioral test coverage so their
flagged functions drop below the CRAP cap and the version-start-pinned Fallow
audit passes truthfully — the code becomes tested and the signal is resolved
rather than masked. Principle outcome (P10 / AGENTS.md testing policy): add
meaningful behavioral coverage with no waiver, baseline, suppression, or padding.

## 2. Current context and dependencies

The pinned audit (`FALLOW_AUDIT_BASE=291ee78… pnpm fallow`) fails on 6 CRAP
findings (cyclomatic 5–8, all under raw thresholds; CRAP 30–72 from 0% coverage):

- `src/features/skill-queue/queries.ts` `saveCharacterSkills` (CRAP 72; DB
  insert/`onConflictDoUpdate`) — file 0/9 functions covered.
- `src/data/eve-data/ingest.ts` `streamInsert` + two arrows (42/42/30; SDE
  streaming insert) — 0/15.
- `src/data/eve-data/station-names.ts` `resolveNpcStationNames` (30;
  dependency-injected `db` param + ESI fetch + batched UPDATE) — 0/6.
- `src/db/skills-sync.ts` `nameIds` (30; a pure `Set`-collection transform inside
  a config object) — 0/19.

The coverage snapshot is DB-suite-inclusive (the `auth/` and `gsc/` trees show
real coverage), so the 0% on these four modules is genuine — they have no test.
The real-Postgres harness (`createDbTestHarness`, 3.9.2.1) is the standard for
DB-bound suites. This sub-version is the audit-gate blocker; strict-sequenced
after 3.9.5.1.

## 3. Done conditions

- Each flagged function has meaningful behavioral coverage: DB-harness
  (`*.db.test.ts`) suites for the DB-bound functions (`saveCharacterSkills`,
  `resolveNpcStationNames`, `streamInsert`/ingest arrows) and unit coverage for
  the pure transform (`nameIds`, extracted to a named function only if that makes
  it honestly testable — P9 behavior-preserving).
- Each flagged function's CRAP is below 30 in a fresh full-Postgres coverage run.
- The version-start-pinned audit (`FALLOW_AUDIT_BASE=291ee78… pnpm fallow` with
  fresh coverage) is green; no new introduced complexity/dead-code/duplication.
- No `.fallowrc` waiver/threshold/baseline, no lint suppression, no coverage
  padding (assertions exercise real behavior, not metric-gaming).
- APP_VERSION bump + `content/changelog/v3.9.md` entry.

## 4. In scope

New/extended co-located tests for the four modules (DB-harness suites + unit
tests); a minimal, behavior-preserving pure-logic extraction only where it makes
a function honestly unit-testable; APP_VERSION + changelog.

## 5. Out of scope

Any behavior change to the four modules beyond a behavior-preserving extraction;
touching Fallow config, `docs/VERSION_AUDIT.md`, or the audit plan's gate command
(the raw pinned command passes natively once coverage lands); a Fallow upgrade;
any custom attribution tooling; coverage for functions not driving a CRAP finding
(no padding); the 3.9.5.1 findings.

## 6. Hard constraints

- Tests exercise real behavior (P9 / testing policy); no assertions added solely
  to move the metric.
- DB suites use `createDbTestHarness`; direct `postgres()` construction and
  connection-string literals are lint-banned in `*.db.test.ts`.
- Any extraction is a separate behavior-preserving commit with the suite green
  before and after.
- The fix is coverage only — no waiver, baseline, suppression, or Fallow change.

## 7. Decisions the session plan must resolve

- Per function: DB-harness vs unit vs a small pure-logic extraction (recommended:
  harness for `saveCharacterSkills` / `resolveNpcStationNames` / `streamInsert` +
  ingest arrows; unit for `nameIds`).
- Whether `resolveNpcStationNames`'s ESI fetch is stubbed at the
  `fetchStationNames` seam or exercised through the ESI gate's test seam.
- The minimum honest coverage that clears CRAP < 30 per function without padding
  (verify with a fresh coverage run; do not target a number).

## 8. Acceptance criteria

- Fresh `pnpm test:coverage` shows each flagged function covered; a fresh
  `FALLOW_AUDIT_BASE=291ee78… pnpm fallow` verdict is `pass` with zero introduced
  complexity findings.
- No waiver/baseline/suppression/padding introduced (grep/inspection).
- `pnpm verify` and the drift gate green.

## 9. Verification

- `pnpm verify`; `pnpm test:coverage` then both
  `FALLOW_AUDIT_BASE=291ee78bb1f0231f06a021b910f1181ad8c39bff pnpm fallow` (the
  version-start audit gate — must pass) and
  `FALLOW_AUDIT_BASE=$(git rev-parse origin/main) pnpm fallow` (the ordinary
  close-out gate).
- `python3 .agent-local/check_agent_drift.py`;
  `python3 .agent-local/check_release_consistency.py --check --expect pre-pr`.

## 10. UX/operator gates

**UX gate: No.** Test-only plus at most a behavior-preserving extraction; no user
surface.

## 11. Baseline/hotspot boundary

Improves. Close-out flips the baseline "Version-start-pinned Fallow verdict"
Step-1 row Fail→Pass, moves the campaign-queue row (AF-013) to Delivered, and
updates the "Functions above health thresholds" note (the six clear via real
coverage). Verified only at the cycle-2 audit.

## 12. Close-out behavior

Standard close-out; mark AF-013 Delivered on merge; reconcile the baseline
Fallow-verdict and campaign-queue rows.
