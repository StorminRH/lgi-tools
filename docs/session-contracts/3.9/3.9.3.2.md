## Session 3.9.3.2 — EVE image resolver & app-wide adoption

**Sub-version:** 3.9.3.2 (Phase 3 — Backlog clearance; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.2
**UX gate:** Yes

## 1. Objective

One shared per-intent descriptor resolver decides which EVE image rendition
every surface shows; the drifted per-call-site decisions collapse onto it, and
the `/industry` landing rows upgrade from monogram initials to product icons.

**Verdict & evidence (backlog: "EVE images", Ryan-directed 2026-07-10).**
*Create/Expand* — the decision layer over the existing render door.
`TypeIcon`/`EveImage` already own rendering (3.8.2.6: sole `next/image`
importer, host + variants + monogram fallback); what is duplicated and drifted
is the *choice* of rendition per call site. The per-intent rule from the
backlog entry is the spec: show-the-item surfaces → product `icon` (hero
upgrades to `render` for renderable categories); show-what-you-run surfaces →
producing blueprint/formula `bp`; monogram stays the terminal fallback.

## 2. Current context and dependencies

- No dependencies. Waits for the 3.9.2.7 ledger only for its ledger-row
  update, never for its delivery.
- The two seed helpers (`nodeIcon`, `isRenderableCategory` in
  `industry-styles.ts`) promote to `src/data/eve-data/` (SDE category
  semantics live there; feature→data is boundary-legal).

## 3. Done conditions

- Every EVE-image call site consumes the resolver (landing-page
  Recents/Templates/Active-jobs/Corp-jobs rows, `SavedPlanRowItem`,
  `GlobalSearch`, planner nodes/hero, with the needed ids threaded).
- The monogram→icon landing change passes Ryan's browser review.
- `CharacterPortrait` and corp/structure icons stay out (different image
  families).

## 4. In scope

The resolver module in `src/data/eve-data/`, the seed-helper promotion, every
EVE-image call site listed above, the restricted-import rail.

## 5. Out of scope

- `CharacterPortrait` and corp/structure icon families.
- `EveImage`/`TypeIcon` rendering behavior (the render door is consumed
  as-is).
- Any new image variants or CCP endpoints.

## 6. Hard constraints

- **Rail:** restricted-import — variant strings / rendition decisions banned
  outside the resolver; call sites pass intent, not variant.
- The 3.9.2.7 primitive ledger gains its row (post-audit ledger update —
  living state).
- Diagnosis-first does not apply (not a bug fix), but UX-touching: pause for
  Ryan's local review before the PR opens.
- Shipped backlog entry deleted in the same close-out (one home).

## 7. Decisions the session plan must resolve

- Resolver surface shape (intent vocabulary; how hero render-upgrades
  declare renderable categories).
- Id-threading strategy for call sites that lack the producing
  blueprint/formula id today.

## 8. Acceptance criteria

- Grep: zero rendition decisions outside the resolver.
- The rail is red on a seeded out-of-resolver variant string, green live.
- `/industry` landing rows show product icons with monogram fallback.
- Ryan's browser review passed.

## 9. Verification

- ux-check sweep (desktop + mobile) on affected routes + operator review.
- `pnpm verify` + coverage-backed Fallow green.
- Grep evidence in PR notes; backlog entry deleted.
- Changelog entry + APP_VERSION bump (3.9.3.2).

## 10. UX/operator gates

**UX gate: Yes.** ux-check evidence plus Ryan's local browser review before
the PR opens.

## 11. Baseline/hotspot boundary

Improves (deletes drifted per-site decisions). No baseline hotspot row
touched (new module in `src/data/eve-data/`). No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: ux-check + operator review, then full close-out —
`pre-pr-design-review`, PR, Greptile loop, clean merge, production
reconciliation. Roadmap row 3.9.3.2 → terminal on merge evidence.
