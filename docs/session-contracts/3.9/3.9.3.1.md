## Session 3.9.3.1 — Backlog triage & hygiene sweep

**Sub-version:** 3.9.3.1 (Phase 3 — Backlog clearance; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.1
**UX gate:** No

## 1. Objective

The backlog reflects post-3.8 reality: every entry is live, accurately sized,
and correctly triggered — or gone.

## 2. Current context and dependencies

- No dependencies; **runs first in Phase 3**. Phase 3 may interleave with
  Phases 1–2 only after this slice.
- `docs/backlog.md` is 486 lines across ~14 areas.

## 3. Done conditions

- Stale/superseded entries are deleted with their supersession named —
  verified candidates: the `<img>` → next/image migration entry (superseded by
  3.8.2.6's `EveImage` pipeline), the CLAUDE.md bare-filename/tech-stack entry
  (superseded by the thin-adapter rewrite), and any others the sweep proves
  shipped.
- Executed-in-session hygiene: the leftover fallow-trial artifacts in
  gitignored `docs/` (748 KB `fallow-audit-full.json` et al.) are removed and
  the lone `fallow-trial-log.mjs` lint warning cleared; the CLOSED fallow
  code-health section collapses to an archive pointer per doc hygiene.
- Remaining entries get their size/trigger re-verified against the 3.9 plan
  (e.g. per-row staleness UX now notes 3.9.2.3 as its data layer).
- A short dispositions report lists anything the sweep believes belongs in 3.9
  beyond the planned slices, for Ryan's call via the amendment path.

## 4. In scope

`docs/backlog.md`, the gitignored artifact cleanup, the one-line lint fix.

## 5. Out of scope

- Implementing any entry.
- Deleting anything not provably shipped/superseded.

## 6. Hard constraints

- Arc-wide (Phase 3): nothing feature-shaped is absorbed under a cleanup
  label; one home — each deletion names its supersession.
- The feature clusters the master plan explicitly keeps deferred (fees &
  margin, whole-tree build time, asset tracking, market-score tempering,
  sites editorial, navigation, roster Phase B, a11y pass, React DOM test
  stack, F3 bundle trim, client-settled static) keep their entries and
  triggers unless provably shipped.

## 7. Decisions the session plan must resolve

- The evidence bar for "provably shipped/superseded" per candidate entry.
- The dispositions-report format for potential 3.9 additions.

## 8. Acceptance criteria

- The diffed backlog shows each deletion with its supersession evidence.
- The fallow-trial artifacts are gone and the lint warning cleared.
- Re-verified sizes/triggers on remaining entries.
- The dispositions report exists for Ryan.

## 9. Verification

- `pnpm verify` green (includes the cleared lint warning); drift gate green.
- The diffed backlog + dispositions report in session evidence.
- Changelog entry + APP_VERSION bump (3.9.3.1).

## 10. UX/operator gates

**UX gate: No.** Operator touch: Ryan's call on the dispositions report (via
amendment path; not a session blocker).

## 11. Baseline/hotspot boundary

Neutral. No hotspot contact. No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.3.1 →
terminal on merge evidence.
