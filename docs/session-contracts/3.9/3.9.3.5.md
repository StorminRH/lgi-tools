## Session 3.9.3.5 — Update-watch routine (report-only dependency/service watch)

**Sub-version:** 3.9.3.5 (Phase 3 — Backlog clearance; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.5
**UX gate:** No

## 1. Objective

The report-only dependency/service watch deferred from 3.8.4.9 ships: a
committed baseline (every dependency, the platform services, the EVE developer
surface), a self-contained routine skill, and a daily cloud routine that opens
a GitHub digest issue only on deltas.

## 2. Current context and dependencies

- No hard dependencies; after Phase 1 keeps the drift-gate wiring stable
  (soft ordering).
- Evidence: backlog "Infra & bundle"; operator decision 2026-07-14 to revisit
  in the active version — 3.9 is it.
- This is an agent-tools primitive in the taxonomy — it joins the 3.9.2.7
  ledger's map.

## 3. Done conditions

- The routine is strictly report-only — no package changes, baseline edits,
  commits, or PRs.
- Digests prioritize major versions and security advisories.
- Per the backlog's own caveat, the session plan re-verifies current Claude
  Code cloud-routine/scheduling documentation before any account or network
  access is configured (find-docs rule).
- The committed baseline covers every dependency, the platform services
  (Vercel, Neon, Convex, Upstash), and the EVE developer surface.

## 4. In scope

The baseline file, the routine skill (both runtimes, manifest revision bump),
the scheduled routine, the digest format.

## 5. Out of scope

- Renovate or any auto-merge behavior (advisory-only was the standing
  decision).
- Acting on any finding.

## 6. Hard constraints

- Report-only is a hard property, not a default: the routine has no write
  path to the repo.
- Skill-pair parity + manifest revision bump in the same change.
- Current scheduling/cloud-routine docs verified before configuration
  (find-docs rule); no remembered APIs.

## 7. Decisions the session plan must resolve

- Digest cadence/quietness rules (what counts as a delta worth an issue).
- Baseline file format and where the routine reads it from.
- The scheduling mechanism, chosen from current verified documentation.

## 8. Acceptance criteria

- One live digest run (or a seeded-delta demonstration) produces a correct
  GitHub digest issue.
- A no-delta run opens nothing.
- The baseline file is committed and complete per §3.

## 9. Verification

- The live-run or seeded-delta evidence recorded.
- Drift gate green at the bumped revision; `pnpm verify` green.
- Changelog entry + APP_VERSION bump (3.9.3.5).

## 10. UX/operator gates

**UX gate: No.** Operator touch: any account/network access configuration is
surfaced to Ryan before it happens.

## 11. Baseline/hotspot boundary

Neutral. No hotspot contact. No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.3.5 →
terminal on merge evidence; backlog entry deleted in the same close-out.
