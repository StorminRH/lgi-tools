## Session 3.9.3.3 — Invalid-route rendering fix (React #419)

**Sub-version:** 3.9.3.3 (Phase 3 — Backlog clearance; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.3
**UX gate:** No

## 1. Objective

The correct 404/noindex response for an invalid site detail stops emitting
React #419 in production runtime.

## 2. Current context and dependencies

- No dependencies.
- **Evidence (backlog: "Sites & content").** Known production runtime error; a
  prior bounded candidate (3.8.4.1.1) failed its production-preview gate and
  was reverted, so the naive fix is already falsified.

## 3. Done conditions

- Diagnosis-first per the house rule: verify, reproduce, root-cause, and
  present evidence **before** any fix.
- The fix preserves the correct 404 + noindex contract.
- The fix is verified against a production-mode environment via the documented
  manual-preview exception (the one case local dev cannot represent — the
  prior revert proves it); the preview is torn down afterward.

## 4. In scope

The invalid-site render path.

## 5. Out of scope

- The sites editorial layer.
- Route-mode changes elsewhere.

## 6. Hard constraints

- Diagnosis before prescription: if asked only to diagnose, stop after
  reporting; the fix follows only from the evidenced root cause.
- The manual `vercel deploy` preview is the sanctioned exception (local data
  cannot represent the behavior); remove the preview promptly afterward.
- Never run a production build locally — the production-mode check happens on
  the Vercel preview only.

## 7. Decisions the session plan must resolve

- Reproduction strategy (what makes #419 observable and measurable in the
  preview).
- The fix approach, chosen only after the root cause is evidenced (the
  3.8.4.1.1 revert falsifies the naive candidate).

## 8. Acceptance criteria

- A written diagnosis with reproduction and root cause precedes the fix.
- A production-preview check shows the invalid-site 404 path clean of #419
  with the 404 + noindex contract intact.
- The preview is torn down.

## 9. Verification

- The diagnosis write-up + production-preview readout in PR notes.
- `pnpm verify` + coverage-backed Fallow green; route classification
  unchanged (or justified).
- Backlog entry deleted; changelog entry + APP_VERSION bump (3.9.3.3).

## 10. UX/operator gates

**UX gate: No** (error-path only).

## 11. Baseline/hotspot boundary

Neutral. No hotspot contact. No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.3.3 →
terminal on merge evidence.

## 13. Operator-approved workflow rider (2026-07-18)

Before PR review, retire the standalone `drive-session` skill and replace its
stage-wide delegated-execution policy with one global Claude subagent rule:

- every Claude subagent runs headlessly through the Codex CLI as
  `gpt-5.6-sol`;
- effort maps to the Claude seat the task would otherwise use: high for Opus
  4.8, medium for Sonnet, and low for Haiku;
- every background task title exposes `gpt-5.6-sol@<effort>` and its bounded
  purpose;
- xhigh is reserved for an explicit workflow requirement or escalation;
- every complete session, version, version-audit, or audit-remediation plan
  receives a fresh read-only xhigh adversarial review before approval;
- approved-plan persistence remains session-terminal; no generic skill
  delegates an entire implementation session.

Update the canonical agent policy, Claude runtime adapter, both planning skill
trees, audit-planning handlers, triage's broad-sweep path, policy manifest, and
current SCRATCHPAD guidance. The drift gate must pass at the new policy
revision, and both retired skill files must be absent.
