## Session 3.9.3.6 — Planner polish pair

**Sub-version:** 3.9.3.6 (Phase 3 — Backlog clearance; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.6
**UX gate:** Yes

## 1. Objective

Two ratified-small UX deferrals land together: the multibuy panel's
always-built product row (XS — a pinned, checked, disabled product row above
Tier 1), and the primitive-reference fidelity polish (S — Tooltip surface
treatment, checked-Checkbox reference comparison, Field density/invalid states
in the admin preview).

## 2. Current context and dependencies

- No dependencies.
- Both items carry ratified backlog specs; this session implements to spec,
  not to new design.

## 3. Done conditions

- Both items match their backlog specs.
- Both pass Ryan's local review.
- Both entries deleted from the backlog in the same close-out.

## 4. In scope

The multibuy panel row; the named primitive surfaces + the admin
`/preview/primitives` page.

## 5. Out of scope

- Any other planner UI deferral (the slots scope-mismatch hint stays
  likely-never).
- New primitives.

## 6. Hard constraints

- Preserve the established terminal/EVE visual identity; primitives keep
  abstract tones; reuse adopted `src/components/ui/` wrappers.
- UX-touching: ux-check sweep + Ryan's local browser review before the PR
  opens.

## 7. Decisions the session plan must resolve

- Faithful reading of each backlog spec against the current primitive set
  (anything ambiguous is resolved in the plan, not improvised in
  implementation).

## 8. Acceptance criteria

- The multibuy panel shows the pinned, checked, disabled product row above
  Tier 1.
- The named primitive-fidelity items render per spec in the admin preview.
- Ryan's local review passed for both.

## 9. Verification

- ux-check sweep (desktop + mobile) on affected routes + operator review.
- `pnpm verify` + coverage-backed Fallow green.
- Backlog entries deleted; changelog entry + APP_VERSION bump (3.9.3.6).

## 10. UX/operator gates

**UX gate: Yes.** ux-check evidence plus Ryan's local browser review before
the PR opens.

## 11. Baseline/hotspot boundary

Neutral. No baseline hotspot row touched (multibuy panel and preview surfaces
are not measured hotspots; planner hotspot files are not in scope).
No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: ux-check + operator review, then full close-out —
`pre-pr-design-review`, PR, Greptile loop, clean merge, production
reconciliation. Roadmap row 3.9.3.6 → terminal on merge evidence.
