## Session 3.9.4.1 — Smoke confirmation, security-report verification & DB-privilege hardening

**Sub-version:** 3.9.4.1 (Phase 4 — Continuity & recovery; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.4.1
**UX gate:** No

> **Amended 2026-07-19 by operator-approved master-plan amendment.** The
> original smoke-restoration objective was overtaken by events: the 3.9.3.8
> close-out ran the browser-first production smoke end-to-end successfully.
> Ryan folded the externally sourced security deep-research report
> (2026-07-19, code snapshot `141e914`, findings LGI-01…LGI-12) into this
> final 3.9 session: act only on LGI-08 (database privilege separation),
> verify and triage everything else, and close the smoke question honestly.

## 1. Objective

Three outcomes in one final session:

1. Every security-report finding is verified against live code and routed —
   a disposition register with evidence, and confirmed non-LGI-08 work in
   `docs/backlog.md`. No behavior changes for those findings.
2. LGI-08 is acted on: the database's real privilege posture is measured on a
   disposable child branch and expressed as an actor/credential/capability
   risk model; a least-privilege runtime role (migration-owned `NOLOGIN`
   privilege role + operator-owned SQL-created LOGIN credential) is proven
   against the application's workload on that branch; production credential
   cutover is prepared and operator-gated.
3. The working browser-first smoke is confirmed (bounded reproduction attempt
   of the historical failure, honest diagnosis record) and the procedure is
   recorded where close-out already points.

## 2. Current context and dependencies

- No dependencies; final session of the version before the close audit.
- **Evidence.** 3.9.3.8 shipped-ledger: browser-first production smoke
  succeeded end-to-end. Historical 3.8 failures (`Cannot redefine property:
  process`) have not been re-reproduced since the connected browser surface.
- **DB facts (verified during planning).** One privileged identity serves the
  pooled request path (`DATABASE_URL`) and the unpooled advisory-lock/cron
  path (`DATABASE_URL_UNPOOLED`); migrations run inside `pnpm vercel-build`
  under the same credential; the daily SDE refresh executes `TRUNCATE` on
  regenerable SDE tables at runtime; no RLS exists; `@neondatabase/config`
  (`neon.ts`) cannot declare roles or grants; Neon Console/API/CLI-created
  roles automatically receive `neon_superuser`, so the runtime LOGIN must be
  created via SQL with explicit safe attributes.
- **No client-reachable DB surface exists** (no Data API; all access is
  server-side behind Better Auth + Zod + Drizzle); the session records this
  as a verified non-path and a standing invariant.

## 3. Done conditions

- Disposition register covers all 12 findings with per-finding verdict
  (`confirmed | partially-confirmed | refuted | not-applicable`), file:line
  evidence, and routing; confirmed non-LGI-08 findings appear in
  `docs/backlog.md` with sizes and triggers.
- The DB-privilege runbook exists with: the actor/credential/capability
  matrix; the verified-absent client-to-DB path; the honest threat claim
  (the split contains credential leaks and app-logic/injection DDL, not full
  runtime compromise); drill readouts; credential custody/rotation; cutover +
  rollback sequence.
- The grants migration (fail-closed idempotent `NOLOGIN` role + schema/table/
  sequence grants + table-scoped SDE `TRUNCATE` + per-creator default ACLs)
  and the `resolveMigrationUrl` seam (`DATABASE_MIGRATION_URL` with
  empty-as-missing fallback to `DATABASE_URL`) pass the drill on the child
  branch: identity assertions, positive workload probes (request reads,
  auth writes, advisory locks, SDE pipeline TRUNCATE, forced bootstrap write
  branches, default-ACL proof), and negative probes (DDL, cross-table
  TRUNCATE, GRANT/REVOKE, role escalation; TEMP tested and decided, not
  assumed).
- Production credential cutover is **not** required for completion: it is
  Ryan's explicit point-of-action decision from the diagnosis. The grants
  migration shipping at merge (inert, additive) is disclosed in the PR.
- Smoke: one bounded reproduction attempt recorded; if not reproducible, the
  record states "historical failure not reproducible under the current
  connected browser surface; working configuration demonstrated." The
  procedure text lives where close-out points (`docs/PR_REVIEW.md` + the
  close-out skill). The session's own close-out smoke is the demonstrated
  end-to-end run.
- Workflow rider: mandatory planning adversarial-review effort changes
  xhigh→high across the live policy surfaces (canonical docs, four skill
  pairs, policy-manifest regexes + revision bump), preserving the distinct
  generic xhigh-escalation rule; drift gate green.

## 4. In scope

Security-report verification/triage; LGI-08 diagnosis, role design, drill,
runbook, and code/env scaffolding (`drizzle/` grants migration,
`src/db/migrate.ts` seam, `src/lib/env.ts` key, `.env.example`, `neon.ts`
pointer comment); smoke confirmation + procedure docs; the review-effort
rider; backlog routing.

## 5. Out of scope

- Implementing any non-LGI-08 finding (verify + backlog only).
- RLS policy deployment (identity-propagation campaign; batch-transaction
  mechanism recorded as viable) — future version.
- Production credential cutover + Vercel env rewire without Ryan's explicit
  point-of-action go-ahead.
- Fully isolating the owner credential from the deployed runtime (separate
  migration job) — recorded future option.
- The `/devlog` remote-cache 413 (app-wide fix later, per operator) and the
  merge-helper Greptile-thread bug — remain in backlog.
- Replacing the browser-first smoke policy with scripted HTTP.

## 6. Hard constraints

- Arc-wide (Phase 4): diagnosis and drills run against a **disposable Neon
  child branch only — never production SQL**; secrets never enter any
  mechanism, git, transcripts, or shell history; operator-mode steps are
  explicit.
- Point-of-action operator approvals (not mere disclosure): redaction-safe
  production-URL username confirmation; child-branch creation and its
  explicit deletion (no TTL reliance — `neon.ts` never auto-applies);
  production credential cutover.
- The runtime LOGIN role is created via SQL with explicit safe attributes,
  never via `neon roles create`/Console/API (those grant `neon_superuser`).
- Subagents are headless `gpt-5.6-sol` Codex workers per the routing policy;
  workers advise, the session owns every verdict.
- Diagnosis before any configuration change; evidence precedes the fix.

## 7. Decisions the session plan must resolve

Resolved by the approved implementation plan (two adversarial passes,
2026-07-19). Remaining operator decisions at execution time: the production
credential cutover go/no-go, and the TEMP-table disposition (revoke vs.
accepted-and-recorded) from drill evidence.

## 8. Acceptance criteria

- Disposition register + backlog routing for all 12 findings.
- Runbook with risk model, drill readouts (owner control vs. runtime
  subject), custody/rotation, cutover + rollback.
- Grants migration + `resolveMigrationUrl` seam + env/example entries merged;
  `resolveMigrationUrl` unit tests green; env-example drift checker green.
- Recorded smoke diagnosis + procedure text; demonstrated close-out smoke.
- Review-effort rider applied; drift gate green.
- Changelog entry + APP_VERSION bump (3.9.4.1).

## 9. Verification

- `pnpm verify`; fresh full-coverage run + origin/main-pinned coverage-backed
  Fallow at close-out.
- Drill probe readouts recorded in the runbook (the LGI-08 proof).
- `python3 .agent-local/check_agent_drift.py` green (rider + any skill text).
- Close-out per `docs/PR_REVIEW.md`, including the browser-first production
  smoke.

## 10. UX/operator gates

**UX gate: No.** Operator involvement: the three point-of-action approvals in
§6, the TEMP-table disposition, and the cutover decision.

## 11. Baseline/hotspot boundary

Neutral. No hotspot contact (`env.ts` is a protected cohesive module; the
addition is one registry key). No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out per the delivery shape (PR,
Greptile loop, merge gates, production reconciliation). Roadmap row 3.9.4.1 →
terminal on that evidence. The version's remaining step afterward is the
3.9 version-close audit.
