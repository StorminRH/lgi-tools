## Session 3.9.4.1 — Production smoke restoration (browser-control diagnosis)

**Sub-version:** 3.9.4.1 (Phase 4 — Continuity & recovery; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.4.1
**UX gate:** No

## 1. Objective

The browser-first post-merge production smoke works again from the agent
runtime, so every 3.9 close-out gets its required real-browser verification
instead of the "Browser runtime could not initialize" fallback that recurred
through 3.8.

## 2. Current context and dependencies

- No dependencies — **runs first in the version if practical**, since every
  subsequent close-out benefits.
- **Evidence.** Four 3.8 shipped-ledger entries record the smoke skipped
  (`Cannot redefine property: process`); operator context: the failure
  involves local permissions and the agent driving the Brave browser.

## 3. Done conditions

- Diagnosis-first: reproduce the failure, separate the runtime error from the
  macOS permission/browser-selection question, and present the root cause
  before any fix.
- The fix may be configuration (permissions, a designated automation
  browser/profile) rather than code; if Brave is the blocker, a dedicated
  automation profile or browser is an acceptable outcome — **Ryan decides the
  browser policy from the diagnosis**.
- Success = a demonstrated end-to-end production smoke (version, an affected
  route, an auth gate, console) run by the agent, and the procedure recorded
  where close-out already points.

## 4. In scope

The agent-runtime browser tooling, local permissions, smoke procedure docs.

## 5. Out of scope

- Replacing the browser-first policy with scripted HTTP (the policy stands —
  the edge rate-limits scripts).

## 6. Hard constraints

- Arc-wide (Phase 4): drills/diagnosis against non-destructive targets;
  secrets never enter any mechanism; operator-mode steps are explicit (this
  is an agent+Ryan hybrid where permissions dialogs are involved).
- Diagnosis before any configuration change; evidence precedes the fix.

## 7. Decisions the session plan must resolve

- The reproduction/diagnosis sequence separating runtime error from
  permission/browser-selection causes.
- The browser-policy options to present (fix Brave vs. dedicated automation
  profile/browser) — Ryan decides from the diagnosis.

## 8. Acceptance criteria

- A recorded diagnosis naming the root cause with evidence.
- One real production smoke run end-to-end by the agent (version, affected
  route, auth gate, console readout).
- The procedure recorded where close-out already points.

## 9. Verification

- The diagnosis + smoke-run readout recorded.
- Updated procedure text; drift gate green if skills changed.
- Changelog entry + APP_VERSION bump (3.9.4.1) if tracked files changed;
  otherwise the non-code completion shape with recorded evidence.

## 10. UX/operator gates

**UX gate: No.** Operator involvement: macOS permission dialogs and the
browser-policy decision are Ryan's.

## 11. Baseline/hotspot boundary

Neutral. No hotspot contact. No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: close-out per the delivery shape (PR if tracked
files changed; otherwise recorded non-code completion evidence). Roadmap row
3.9.4.1 → terminal on that evidence.
