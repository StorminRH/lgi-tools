## Session 3.9.3.8 — Public document truth pass

**Sub-version:** 3.9.3.8 (Phase 3 — Backlog clearance; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.8
**UX gate:** Yes

## 1. Objective

Every committed public-facing document tells the truth about the current app,
and the `/legal` privacy page is rewritten so it *stays* true: policy-level
wording that survives feature growth instead of enumerations that drift.

## 2. Current context and dependencies

- Prefers running after 3.9.1.1/.3 so the truth loop (SELF_REVIEW prompt +
  audit docs-truth enumeration + env-example checker) exists when the pass
  lands.
- **Evidence.** The `/legal` page currently states "we request exactly four
  read-only scopes" — presently false, and structurally doomed to keep going
  false because it enumerates. README/CONTRIBUTING/SECURITY and the PR/issue
  templates have no truth loop at all.
- Note: `docs/UI_SYSTEM_AUDIT_3_8_2_8.md` disposition may already be complete
  — its archived deletion was slated to commit on the first approved 3.9
  session branch (SCRATCHPAD rider via 3.9.1.1). Verify, don't redo.

## 3. Done conditions

- `/legal` is rewritten at policy level: what *categories* of data are stored
  (account identity, encrypted EVE tokens, ESI-mirrored gameplay data,
  in-house telemetry), the standing guarantees
  (read-only-plus-the-named-waypoint-exception access posture,
  application-layer encryption, full purge/deletion rights and how to exercise
  them, no sale of data), and the third-party processors by role (hosting,
  database, live sync, rate-limit store, CCP's ESI) — **no scope enumeration,
  no feature list**; the page points at EVE SSO's own consent screen as the
  authoritative per-scope record. Ryan approves the wording (UX gate).
- README, CONTRIBUTING (general prose), SECURITY, and the PR/issue templates
  are trued against the current app and workflow — the PR template matches
  PR_REVIEW's canonical section format.
- `.env.example` reconciled against the env registry (the 3.9.1.3 checker then
  keeps it true).
- `docs/UI_SYSTEM_AUDIT_3_8_2_8.md` is dispositioned — relocated to the
  Document Archive or explicitly kept with a stated reason (verify the rider
  already handled it).

## 4. In scope

The named documents and page.

## 5. Out of scope

- New legal obligations analysis (this is truthful-description work, not
  counsel).
- `content/devlog` (already maintained).
- Any feature copy.

## 6. Hard constraints

- Policy-level wording only on `/legal`: no scope enumeration, no feature
  list.
- **Rail:** the 3.9.1.1 loop additions + the 3.9.1.3 env-example checker;
  content truth beyond that stays a judgment gate by design.
- LGI is third-party, never first-party/official, in all public copy.

## 7. Decisions the session plan must resolve

- The `/legal` information architecture (categories/guarantees/processors) and
  draft wording for Ryan's approval.
- Per-document truth findings and their fixes (enumerated in the plan after a
  live read of each).

## 8. Acceptance criteria

- `/legal` renders the policy-level rewrite; no scope enumeration remains.
- Each named public document's diff is summarized and true against the current
  app.
- Env-example checker green.
- Ryan approved the `/legal` wording in local review.

## 9. Verification

- ux-check on `/legal` + operator review of the wording.
- `pnpm verify` green; env-example checker green; drift gate green.
- Each document's diff summarized in PR notes.
- Changelog entry + APP_VERSION bump (3.9.3.8).

## 10. UX/operator gates

**UX gate: Yes** — the `/legal` wording is Ryan's call; local review before
the PR opens.

## 11. Baseline/hotspot boundary

Neutral. No hotspot contact. No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: ux-check + operator review, then full close-out —
`pre-pr-design-review`, PR, Greptile loop, clean merge, production
reconciliation. Roadmap row 3.9.3.8 → terminal on merge evidence.
