## Session 3.9.1.1 — Lifecycle doc reconciliation & machine-readable formats

**Sub-version:** 3.9.1.1 (Phase 1 — Workflow mechanization; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.1.1
**UX gate:** No

## 1. Objective

Give every lifecycle decision exactly one home and a machine-readable form, so
the scripts in 3.9.1.2–3.9.1.5 have unambiguous state to read — and fix the
known procedure/state leaks found in the 3.8-era docs review.

## 2. Current context and dependencies

- 3.8 is archived (satisfied gate); this is the first 3.9 session and the first
  Phase 1 slice. **3.9.1.1 must merge before any other Phase 1 slice starts.**
  Everything later in the arc depends on this slice's formats.
- No dependencies within the phase.
- Rider: the staged deletion of `docs/UI_SYSTEM_AUDIT_3_8_2_8.md` (already
  archived; SHA-256 verified against the archive copy) commits on this session's
  branch per the SCRATCHPAD first-next-version rider. Do not restore the file.

## 3. Done conditions

- `docs/VERSION_AUDIT.md` Step 1 references "every known-wide surface named in
  the current baseline" generically; version-specific measurement commands live
  only in the version's audit plan. No lifecycle doc names a repo path that does
  not exist.
- Watch promotion triggers have one owner — the baseline's Watch rows (living
  state that survives version archival). The audit ledger cites the AF id
  rather than restating the trigger. The artifact-ownership table in
  `docs/DEVELOPMENT_LIFECYCLE.md` records this.
- Marker vocabulary is normalized and documented: `Execution status` is exactly
  `Pending` or `Complete`; roadmap terminal statuses are an exact closed set.
- Three machine-readable formats are specified in their owning docs (shapes in
  the master plan are proposals for the session plan to finalize):
  - **Watch trigger block** in the audit ledger — one fenced block per Watch
    finding, limited to a small comparator grammar over countable evidence.
  - **`**Baseline effect:**`** marker in session plans — exactly one of
    `Improves | Neutral | Temporary pressure`, per SESSION_PLANNING Step 7.
  - **`**UX gate:**`** marker in contracts — `Yes | No`; drives the
    operator-review pause mechanically in later slices.
- The churn-window difference between SESSION_PLANNING (proximity, rolling) and
  VERSION_AUDIT (version lens, since previous baseline) is stated as intentional
  in one sentence in each doc.
- **Planning outcomes become session-terminal:** `docs/DEVELOPMENT_LIFECYCLE.md`
  §5, `docs/SESSION_PLANNING.md` Step 9, and the `start-session` + all `plan-*`
  skills (both runtimes, manifest revision bump) state one rule: a session that
  planned an artifact never executes it. Runtime plan-mode acceptance authorizes
  artifact persistence only; after a planning handler persists its approved
  artifact, `start-session` reports the resolver's new directive and stops.
  Execution begins in a fresh `start-session`, whichever runtime runs it.
- **Committed public documents join the truth loop:** `docs/SELF_REVIEW.md`
  gains a public-documents prompt (README, CONTRIBUTING, SECURITY, PR/issue
  templates, `.env.example`, `/legal`), and `docs/VERSION_AUDIT.md` Step 3's
  docs-truth sweep explicitly enumerates that committed set alongside the
  workspace docs.
- **The comment standard is specified** (implementation lands in 3.9.1.7):
  hybrid TSDoc-lite — `/** */` interface comments on every exported surface
  (summary prose stating the contract; tags only where they add information, no
  mandatory `@param` ceremony); `//` unchanged for module prologues, rationale,
  and implementation commentary; deferred work routes to `docs/backlog.md`,
  never a source `TODO`/`FIXME`. `docs/SESSION_PLANNING.md` Step 8's output
  schema requires interface designs to include the draft interface comments
  themselves. Quality remains a judgment gate (pre-PR review §3); no
  comment-coverage or density metric is ever adopted (P10).

## 4. In scope

The lifecycle/stage docs, SESSION_CONTRACTS/PLANNING schemas, the
artifact-ownership table, format specifications, and the same-change
`policy-manifest.json` revision bump + skill-tree marker updates the drift gate
requires.

## 5. Out of scope

- Any script or resolver change (3.9.1.2+).
- Retro-editing 3.8 archived artifacts.
- Changing constitution principles (P1–P10 text is frozen this version unless a
  durable rule emerges through the audit process).

## 6. Hard constraints

- Arc-wide (Phase 1): no behavior change to application code; new checks (when
  they arrive in later slices) wire into `check_agent_drift.py` or close-out —
  never a new command an agent must remember unprompted; error = contradiction
  (blocks), warn = suspicious (reported, non-blocking).
- Standing decisions: the resolver's directive contract (stages, handlers,
  seven fields) is frozen in 3.9; legacy exemption — machine-readable
  marker/vocabulary requirements apply to 3.9-and-later artifacts, archived 3.8
  material is never retro-edited.
- Shared policy changes bump the revision in `.agent-local/policy-manifest.json`
  and update both skill trees' `shared-policy-revision` markers in the same
  change.

## 7. Decisions the session plan must resolve

- Final trigger-block grammar (which countable evidence kinds: file LOC, export
  count, file count in a zone, clone-group count).
- Whether existing 3.8-cycle artifacts are exempt from the new marker
  vocabulary (proposed: yes — formats apply from 3.9 forward; checkers treat
  pre-3.9 artifacts as legacy).

## 8. Acceptance criteria

- Every done condition above is observable in the named docs.
- Each of the three formats has a written specification in exactly one owning
  document, with vocabulary stated as a closed set.
- The planning-terminal rule appears in the lifecycle doc, SESSION_PLANNING,
  and both runtimes' `start-session`/`plan-*` skills, with the manifest
  revision bumped.
- A manual doc-ref sweep (mechanized later by 3.9.1.3) shows zero dead repo
  paths in the lifecycle docs.

## 9. Verification

- `python3 .agent-local/check_agent_drift.py` green at the new policy revision.
- Manual doc-ref sweep results recorded.
- Changelog entry + APP_VERSION bump (3.9.1.1).
- `pnpm verify` green (docs-only change; still required before commit).

## 10. UX/operator gates

**UX gate: No.** Non-UX slice; session self-finishes through close-out with no
operator pause.

## 11. Baseline/hotspot boundary

Neutral; touches no measured surface. No hotspot contact. The version's
explicit no-campaign decision stands (empty cycle-2 queue; AF-006–AF-009 remain
Watch-only).

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.1.1 →
terminal on merge evidence. The UI_SYSTEM_AUDIT deletion rider rides this PR.
