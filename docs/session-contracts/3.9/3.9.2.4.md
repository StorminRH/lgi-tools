## Session 3.9.2.4 — Endpoint contract gate (Expand)

**Sub-version:** 3.9.2.4 (Phase 2 — Primitive lifecycle; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.2.4
**UX gate:** No

## 1. Objective

A route handler cannot exist without its typed contract — the api-contract
convention becomes mechanically complete instead of convention-complete.

**Verdict & evidence.** *Expand* the existing contract convention (17
`api-contract.ts` files, `apiFetch`, `route-body.ts`, the no-user-input marker
comment already expected by an audit) into a completeness gate. Pairing route
↔ contract ↔ marker is currently enforced by review habit; the repeated shape
(schema + response type + endpoint object + marker) is assembled by hand per
route.

## 2. Current context and dependencies

- No dependencies; may interleave anywhere in Phase 2.

## 3. Done conditions

- One gate (registry test or lint audit — session plan compares) asserts for
  every `src/app/api` route: it either consumes a Zod schema from its owning
  slice's `api-contract.ts` via `route-body.ts` (or the mutation pipeline), or
  carries the no-user-input marker; its response type is exported from the
  same contract file; and every `apiFetch` call site references a declared
  endpoint object.
- Gaps found in the sweep are fixed in-slice where Floss-sized, reported
  otherwise.

## 4. In scope

The gate, a routes sweep, small conformance fixes, CONTRIBUTING/agent-guide
sentence updates.

## 5. Out of scope

- Changing any contract's shape or any route's behavior.
- The mutation pipeline (AF-001 Verified in cycle 2 — its shape is preserved,
  not extended).
- OpenAPI-style codegen (backlog if ever).

## 6. Hard constraints

- **Rail:** this slice *is* a rail — the completeness gate itself. Its
  enforcement evidence is the seeded contractless-route failure.
- Combine-bias: prefer expanding the existing API-contract audit over a new
  sibling mechanism.
- Protected non-goals off-limits; anything campaign-shaped stops and reports.

## 7. Decisions the session plan must resolve

- Gate mechanism (extend the existing API-contract audit vs. new registry
  test — prefer expanding the existing one, combine-bias).
- How the sweep classifies the cron/webhook family (bearer-auth marker vs.
  contract).

## 8. Acceptance criteria

- Gate **red** on a seeded contractless route; green on the live tree.
- Sweep findings ledgered (fixed in-slice or reported) in the PR notes.
- Every `apiFetch` call site references a declared endpoint object.

## 9. Verification

- `pnpm verify` + coverage-backed Fallow green; drift gate green if guides
  changed.
- Seeded-violation demonstration + sweep ledger in PR notes.
- Changelog entry + APP_VERSION bump (3.9.2.4).

## 10. UX/operator gates

**UX gate: No.** Session self-finishes through close-out.

## 11. Baseline/hotspot boundary

Neutral-to-Improves (may delete drifted contract remnants). **Hotspot note:**
the AF-001 mutation-pipeline shape is preserved, not extended. No-campaign
decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.2.4 →
terminal on merge evidence.
