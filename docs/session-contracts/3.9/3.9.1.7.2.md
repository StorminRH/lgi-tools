## Session 3.9.1.7.2 — Comment standard enforcement (authoring + lint flip)

**Sub-version:** 3.9.1.7 (Phase 1 — Workflow mechanization; session 2 of 2, one branch)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.1.7
**UX gate:** No

## 1. Objective

Second half of the migration: every uncommented exported surface gets an
authored interface comment to the standard, stray `TODO`s are dispositioned to
the backlog, and the three lint rules flip on and pass repo-wide — never a
two-style period, never a changed-files-only carve-out.

## 2. Current context and dependencies

- Depends on 3.9.1.7.1 (the conversion sweep and its uncommented-export
  worklist) on the same branch.
- Authored comments are the judgment half of the slice and are reviewed as
  such — restating the signature fails the pre-PR §3 bar.

## 3. Done conditions

- Every exported surface with no comment gets one **authored** to the standard
  (contract prose: what it does, units, preconditions, what the caller owns).
- Three lint rules flip on and pass repo-wide in this sub-version:
  - doc-comment presence on exports (`eslint-plugin-jsdoc` require-jsdoc
    scoped to exported declarations);
  - TSDoc syntax validation (`eslint-plugin-tsdoc`);
  - the TODO ban (`no-warning-comments` for `TODO`/`FIXME` — deferrals route
    to `docs/backlog.md`).
- Any `TODO`s the sweep found are dispositioned to the backlog, not deleted.
- Zero executable-code changes: `pnpm verify` green with an unchanged test
  count, and the branch diff is comments-plus-lint-config only.
- The sweep tooling from session .1 is deleted (scratch-script hygiene).

## 4. In scope

The worklist authoring across `src/` and `convex/` production exports; the
lint config; backlog routing of found `TODO`s; tooling deletion.

## 5. Out of scope

- Rewording any existing comment.
- Test files and `scripts/` (per the exemption set session .1's plan
  confirmed).
- Doc-comment tags beyond TSDoc-lite (no mandatory `@param` ceremony).
- Any code change.

## 6. Hard constraints

- The three lint rules **are** the rail; no changed-files-only carve-out.
- Suppression count must not grow (a file that can't satisfy TSDoc syntax gets
  fixed, not suppressed); the five generated Convex header suppressions must
  not trip TSDoc validation.
- Comment quality is judged (pre-PR §3), never metricized (P10).

## 7. Decisions the session plan must resolve

- Any residual exemption-set adjustments the .1 dry-run exposed.
- Ordering/batching of the authoring work (by slice, by area) for reviewable
  commits.

## 8. Acceptance criteria

- All three rules green repo-wide.
- Presence-lint demonstrated **red** on a seeded uncommented export.
- The uncommented-export worklist is closed.
- Found `TODO`s appear in `docs/backlog.md` with context, and no
  `TODO`/`FIXME` remains in production source.
- Unchanged test count; comments-plus-lint-config-only branch diff.

## 9. Verification

- `pnpm verify` green (zero-warning lint now includes the three rules).
- Seeded-violation demonstration recorded; spot-diff sample in the PR notes
  proving verbatim conversion held across the branch.
- Coverage-backed Fallow per close-out; drift gate green.
- Changelog entry + APP_VERSION bump (3.9.1.7); `UX gate: No`.

## 10. UX/operator gates

**UX gate: No.** Session self-finishes through close-out.

## 11. Baseline/hotspot boundary

Neutral — comments only; suppression count unchanged or lower. No hotspot
contact. No-campaign decision preserved.

## 12. Close-out behavior

Slice-final session: full close-out — `pre-pr-design-review`, single PR for
the 3.9.1.7 branch, Greptile loop, clean merge, production reconciliation.
Roadmap row 3.9.1.7 → terminal on merge evidence.
