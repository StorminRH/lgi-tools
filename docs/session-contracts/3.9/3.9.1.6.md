## Session 3.9.1.6 — Drift-manifest derivation

**Sub-version:** 3.9.1.6 (Phase 1 — Workflow mechanization; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.1.6
**UX gate:** No

## 1. Objective

The drift gate's hand-synced expectations become derived wherever a source of
truth already exists, cutting the per-change maintenance cost the manifest
currently imposes.

## 2. Current context and dependencies

- Best run **last in the Phase 1 arc** so it derives from the formats and
  wiring the earlier slices landed.
- `sessionContracts.expected` in `.agent-local/policy-manifest.json` is today a
  hand-listed path set that must be re-edited at every version boundary (the
  3.8 archive required manually emptying it; 3.9 contract creation required
  manually populating it).

## 3. Done conditions

- `sessionContracts.expected` is derived from the active version's `INDEX.md`
  (plus the resolver's active-version discovery) instead of a hand-listed path
  set; the per-version update ritual disappears.
- Required skill paths are derived from `pairedSkills` + `skillRoots` rather
  than enumerated twice.
- The regex-phrase requirements are reviewed pair-by-pair: each either stays
  (with a one-line note of the drift it guards) or is replaced by a structural
  check (marker present, doc referenced, revision stamp) — the goal is fewer
  prose-coupled patterns, not zero.
- `check_agent_drift.py` behavior is otherwise unchanged and its full check
  suite still fails on each seeded violation class it failed on before.

## 4. In scope

`check_agent_drift.py`, `policy-manifest.json`, `bump_policy_revision.py` if
the revision flow changes, fixtures.

## 5. Out of scope

- Weakening any existing guarantee.
- Touching the tooling-parity scripts (Vercel adapter flow is stable).
- Skill content changes beyond revision stamps.

## 6. Hard constraints

- Arc-wide (Phase 1): Python 3 stdlib only; `--check`/`--pretty`; error vs.
  warn; fixtures per the established pattern; no application-code behavior
  change.
- Proposed derivation point: check time — no generated files to drift (the
  session plan confirms or argues the alternative).

## 7. Decisions the session plan must resolve

- Which phrase requirements are genuinely load-bearing vs. legacy (present the
  keep/replace table for approval).
- Whether derivation happens at check time or via a generated section in the
  manifest (proposed: check time).

## 8. Acceptance criteria

- A before/after manifest diff shows the removed hand-synced lists.
- The seeded-violation matrix is green: every violation class the gate failed
  on before, it still fails on.
- The keep/replace table for phrase requirements is recorded and approved.

## 9. Verification

- Fixture suites green; seeded-violation matrix demonstrated.
- `python3 .agent-local/check_agent_drift.py` green at the bumped revision.
- Changelog entry + APP_VERSION bump (3.9.1.6); `pnpm verify` green.

## 10. UX/operator gates

**UX gate: No.** Session self-finishes through close-out.

## 11. Baseline/hotspot boundary

Neutral. No hotspot contact. No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.1.6 →
terminal on merge evidence.
