## Session 3.9.2.8 — Planner freshness consumption (PL-011 Expand)

**Sub-version:** 3.9.2.8 (Phase 2 — Primitive lifecycle, approved extension; one session)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.2.8
**UX gate:** No

## 1. Objective

Make the shared ESI freshness primitive the planner's sole authority for
price-staleness boundaries without changing the planner's refresh or
rendering behavior.

**Verdict & evidence.** *Expand* (PL-011, approved 2026-07-17).
`src/lib/esi-datasets/freshness.ts` owns the exact `staleAfter <= now`
boundary (`isBoundaryStale`), but
`src/features/industry-planner/industry-styles.ts` repeats the comparison
for per-row confidence and aggregate shortfall counts. The planner
intentionally refreshes the whole price set on view; that behavior must
remain.

## 2. Current context and dependencies

- 3.9.2.7 recorded and Ryan approved PL-011. Independent of the other
  approved extension rows (3.9.2.9, 3.9.2.10).
- Live duplication: `priceConfidence` and its aggregate classifier in
  `industry-styles.ts` each compare `staleAfterMs <= nowMs` against a
  `number | null` shape, while the shared primitive compares
  `Date | undefined` — the consumption seam must bridge that shape without
  moving planner policy into `src/lib/`.

## 3. Done conditions

- Planner price-confidence and aggregate-staleness derivations consume the
  exact `staleAfter <= now` semantics owned by
  `src/lib/esi-datasets/freshness.ts`; no equivalent boundary comparison
  remains in `industry-styles.ts`.
- The planner still refreshes the complete price set on view, so the
  existing visual confirmation behavior is unchanged.
- The AF-005 pricing contexts do not widen.

## 4. In scope

The shared freshness surface needed by the planner, its price-staleness
derivations, and focused behavior tests.

## 5. Out of scope

- Changing price-refresh cadence, data placement, confidence labels, or UI
  appearance.
- The `PricingContextValue` / `usePricing` / `useBuildPlan` ownership split
  (the five-concern taxonomy is AF-005 Verified state).

## 6. Hard constraints

- Behavior-preserving: same verdicts at the same instants, including the
  exact boundary instant.
- Planner policy (what confidence levels mean, reason strings, aggregation)
  stays feature-owned; only the staleness boundary semantics move to the
  shared primitive.
- Hotspot guard: `PricingProvider.tsx` (902 LOC, monitored-not-actionable)
  is not touched by adjacent work; add nothing to the pricing concern
  contracts.
- Protected non-goals off-limits; anything campaign-shaped stops and
  reports.

## 7. Decisions the session plan must resolve

- The smallest browser-safe freshness surface that eliminates the
  duplicated boundary without moving planner policy into `src/lib/` (the
  entries-manifest import weight and the `Date | undefined` vs
  `number | null` shape bridge are the live tensions).
- The characterization cases that prove visible behavior is unchanged
  (exact-boundary instant, null/missing price row, aggregate counts).

## 8. Acceptance criteria

- Focused exact-boundary and aggregate-confidence tests green.
- No direct planner staleness comparison remains (grep-verifiable in
  `industry-styles.ts`).
- AF-005 concern-contract field counts unchanged.

## 9. Verification

- `pnpm verify` + coverage-backed Fallow green.
- Changelog entry + APP_VERSION bump (3.9.2.8).

## 10. UX/operator gates

**UX gate: No.** The approved end-state is behavior-preserving; session
self-finishes through close-out.

## 11. Baseline/hotspot boundary

Neutral. The plan must treat the planner pricing surfaces as an existing
hotspot and avoid unrelated cleanup. No-campaign decision preserved.

## 12. Close-out behavior

Single-session sub-version: full close-out — `pre-pr-design-review`, PR,
Greptile loop, clean merge, production reconciliation. Roadmap row 3.9.2.8 →
terminal on merge evidence; ledger row PL-011 → Delivered in the same
close-out.
