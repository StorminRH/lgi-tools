## Session 3.9.3.4.2 — Local dev performance (sample-data mode + readout)

**Sub-version:** 3.9.3.4 (Phase 3 — Backlog clearance; session 2 of 2, one branch)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.4
**UX gate:** No

## 1. Objective

A dev-only sample-data mode completes the local-dev performance work, and the
sub-version delivers its measured before/after cold `/sites` readout.

## 2. Current context and dependencies

- Depends on 3.9.3.4.1 (profile + lazy rendering) on the same branch.

## 3. Done conditions

- The dev sample-data mode is **dev-only and clearly labeled**; production
  behavior and data paths are untouched.
- Cold `/sites` in dev is measured before/after across the branch and the
  readout recorded.

## 4. In scope

The dev-only sample-data mode; the measured before/after readout; branch
close-out.

## 5. Out of scope

- Any production data path or rendering change beyond session .1's.
- The sites editorial layer.

## 6. Hard constraints

- The sample mode must be impossible to enable in production (dev-gated, not
  env-toggled in prod).
- Production route modes unchanged in `route-classification.json` unless
  justified (carried from session .1).

## 7. Decisions the session plan must resolve

- Sample-data shape and the gating mechanism (how dev-only is enforced).
- The measurement protocol for the final readout (cold-start definition,
  repetitions).

## 8. Acceptance criteria

- Sample mode works in dev, clearly labeled, and is provably absent from
  production builds/routes.
- Before/after cold `/sites` timings recorded, showing the >60s stall
  resolved.

## 9. Verification

- `pnpm verify` + coverage-backed Fallow green.
- Before/after timings + unchanged production route modes (or the justified
  diff) in PR notes; Ryan's local review of the branch's user-facing change
  (from session .1) confirmed before PR.
- Backlog entry deleted; changelog entry + APP_VERSION bump (3.9.3.4).

## 10. UX/operator gates

**UX gate: No** for this session's dev-only work; the branch's PR carries
session .1's operator-review evidence.

## 11. Baseline/hotspot boundary

Neutral-to-Improves. No baseline hotspot row touched. No-campaign decision
preserved.

## 12. Close-out behavior

Slice-final session: full close-out — `pre-pr-design-review`, single PR for
the 3.9.3.4 branch, Greptile loop, clean merge, production reconciliation.
Roadmap row 3.9.3.4 → terminal on merge evidence.
