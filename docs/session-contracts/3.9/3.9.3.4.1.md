## Session 3.9.3.4.1 — Local dev performance (profile + lazy detail rendering)

**Sub-version:** 3.9.3.4 (Phase 3 — Backlog clearance; session 1 of 2, one branch)
**Master plan:** `docs/VERSION_3_9_PLAN.md` §3.9.3.4
**UX gate:** Yes

## 1. Objective

A cold `/sites` in `pnpm dev` stops taking >60s, blocking the Node event loop,
and driving the machine into swap: this session confirms the stall by
profiling, then ships the lazy detail-body rendering step.

## 2. Current context and dependencies

- No dependencies. Session .2 (dev sample-data mode + measured readout) shares
  this branch.
- **Evidence (backlog: "Infra & bundle"; full diagnosis in
  `docs/DEV_PERF_DIAGNOSIS.md`).** Root cause already diagnosed: the page
  assembles the entire catalogue and eagerly server-renders every site's full
  detail body per request with no prerender warm cache. The doc's A/B/C plan
  is the session-plan seed.

## 3. Done conditions

- The stall is profiled and confirmed (diagnosis-first — the doc predates
  3.8's changes); the before measurement is recorded.
- The lazy-render step ships behavior-preserving, helping dev *and* prod, with
  the prod rendering modes unchanged in `scripts/route-classification.json`
  unless justified.

## 4. In scope

Profiling the cold `/sites` dev render; the lazy detail-body rendering change;
its tests.

## 5. Out of scope

- The dev-only sample-data mode (session .2).
- The sites editorial layer; unrelated dev-server tuning.

## 6. Hard constraints

- Diagnosis before prescription: confirm the DEV_PERF_DIAGNOSIS root cause
  still holds before implementing.
- Production route modes unchanged unless justified in
  `route-classification.json`.
- UX-touching (rendering change on a user-facing page): Ryan reviews locally
  before the sub-version's PR opens (gate rides the slice-final session if the
  branch merges once).

## 7. Decisions the session plan must resolve

- Profiling method and the stall's measurable signature.
- The lazy-render mechanism (how detail bodies defer without changing the
  page's visible behavior or prod modes).

## 8. Acceptance criteria

- Recorded before-profile confirming the diagnosed stall.
- Lazy rendering in place with unchanged visible behavior and unchanged (or
  justified) prod route modes.
- Cold `/sites` dev timing measurably improved (final before/after readout
  completes in session .2).

## 9. Verification

- `pnpm verify` green; ux-check sweep on `/sites` desktop + mobile.
- Verified commit/push on the 3.9.3.4 branch (no PR yet).

## 10. UX/operator gates

**UX gate: Yes.** ux-check evidence plus Ryan's local review of `/sites`
(operator review may run at the branch's PR boundary in session .2, but the
rendering change is demonstrated to Ryan from this session's work).

## 11. Baseline/hotspot boundary

Neutral-to-Improves. No baseline hotspot row touched. No-campaign decision
preserved.

## 12. Close-out behavior

Mid-slice session: ends with a verified commit/push to the shared 3.9.3.4
branch and updated session memory — **no PR**. Session .2 completes the
sub-version and opens the single PR.
