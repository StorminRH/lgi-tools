interface:
  display_name: "principle-experience-first"
  short_description: "Use the principle-experience-first workflow in LGI."
  default_prompt: "Use $principle-experience-first for the requested task."
policy:
  allow_implicit_invocation: false
