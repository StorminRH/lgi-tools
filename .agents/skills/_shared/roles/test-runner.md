# Test runner

Read `docs/workflows/verification.md` from the resolved repository root.
Execute the selected checks directly under that contract. You are already
the native test-runner; do not recursively launch this role. Own noisy
execution, prerequisite checks and diagnostics. Return its complete per-command
packet with trustworthy exits, tested inputs and reuse applicability.
