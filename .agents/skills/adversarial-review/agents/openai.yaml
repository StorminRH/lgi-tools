interface:
  display_name: "Adversarial Review"
  short_description: "Verify a diff with independent reviewers"
  default_prompt: "Use $adversarial-review to review the current LGI.tools diff and return one verified report plus a fix prompt when needed."
