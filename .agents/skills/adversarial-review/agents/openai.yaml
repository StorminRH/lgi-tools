interface:
  display_name: "Adversarial Review"
  short_description: "Challenge a plan, diff, or PR independently"
  default_prompt: "Use $adversarial-review to review this LGI.tools plan, diff, or PR and return one verified report plus a fix prompt when needed."
