interface:
  display_name: "principle-fix-root-causes"
  short_description: "Use the principle-fix-root-causes workflow in LGI."
  default_prompt: "Use $principle-fix-root-causes for the requested task."
policy:
  allow_implicit_invocation: false
