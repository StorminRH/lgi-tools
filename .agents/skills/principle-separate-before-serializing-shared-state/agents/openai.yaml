interface:
  display_name: "principle-separate-before-serializing-shared-state"
  short_description: "Use the principle-separate-before-serializing-shared-state workflow in LGI."
  default_prompt: "Use $principle-separate-before-serializing-shared-state for the requested task."
policy:
  allow_implicit_invocation: false
